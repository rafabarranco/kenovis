<!-- PROJECT-SPECIFIC: this product's own context, not framework. Authored by /init-project or /adopt-project; kenovis sync never overwrites it. -->

ROADMAP.md

Product Roadmap

Version: 1.81
---
Purpose

This document defines the strategic product evolution of the platform.

It establishes:

- What we build.
- When we build it.
- Why we build it.
- What we deliberately avoid.

The roadmap exists to protect product focus.

Closed items live in `PRODUCT/ROADMAP-ARCHIVE.md`, verbatim — one line stays here per closed item, with a pointer. A superseded round narrative — everything a round wrote between one `Next:` pointer and the next, once a later pointer replaces it — is closed work by the same rule and archives the same way; only the single most recent `Next:` pointer stays inline, since that is the only one `/next` Step 3 ever reads. Finished work was costing the same to load as scheduled work, on every session's context, forever. The archive is read on demand; this document is the one that is read at session start.

Findings that earlier rounds did not fix live in "Open Findings" below, not in the narrative of the item that found them. `/next` reads that queue alongside the scheduled items.
---
Product Vision

Kenovis matures into the default operating system a small team installs the moment it decides to build something real — giving that team, from day one, the specialized roles (product, architecture, security, design) it can't yet afford to hire.
---
Product Strategy

Single-segment-first, then land-and-expand across verticals. Phase 0-1 prove the CLI/template distribution model with software development teams (the only Vertical that exists). Phase 2 proves retention and layers the open-core paid tier. Phase 3 extends the same operating model to adjacent professional practices (legal, accounting). Phase 4 becomes shared infrastructure across verticals (marketplace of Agent Rosters, optional hosted layer).
---
Product Development Principles

Principle 1: Solve workflow problems

We do not build isolated features.

We build solutions to operational workflows.

Example:

Bad:

"Add [isolated feature]."

Good:

"[Reduce the time/friction of the actual workflow the user is trying to complete.]"
---
Principle 2: Build the minimum complete workflow

A feature is not complete when a screen exists.

A workflow is complete when the user can achieve their goal.

Example:

Not complete:

- [Single screen in isolation.]

Complete:

- [Full sequence of steps the user needs, start to finish.]
---
Principle 3: Avoid premature expansion

Do not build for future markets before winning the first market.

The first customer segment provides the learning foundation.
---
Product Phases
---
Phase 0 — Foundation

Objective

Build Kenovis's own Product layer (this document set) and a real CLI installer/sync tool for distributing the Framework layer.

Duration

Not yet estimated.

Users

Internal — Kenovis dogfoods itself on this repository.

Goal

Build the minimum CLI tooling required to install and sync the AI-OS into an external repository.
---
Deliverables

Technical:

- CLI command that scaffolds the Framework layer (AI/, CLAUDE.md, README.md) into a target repository.
- CLI install command detects whether the target repository is greenfield (empty or no real implementation) or brownfield (a real implementation already exists in the target repository) and points the installation at /init-project or AI/commands/adopt-project.md accordingly, instead of always assuming a blank slate.
- CLI command that syncs a newer Framework Release into an existing Installation without touching Product-layer files (RULE-INST-01).
- No database, no authentication, no hosted service (DECISION-013).

Documentation:

- Domain model (done — see DOMAIN/DOMAIN_MODEL.md).
- Architecture (done — see ENGINEERING/ARCHITECTURE.md).
- Development standards (existing AI/policies/, unchanged by this initialization).
---
Success Criteria

A team outside Kenovis can install the CLI, get the Framework layer into their repository, and complete /init-project (greenfield) or /adopt-project (brownfield, existing codebase) with their own real answers — end to end, without help.
---
Phase 0 — Immediate Priority (added 2026-08-05, from /analyze on distribution friction)

Founder-flagged blocker: today, "installing" Kenovis means manually copy-pasting this repo's structure into a target repository, and adopting an existing product means copy-pasting the customer's own code into this repo's own implementation directory to run /adopt-project — backwards from how a real install should work, and not viable for "usable by anyone." This is the highest-priority work in Phase 0, ahead of any other Phase 0/1 item, until all three land.

Execute in this order:

1. DONE (2026-08-05) — Architecture decision: Framework layer physical packaging (visible/invisible footprint). → `PRODUCT/ROADMAP-ARCHIVE.md`
2. DONE (2026-08-05) — Fix /adopt-project's install-direction assumption. → `PRODUCT/ROADMAP-ARCHIVE.md`
3. DONE (2026-08-06) — Build the CLI installer/sync tool. → `PRODUCT/ROADMAP-ARCHIVE.md`
4. DONE (2026-08-06) — Architecture decision and CLI implementation, both shipped via /next. (added 2026-08-06, from /analyze on install-flow friction, founder-flagged highest priority — ahead of Phase 1) — Auto-trigger `/init-project` / `/adopt-project` without a manual slash-command step, plus a new `kenovis add` command, cross-detection errors, and a bare `kenovis` autodetect dispatch. → `PRODUCT/ROADMAP-ARCHIVE.md`
5. DONE (2026-08-06, via /next) — Guard Product-layer files against silent overwrite during /init-project and /adopt-project, mirroring the ExistingClaudeMdError pattern. → `PRODUCT/ROADMAP-ARCHIVE.md`
6. DONE (2026-08-06, via /next) — added 2026-08-06, from /analyze on the v0.1.0/v0.2.0 → v0.3.0 upgrade path, founder-flagged maximum priority — ahead of any other Phase 0/1 item. → `PRODUCT/ROADMAP-ARCHIVE.md`
7. DONE (2026-08-06, via /next) — close the Learning-007 follow-up: the CLAUDE.md guard's append-content blind spot. → `PRODUCT/ROADMAP-ARCHIVE.md`
8. DONE (2026-08-07, via /next) — DECISION-017's own deferred Phase 2: this repository migrates its own Framework layer (`AI/agents/`, `AI/workflows/`, `AI/policies/`, `AI/commands/`, `AI/templates/`, `AI/SYSTEM.md`) into `.kenovis/AI/`, the same packaging every customer Installation already gets. → `PRODUCT/ROADMAP-ARCHIVE.md`

Phase 0 is closed. All eight "Immediate Priority" items are DONE, and Phase 0's own Success Criteria were satisfied by the Real External Validation recorded under Phase 1 below. Work continues in the Phase 1 priority block that follows.
---
Phase 1 — Immediate Priority (added 2026-08-07, from /next after Phase 0 closed)

Two items, in this order. Neither is blocked. Ordering matters: item 1 lands a real customer-facing bug fix, item 2 packages it — doing them in this order means one release carries both instead of cutting two.

Items 3-17 continued that alternation of work and release. Items 18-23 are a second priority block, added 2026-08-12 and founder-flagged maximum priority — see its own header below.

1. DONE (2026-08-07, via /next) — close Learning-010: `sync` disarms DECISION-018's first-session auto-trigger. → `PRODUCT/ROADMAP-ARCHIVE.md`
2. DONE (2026-08-07, via /next) — promote `development` → `preproduction` → `main` and cut the next release. → `PRODUCT/ROADMAP-ARCHIVE.md`
3. DONE (2026-08-07, via /next) — an Installation records which Framework Release it tracks. → `PRODUCT/ROADMAP-ARCHIVE.md`
4. DONE (2026-08-08, via /next) — an Installation receives its Product layer from Framework templates. → `PRODUCT/ROADMAP-ARCHIVE.md`
5. DONE (2026-08-08, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying items 3 and 4. → `PRODUCT/ROADMAP-ARCHIVE.md`
6. DONE (2026-08-09, via /next) — execute `/init-project` end to end against a real published Installation, and fix what breaks. → `PRODUCT/ROADMAP-ARCHIVE.md`
7. DONE (2026-08-09, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 6. → `PRODUCT/ROADMAP-ARCHIVE.md`
8. DONE (2026-08-09, via /next) — execute `/adopt-project` end to end against a real published Installation, and fix what breaks. → `PRODUCT/ROADMAP-ARCHIVE.md`
9. DONE (2026-08-09, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 8. → `PRODUCT/ROADMAP-ARCHIVE.md`
10. DONE (2026-08-09, via /next) — the `PROJECT-SPECIFIC` marker states layer, not state; and the `CLAUDE.md` stub enumerates the whole Product layer. → `PRODUCT/ROADMAP-ARCHIVE.md`
11. DONE (2026-08-09, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 10. → `PRODUCT/ROADMAP-ARCHIVE.md`
12. DONE (2026-08-09, via /next) — execute `/feature` end to end from a real published Installation, and fix what breaks. → `PRODUCT/ROADMAP-ARCHIVE.md`
13. DONE (2026-08-09, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 12. → `PRODUCT/ROADMAP-ARCHIVE.md`
14. DONE (2026-08-10, via /next) — execute `/bug` end to end against a real published Installation, and fix what breaks. → `PRODUCT/ROADMAP-ARCHIVE.md`
15. DONE (2026-08-10, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 14. → `PRODUCT/ROADMAP-ARCHIVE.md`
16. DONE (2026-08-10, via /next) — execute `/review` end to end against a real published Installation, and fix what breaks. → `PRODUCT/ROADMAP-ARCHIVE.md`
17. DONE (2026-08-10, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 16. → `PRODUCT/ROADMAP-ARCHIVE.md`

---
Phase 1 — Immediate Priority, second block (added 2026-08-12, from /analyze on framework context scalability, founder-flagged maximum priority — ahead of any other Phase 1/2/3 item)

Twenty-one items, items 18-38 — sixteen as originally written, plus items 34-38 added as the block ran. Item 25 is rejected, superseded by item 37. In the order written, except where an item states its own ordering constraint, and three do. **Item 26 comes first** (it unblocks 27), **item 27 next** (it is the root cause behind this block needing to be asked for at all), and **item 29 gates item 19**, because archiving before triage converts a visible backlog into an invisible one. Item 25 goes with or after item 21, never before. Founder flagged this block maximum priority on 2026-08-12: it is executed before `/architect` and `/release` end-to-end runs, before the `sync`-names-removed-paths backlog finding, before the npm-registry version check, and before the paid-tier ADR — all of which stay scheduled behind it.

The R-tags (R1-R6) are the recommendation labels from the 2026-08-12 `/analyze` run that produced this block, kept so the items stay traceable to their analysis. Execution order is not R-tag order: R5 is cheap and independent so it lands with the other Do-Now items, and R4 precedes R3 because a lifecycle rule that does not exist cannot govern the restructuring that R3 performs.

Items 24-25 and the two scope additions inside items 19 and 21 come from a second `/analyze` run the same day, on the three open risks item 18 closed with rather than closed. Their tags are RA1-RA4, kept distinct from R1-R6 so the two analyses stay separable.

Items 26-30 come from a third `/analyze` run the same day, tagged RB1-RB5, on the founder's observation that the AI-OS detects gaps and then leaves them to die in prose: *"lo que pasa ahora es simple, está detectándolo pero no está planificándolo, por lo que siempre se queda ahí, muerto, sin resolver."* The evidence held — 13 findings parked in this document's prose and 24 `Future action:` entries in `learnings.md`, of which 5 name the roadmap, with the oldest open one untouched since 2026-08-09. These five items are the highest-leverage in the block: they are why the other eight needed a human to notice them. The `Open Findings` queue that follows this block is their first output.

Measured finding this block exists to close, from git history over 2026-07-30 → 2026-08-10 (11 days, 58 commits): `DECISIONS.md` 9.8 KB → 119.9 KB, `PRODUCT/ROADMAP.md` 5.0 KB → 124.9 KB, `AI/memory/learnings.md` 4.1 KB → 65.2 KB, `CHANGELOG.md` 1.5 KB → 54.3 KB. Combined ~31 KB/day, append-only, with no archival, rotation or size rule anywhere in the framework. `CLAUDE.md` → "Session Initialization Protocol" mandates an unconditional full read of `COMPANY_OS.md` + `DECISIONS.md` + `AI/SYSTEM.md` — ~137 KB (~34k tokens) before any work starts — and a full `/bootstrap` reaches ~357 KB (~89k tokens). The growth rate is from an intense bootstrap phase and will slow; the direction does not depend on the rate. At one quarter of it, `DECISIONS.md` still passes 200 KB of mandatory per-session reading inside 90 days.

Why it outranks everything else on this document's own priority formula: it is not an infrastructure problem — Kenovis operates no backend to scale, per DECISION-013 — it is the documentation-as-memory discipline of DECISION-009 and DECISION-011 compounding with no retrieval or lifecycle counterpart. COMPANY_OS.md → Competitive Advantages names that same discipline as the customer's accumulated switching cost. The mechanism that creates the moat is the mechanism that hits the wall, so the failure lands inside the value proposition rather than beside it. And because Kenovis holds zero central infrastructure, it can neither observe nor remediate a customer's own accumulated Product layer — only ship framework changes through `sync`. Every Installation carries this unaided.

Dogfooding caveat, stated because it makes this repository unrepresentative: this repo has `graphify-out/` and a `CLAUDE.md` rule that routes questions through a knowledge graph before reading raw files. `bootstrap.md` Steps 2-4 honour that. No customer Installation receives any of it — graphify is third-party and not part of Kenovis — so a real Installation is measurably worse off than the evidence gathered here. Item 18's validation must be run against a real published Installation, not against this repository.

Deliberately short entries. Every item below states its problem, its target and its validation, and stops. This document is itself one of the four files the block exists to bound, and the 17 completed items above are the bulk of its 124.9 KB — writing this block in their style would enlarge the very measurement it is trying to reduce.

18. DONE (2026-08-12, via /next) (R1) — the session-initialization protocol reads the decision index, not every decision body. → `PRODUCT/ROADMAP-ARCHIVE.md`
19. DONE (2026-08-12, via /analyze) (R2) — completed roadmap items move to an archive; the active roadmap keeps one line each. → `PRODUCT/ROADMAP-ARCHIVE.md`
20. DONE (2026-08-12, via /next) (R5) — run `AI/memory/learnings.md`'s own Review Process for the first time. → `PRODUCT/ROADMAP-ARCHIVE.md`
21. DONE (2026-08-12, via /analyze) (R4) — `AI/policies/documentation.md` gains document lifecycle rules, enforced in CI. → `PRODUCT/ROADMAP-ARCHIVE.md`

22. REJECTED (2026-08-16, via `/architect`, DECISION-042) — `DECISIONS.md` becomes a directory, one file per decision, with the current file as its index. The retrieval property this targeted (load one decision without the rest) is already delivered by the Decision Index (item 18) plus a targeted read — practiced throughout this session, now stated as a mechanism in `SYSTEM.md` → "Context Loading Rules" — and the split's own named "Highest risk" (migrating every existing Installation's monolithic `DECISIONS.md`, `sync` never touching the Product layer) was real and left unsolved by the item as scoped. `check_document_size.py` gains a proper `"index"` category instead of `DECISIONS.md` citing this item as a pending exemption. Original text kept verbatim below and must not be executed. → `PRODUCT/ROADMAP-ARCHIVE.md`

Measured before rejecting, per Learning-023 — corrections to the item's own premises: `wc -c DECISIONS.md` → **246,047 bytes**, 34 decisions (was 119.9 KB / seventeen). `grep -l "DECISIONS.md" .github/scripts/*.py` → 7 files, not 4 — but only `check_decision_index.py` and `check_document_size.py` have a real structural dependency; the other five only mention the string in a docstring or a marker list. None of the 51 existing `DECISIONS.md DECISION-0NN` citations across the framework need to change under either option, since the id-based citation form stays valid regardless of physical file layout.

Item 23 inherits this as its own "reassess against measurements from items 18-22" input: filesystem-native retrieval already answers the question item 23 was scoped to answer for an Installation without graphify. Not decided here — item 23 stays its own scope.

**Operating model section served: §6** — *"the persistent product-layer must therefore reconstruct the relevant state of the product."* Distinct from item 24's evidence for the same row (that was behavioural — did a fresh session actually retrieve correctly; this is structural — the retrieval mechanism that behaviour depends on is now named explicitly in `SYSTEM.md` and given a real, checked category in the size guard, instead of resting on an exemption citing work that no longer exists). **The row does not move; it stays `Partial`.** What changed: `DECISIONS.md`'s threshold answer is now correctly classified rather than borrowing item 22's number as a placeholder. What did not change: whether a round actually practices the targeted-read discipline is still one behavioural instance (item 24's), not a closed class.

Also found and fixed while scoping this item, per Learning-023: `check_document_size.py`'s own docstring named three acceptable size-threshold answers and its code implemented two — a silent gap between a policy's stated shape and its guard's actual one, undetected since whichever round wrote the docstring ahead of the code. Fixed in the same edit that added the "index" category; not queued separately since the fix and the finding are the same change.

Findings this item did not fix: none.

23. DONE (2026-08-18, via /next) (R6) — a native retrieval command in the CLI.

Problem: the dogfooding caveat above. This repository routes context through graphify; no Installation has it. After items 18-22 the framework bounds what is read, but an Installation still has no way to ask its own accumulated context a question.

Target: a `kenovis context "<query>"` — filesystem-only, no network, no backend, inside ENGINEERING/ARCHITECTURE.md's Hard Rules and DECISION-013.

**Reassessed against items 18-22's actual current measurements, per this item's own instruction, before building anything:** items 18 and 22 together deliver "read one known citation cheaply" — the Decision Index (item 18) plus a targeted read, now stated as a mechanism in `.kenovis/AI/SYSTEM.md` → "Context Loading Rules" and extended to `ROADMAP-ARCHIVE.md`/`LEARNINGS-ARCHIVE.md` too. That is a *different* capability from "find which citation is relevant when you do not already know its id" — bounding tells a reader how to open door N cheaply once they know it is door N; it does not help them find the door. First-party evidence that the second capability is still missing, from this very round: this repository, despite items 18-22 all being `DONE`/`Rejected`, still required repeated `graphify query` calls (enforced by this session's own tool hooks) before almost every raw file read this session — the exact gap item 23's own Problem statement names ("this repository routes context through graphify; no Installation has it"). Conclusion: the premise still holds. Presented to the founder before writing code (architecture-level addition, per `commands/next.md`/`bootstrap.md`'s approval list) with three scope options; **v1 minimal chosen** — plain keyword/term-frequency scoring, zero new dependencies.

Built: `cli/src/application/commands/context.ts` — chunks a markdown file into paragraphs (a findings-table row is always its own chunk, so an unbroken table of `| OF-NN | ... |` rows does not collapse into one giant match), scores each chunk by query-token overlap, returns ranked `path:startLine-endLine` citations with a short excerpt. Reads only; never prints file contents itself, matching the targeted-read discipline items 18/22 already established. Wired into `cli/src/cli/bin.ts` as `kenovis context "<query>" [<targetDir>] [--limit N] [--include-framework]`, defaulting to `company-os/` only (`--include-framework` also covers the read-only `.kenovis/AI/` mirror).

Validated, run rather than reasoned about: 17 new unit tests against `InMemoryFileSystem` (paragraph chunking, table-row chunking, score ordering, `--limit`, `--include-framework` on/off, no-`company-os/` and no-match cases) — 129/129 CLI tests pass, `tsc --noEmit` clean, all 11 `.github/scripts/check_*.py` guards exit 0. Manually run against this repository's own real `company-os/` (`node cli/bin/kenovis.js context "branch protection" .`): top result was `PRODUCT/ROADMAP-ARCHIVE.md`'s OF-60 row on exactly that topic — a real, relevant hit, not a synthetic one. A query for a nonsense token correctly returned zero matches with `company-os/` still named as searched; a target directory with no `company-os/` correctly refused with a named error instead of a silent empty result.

Explicitly out of scope for this block, and recorded here so it is not proposed again: a hosted backend, a vector database, or any Kenovis-operated service to hold customer context. It contradicts COMPANY_OS.md → Competitive Advantages ("Zero central infrastructure to scale") and → What The Company Will NOT Become, and DECISION-013. This is a data-lifecycle problem, not an infrastructure problem. Also out of scope for v1, by the founder's own choice among the three presented: phrase-matching, configurable ranking, and framework-layer coverage by default — real candidates for a v2 if plain keyword scoring proves insufficient in practice, not assumed necessary now.

**Known limitation, stated rather than hidden:** v1 is lexical, not semantic — a query that shares no keyword with the relevant content will not surface it, even if a human would call the two obviously related. Acceptable for a first version whose job is pointing at the right citation more often than manual guessing, not replacing judgment.

**Operating model section served: §1** — *"[Kenovis] identifies problems, risks, inconsistencies, technical debt, missing work, and opportunities... the finding is then converted into planned work"* and, more directly, the retrieval half of "the persistent product-layer must therefore reconstruct the relevant state of the product" (§6). **The row does not move on this alone** — one Installation-side command shipping is not the same claim as an Installation actually using it to reconstruct context, and OF-30/Learning-031 hold that distinction here too; this is one data point, not closed-class evidence.

Findings this item did not fix: none.

24. DONE (2026-08-16, via /next) — validated that an agent opens a decision body instead of paraphrasing the index.

Fixture built at a scratchpad path (not committed to this repository — an ephemeral, throwaway Installation): "TaskFlow", a fictional B2B SaaS, scaffolded with `node cli/bin/kenovis.js init` against this repository's own bundled framework assets, then hand-authored (bypassing an interactive `/init-project` conversation, which this round had no second human to run) with a non-placeholder Product layer and a **12-decision `DECISIONS.md`**, each with a real ADR body. One decision (DECISION-007, API rate limiting) was written with a deliberate trap: its index line reads as a flat, universal rule ("token bucket per API key, 100 req/min, enforced at the middleware layer"), and only its body carries the load-bearing exception — outbound webhook delivery is exempt from that limiter entirely and routes through a separate queue-backed mechanism instead, with the body explicitly warning that a reader who stops at the index will assume the cap applies uniformly. `PRODUCT/ROADMAP.md`, `ENGINEERING/ARCHITECTURE.md` and `DOMAIN/BUSINESS_RULES.md` in the fixture each carry a light, realistic pointer toward the exception (matching how real docs cross-reference decisions) without stating the mechanism itself, so the test measures whether the body gets opened, not whether any signal exists at all.

**Run in a session that did not author the instruction, per the item's own requirement:** a fresh subagent (Claude Agent tool, `general-purpose`, zero conversational inheritance from this thread) was given the task exactly as a founder would state it — `/architect Design how the new bulk CSV task-import feature should handle its webhook traffic` — with no mention that citation behaviour was being observed, only a normal, unbiasing request to list the files it actually read as part of the ADR's own review trail.

**Result: positive.** The subagent's own report named the specific bodies it opened (DECISION-002, 004, 006, 007, 009) and was honest about over-reading the rest as a side effect of range-reads, rather than claiming minimal-necessary reads it didn't do. More importantly, the artifact it produced — a new DECISION-013, appended to the fixture's `DECISIONS.md` in the file's own house style — could not have been written from the index alone: it correctly identified DECISION-007's specific exemption mechanism (webhook delivery routes through the DECISION-004 queue, not the inbound limiter), explicitly declined to re-decide rate limiting because DECISION-007 already had, and instead solved the real open question (event-pacing between a trust-critical per-row event and an advisory progress event) using facts that exist only in decision bodies — DECISION-004's own documented ~50/sec revisit threshold, DECISION-006's hard-delete exception for `ImportJob`, DECISION-009's signing. An index-only read would most plausibly have produced a wrong or redundant answer: either re-applying the 100 req/min cap to webhook traffic (contradicting DECISION-007), or re-litigating rate limiting from scratch. Neither happened.

**Validated when, from the item's own text: "the run's citations are checked against the bodies it actually opened, and the result is written down either way."** Done — this is that record. A negative result would have been the more common one to plan for; this one was positive, checked against the produced artifact's content rather than trusted on the subagent's self-report alone.

Not fully clean, recorded rather than smoothed: the subagent also updated the fixture's `PRODUCT/ROADMAP.md`, `ENGINEERING/ARCHITECTURE.md` and `AI/memory/learnings.md` unprompted, applying the fixture's own "Nothing Stays In The Thread" rule — a reasonable, in-scope thing for it to do given its instructions, and a small secondary data point that the disposition discipline holds under a cold, unbiased run too, though that was never this item's own question and is not claimed as this item's evidence.

**Operating model section served: §6** — *"the persistent product-layer must therefore reconstruct the relevant state of the product"* (`.kenovis/AI/SYSTEM.md` → "Context Loading Rules" carries the same instruction for decision bodies that OF-37's round already validated for archives). **The row does not move; it stays `Partial`.** One instance is not a closed class (OF-30/Learning-031) — this is the mechanism's first real behavioural evidence for decision bodies specifically, distinct from OF-37's evidence for archives, and both are now one data point each rather than zero.

Findings this item did not fix: none.

25. REJECTED (2026-08-12, founder) — a customer Installation can run a check, not just read a rule. Superseded by DECISION-026 and item 37; the original text is kept verbatim and must not be executed. → `PRODUCT/ROADMAP-ARCHIVE.md`

26. DONE (2026-08-12, via /analyze) (RB2) — `/analyze` is forbidden from doing what its own Step 9 requires. → `PRODUCT/ROADMAP-ARCHIVE.md`
27. DONE (2026-08-12, via /analyze) (RB1) — a finding that is not fixed is scheduled or rejected. Never just mentioned. → `PRODUCT/ROADMAP-ARCHIVE.md`
28. DONE (2026-08-12, via /analyze) (RB3) — the roadmap carries a findings queue, not only phase narrative. → `PRODUCT/ROADMAP-ARCHIVE.md`
29. DONE (2026-08-12, via /analyze) (RB5) — triage the parked findings and the learnings' future actions, before item 19 archives them. → `PRODUCT/ROADMAP-ARCHIVE.md`
30. DONE (2026-08-12, via /analyze) (RB4) — the one part of this that a machine can check. → `PRODUCT/ROADMAP-ARCHIVE.md`

31. DONE (2026-08-12, via /analyze) — promote `development` → `preproduction` → `main` and publish the release carrying items 18, 19, 21 and 26-30. → `PRODUCT/ROADMAP-ARCHIVE.md`

32. DONE (2026-08-18, founder decisions in-session) — the three decisions only the founder can make, and the gap in item 27's own rule that let them sit.

Problem: OF-10 and OF-11 carry the disposition `Open — founder call`, which is honest and insufficient. A finding whose executor is not `/next` does not move because it scores well on a priority formula; it moves when someone names what has to be decided and what input the decision needs. OF-10 had been written in two places since this block was created and produced nothing — the pattern this block exists to end, surviving inside the rule that was supposed to end it.

Fixed already, in the round that scheduled this item: `.kenovis/AI/policies/documentation.md` (2.6 → 2.7) now requires an `Open` finding the AI cannot execute to name **who executes it and what input they need to decide**. `Open` was a valid disposition and the easiest place to hide. Found by the founder asking whether the three founder-call findings were actually planned — they were recorded, which is not the same thing. Item 27's own entry is archived and stays as written; this is where the correction lives.

Target: each decision reaches its own resolution, with the input stated so the founder is not asked to invent it.

- **OF-10 — the per-session context budget as a first-class constraint.** The cheapest of the three and the only one needing no external data: a `DECISIONS.md` entry recording that mandatory per-session reading is bounded by design, and an `AI/memory/learnings.md` entry that documentation-as-memory without a retrieval and lifecycle counterpart is accumulation rather than memory. Input available: everything items 18, 19 and 21 measured — 374.3 KB → 133.9 KB on the bootstrap path, the 60 KB threshold, the four governed documents. The founder decides the position; the entry is drafted against those measurements, not invented.
- **OF-11 — the MVP usage target.** Needs a number only the founder can set, and the input that shapes it is a constraint rather than data: DECISION-013 means no telemetry and no backend, so the target has to be something countable by hand (installations known to Kenovis, teams that report completing `/init-project`), not a metric a dashboard produces. Until it exists, `1.0.0` has no criterion and every release re-argues the same rejection — six so far.
- **OF-12 moves out of this item entirely.** It was mistagged as a founder call. It is work: see item 33.

Validated when: `DECISIONS.md` carries the context-budget decision with its index line, `learnings.md` carries the accumulation entry, `PRODUCT/ROADMAP.md` → MVP Success Metrics carries a real number, and OF-10 and OF-11 are struck from the queue rather than left `Open` beside their own resolution. **Corrected 2026-08-18, at closing, to match what the founder actually decided rather than what this line assumed going in:** the founder answered OF-11 with "no target, ever" rather than a number — DECISION-041 already established that `1.0.0` does not need this number to ship, and this closing round completes it by removing the number as a thing to set at all. The bar this item actually meets: `DECISIONS.md` carries both decisions with index lines (**DECISION-044**, **DECISION-045**), `learnings.md` carries the accumulation entry (**Learning-050**), `MVP Success Metrics` → Usage states the no-target decision directly, and OF-10/OF-11 are struck from the queue. OF-12 already moved to item 33, unchanged.

Findings this item did not fix: none.

33. SCHEDULED, ranked last — re-validate with a real external team, against the published package.

Problem, and the correction that produced this item: OF-12 was tagged `Open — founder call`. It is not a decision, it is work, and tagging work as a decision is a way of parking it. One external team has validated this product, on 2026-08-06, against `kenovis@0.3.0` — eight releases ago, before the `.kenovis/` packaging matured, before the Product-layer templates existed, and before the Decision Index, the findings queue and the lifecycle rules shipped today. `AUTOMATIONS/user-feedback.md` describes a feedback loop that has never run once.

Why it outranks most of what is left: five findings in the queue are `Deferred` on unvalidated Pain — OF-06, OF-07, OF-08 among them — and every one traces to this single gap. It is the cheapest thing that unblocks several at once. The North Star is defined in terms of Installations running a workflow, which is a number this product currently cannot report at all.

Target: at least one external team installs `kenovis@0.13.0` on a real repository, completes `/init-project` or `/adopt-project`, and runs one workflow end to end. Record what breaks in the queue, with dispositions. Run `AUTOMATIONS/user-feedback.md` as written for the first time — if it does not survive contact, that is this item's finding.

Not a smoke test. This repository has run those all day, and they only prove the artifact matches its own intent.

Validated when: a real external team's run is recorded with its findings dispositioned, and `AUTOMATIONS/user-feedback.md` either worked or was corrected.

Update — 2026-08-16, founder instruction (closes OF-89, DECISION-041): the "Why it outranks most of what is left" paragraph above is superseded for ranking purposes, not deleted — it argued correctly that this item is cheap and unblocks several findings, and the founder overrode that argument on an axis the priority formula has no term for: no external-customer testing runs until the rest of the roadmap is otherwise done. This item — any item requiring a real external customer or team — now ranks last, unconditionally, per DECISION-041. The findings it would validate (OF-06, OF-07, OF-08 among them) stay `Deferred` exactly as written; none of their own text assumed this item would run soon.

34. DONE (2026-08-12, via /next) — promote `development` → `preproduction` → `main` and publish the release carrying item 20 and the `Open`-finding-executor fix (`kenovis@0.14.0`). → `PRODUCT/ROADMAP-ARCHIVE.md`

35. DONE (2026-08-12, via /next) — close OF-13: `CHANGELOG.md` archives its own released sections. → `PRODUCT/ROADMAP-ARCHIVE.md`

36. DONE (2026-08-12, via /next) — close OF-17: an omission becomes a missing line, not a silence. → `PRODUCT/ROADMAP-ARCHIVE.md`

37. DONE (2026-08-12 → 2026-08-16, via /next across three rounds) — every rule this repository enforces mechanically now names its Framework-layer home, or records that it has none and why: eight guards cite a real policy/template section, two (`check_links`, `check_changelog`) record `none`, as item 37's own text predicted. → `PRODUCT/ROADMAP-ARCHIVE.md`

38. DONE (2026-08-18, via `/next`) — `sync` names what it removed from `.kenovis/`. Closes OF-01.

Problem: `sync` mirror-replaces the AI-OS layer and prints `already up to date` while files disappear. The oldest finding in the queue, unscheduled for four rounds because it read as needing a design decision.

It did not. RULE-INST-03 (2026-08-12) settled it: the AI-OS layer belongs to the AI-OS, so removal is correct and refusing would break the guarantee that every Installation runs the same team. What is left is one line of honesty — say what was removed.

**Why this round, not item 41's own confirmation:** the `Next` pointer's own top candidate required a session that did not author the item 41 §3/§12 fixes — this session did, in this same thread (item 44's own round, immediately prior). Forcing that confirmation anyway would be exactly the self-report bias `PRODUCT/OPERATING_MODEL.md` OF-30/Learning-031 hold every behavioral claim in this document to. The pointer itself named item 38 as the fallback for precisely this case; taking it is following the pointer, not descending past it (DECISION-030).

**Implementation.** Added `walkFiles(dirPath): Promise<string[]>` to `FileSystemPort` — every file's path relative to `dirPath`, recursive, posix-separated, directories excluded. `NodeFileSystem` implements it as manual recursion over the well-supported non-recursive `readdir` (not `readdir`'s own `recursive: true` option, whose `Dirent` parent-path shape changed across recent Node versions). `runSync` (`cli/src/application/commands/sync.ts`) captures `walkFiles(frameworkDir)` before `removeTree`, and again after every install-time-owned file (`.setup-pending`, the CLAUDE.md hash sidecar) is written back — so neither shows up as falsely "removed" on an ordinary sync — and returns the difference as `removedPaths`. `bin.ts` prints them under "Removed from `.kenovis/` (no longer part of this Framework Release):" and no longer prints `(already up to date)` when `removedPaths` is non-empty, even if the version stamp happens to match. No refusal, no `--force`, no prompt, exactly as scoped.

`InMemoryFileSystem`'s `copyTree` needed a real fix, not just a new method: it previously set one opaque marker key at the target path rather than copying individual seeded files, which would have made every `removedPaths` test see an empty "after" state regardless of what the incoming bundle actually carried. Fixed to copy real nested files by relative path while keeping the marker key for fixtures that seed only the directory path itself — the ~15 existing tests using that older seeding pattern needed no changes, since `walkFiles` excludes the directory-itself key from its results the same way `listDir` already did.

**Validated:** `cli`: `npm run typecheck` and `npm test` — 115/115 (112 pre-existing plus 3 new: a real removed file is reported, nothing is reported removed when nothing was, and the two install-time-owned files are correctly excluded). `NodeFileSystem.sync.integration.test.ts`'s existing real-filesystem test ("replaces stale files from an older release") extended with `assert.deepEqual(result.removedPaths, ["AI/old-agent.md"])` — the exact scenario item 38 targets, on a real filesystem, not only the in-memory double. Manual smoke test against a real `kenovis init`-created fixture: a stray file placed under `.kenovis/AI/` was correctly reported removed and `(already up to date)` was suppressed on that sync; a follow-up sync with nothing changed correctly printed `(already up to date)`. All 11 `.github/scripts/check_*.py` guards pass; `graphify update .` run.

Findings this item did not fix: none.

**Operating model section served: none.** CLI bug-fix work — real code, no numbered `PRODUCT/OPERATING_MODEL.md` behavior advanced (the release/dependency-bump/typo-fix class Step 13 already names as a legitimate "none").

39. DONE (2026-08-13, via /next) — the findings rule is carried by the layer every session loads, not by one command. Closes OF-26. → `PRODUCT/ROADMAP-ARCHIVE.md`

40. DONE (2026-08-13 → 2026-08-16, via /analyze and /next, founder-closed) — the purpose has no verifiable form, so every round optimises the instrumentation that does. Parts 2 and 3 closed by DECISION-040; part 1 was absorbed into item 41 (below).

Problem, and it is the mechanism behind the founder's question rather than the question itself. `policies/documentation.md` → "A Claim Is Read Back Off The Artifact" makes byte counts, grep counts and guard exit codes checkable, and rounds have honoured it well. Nothing makes the stated mission checkable. So a round closes what it can prove it closed, and what it can prove is always the machinery.

Measured, 2026-08-13, each off the tree:

- Of the 30 rows in the queue below, **18** are about this AI-OS's own bookkeeping (guards, document lifecycle, the findings machinery). Of the **17** carrying `Open`, **11** are.
- `wc -l .github/scripts/*.py` → **1198 lines** across ten guards, reaching **0** Installations (item 37 states this). `find cli/src -name '*.ts' | xargs wc -l` → **2604**. Repository-only infrastructure is 46% the size of the shipped product.
- Last 30 commits by area: `PRODUCT/` 29, `.github/scripts` 22, `.kenovis/AI/policies` 19, `cli` 5.
- `cli/package.json` → `0.14.0`. Last external validation: `0.3.0`, 2026-08-06 — **eleven releases**. Six deferrals in the queue rest on that one gap (OF-12, item 33).

Item 37 already named the same drift — "four consecutive rounds recorded that gap as a caveat while widening it" — and treated it as a guard-placement problem. It is not only that. A guard in the right layer is still a guard; the question this item asks is what a round is *for*.

Second half, and the reason the first half persists: `commands/next.md` Step 3 ranks queued findings on the priority formula `Pain × Frequency × Business Impact / Cost`. That formula is written for a *customer's* pain. Eighteen of these thirty rows have pain to Kenovis and none to any customer. Two incompatible populations, one ranking. Instrumentation findings win every time — their Cost is low and exactly knowable — and mission findings lose, because theirs is unknown or needs a party this repository does not have.

Third, and it is upstream of both: COMPANY_OS.md → Company Vision says Kenovis "gives any small, ambitious team the execution capacity of a full specialized organization". The founder's stated objective is "enable products to be developed, maintained, evolved and supported **without requiring a conventional human development team**". Those are different claims — augmentation and replacement — and only the weaker one is written in the Product layer. A round cannot be measured against a criterion that is not written down, so it is measured against the ones that are.

Target, in this order:

1. The objective is written where rounds read it, in a form a round can be checked against — COMPANY_OS.md and `PRODUCT/ROADMAP.md` → success metrics. Not a slogan; a question a closing round answers.
2. The queue separates the two populations, or the formula stops being applied to both. A finding about the recording mechanism and a finding about what the product does for a team do not compete on one axis.
3. A standing constraint on the ratio, decided by the founder rather than proposed here: what fraction of rounds may close on instrumentation before external validation runs again.

Founder call on 2 and 3 — the input they need is the four measurements above, which are gathered. 1 is drafting work once the objective text is settled.

Validated when: a closing round states which of the two the work served and against which written criterion, and the queue's two populations are no longer ranked by one formula.

Findings this item did not fix: **OF-31**, **OF-32**, **OF-33**, **OF-34**, **OF-35**.

Update — the objective text arrived, 2026-08-13, via a second `/analyze` the same day. Part 1 of this item was written as "drafting work once the objective text is settled", and treated the absence of a written objective as a gap to be filled later. It is not a gap: the founder holds an authored 17-section statement of Kenovis's operating model and pasted it into a prompt to ask whether the system had lost it. So part 1's input exists and part 1 is now placement, not drafting — **OF-36** carries it.

What that run added, and it did not restate this item: **OF-36** (the operating model is not on disk), **OF-37** (272 KB of archived memory that no command opens), **OF-38** (the disposition rule's trigger — "before the session ends" — is an event nothing defines or detects, which is why findings still die in threads after item 39), **OF-39** (two Source Of Truth Hierarchies, in `CLAUDE.md` and `SYSTEM.md`, disagreeing on whether a decision or a business rule wins).

The measurement half of this item held on re-check and one figure moved: `grep -c "founder-raised\|founder asking\|raised by a human\|founder-flagged" PRODUCT/ROADMAP.md` → **12** on 2026-08-13 after this item landed, and this run is the thirteenth instance of OF-35's class — the founder asking whether the system still does what it was built to do, and the answer being found by the asking.

Corrected in the same session, after the founder asked what had happened again: the round had the operating-model document in its own input and filed a request that someone write it down. `PRODUCT/OPERATING_MODEL.md` now holds it verbatim with its provenance, OF-36 is half Fixed, and the round's own failure is **OF-40**.

Findings this round did not fix: **OF-37**, **OF-38**, **OF-39**, **OF-40**; **OF-36** half fixed, its reconciliation half still Open. `wc -c PRODUCT/ROADMAP.md` → **104143 bytes** before this round's writes, which is OF-23 growing by another ~10 KB and still the cheapest work on the board.

Update — third `/analyze` the same day, 2026-08-13, on the same question with one new input: the founder's framing of the repository's own layers. Nothing in this item is restated; the operating model was already on disk from the previous round and was verified byte-identical to the founder's paste before anything else ran.

What it added, and part of it lands on this item rather than beside it: **OF-41** — this item's headline ratio measures `.github/scripts` against `cli/src` and calls `cli/src` the shipped product. It is not. The bundle is `.kenovis/AI/` — 64 tracked files, 419,098 bytes — and the CLI delivers it. The drift this item names survives the correction; its magnitude does not, and the corrected figures are the input to part 3, which is a founder decision on a standing constraint. Do not settle part 3 against the numbers as first written. Also **OF-42** (the founder's model of the layering differs from the tree, and no mechanism compares them) and **OF-43** (three full analyses in one day because nothing on disk holds the answer to this question).

Findings this round did not fix: **OF-41**, **OF-42**, **OF-43**.

Update — OF-41, 2026-08-15, via /next: the denominator corrected, re-measured off the tree rather than re-derived from the flagged error.

- `wc -l .github/scripts/*.py` → **1225 lines** (was 1198; item 37's own dispositioning work added lines to several docstrings since). `git ls-files .kenovis | wc -l` → **64** files, all markdown (`git ls-files .kenovis | grep -v '\.md$' | wc -l` → 0), **466,003 bytes** (was 419,098). Guards are **0.26×** the size of the bundle they sit beside — not 46% of a codebase that was never the right comparison.
- Last 30 commits by area, re-measured (`git log --oneline -30 --name-only`), and the number moves as this very round commits, so it is anchored at the point stated rather than treated as a total: `PRODUCT/` 51, `.kenovis/AI/commands` 16, `CHANGELOG.md` 15, `AI/memory/learnings.md` 13, `.kenovis/AI/policies` 11, `DECISIONS.md` 10, `.github/scripts` **1**. One of the last 30 touched `.github/scripts`, against 22 when this item was written — the "22 repository-only commits" this item opened with describes a window that has since closed; item 37's guard-dispositioning work is done, and every round since has been overwhelmingly Product-layer or `.kenovis/AI/` work, which is the shipped product by DECISION-026's own definition.
- The "18 of 30 queue rows are bookkeeping" classification is not re-derived here — it needs a fresh per-row read of a queue that has changed shape since (**88** rows now, **35** carrying `**Open`, against 30 and 17 when the figure was written), and that reclassification is target 2's own job, not OF-41's. What is confirmed without re-classifying: the same mis-denominator OF-41 named for lines of code applies to any bullet that counts `.kenovis/AI/policies` work as "the AI-OS's own bookkeeping" rather than product work, so target 2's eventual recount should exclude framework-layer *policy* rows from the bookkeeping side the way it already excludes them from nowhere today.

**The instrumentation-vs-product ratio this item opened with does not hold under either corrected measure.** Guards are a fraction of the bundle, not half a codebase; the last 30 commits show one repository-only commit, not 22. That does not mean the founder's underlying observation was wrong — it means the two numbers that were offered as its evidence were measuring the wrong thing, and target 3's standing constraint should be set against what the last several rounds actually did (five item-41 step 4 closures, all Product-layer or `.kenovis/AI/` work, no new guard added since item 37 started dispositioning the existing ten) rather than against the 2026-08-13 snapshot.

Findings this round did not fix: none.

Update — parts 2 and 3 closed, 2026-08-16, via /next: the founder answered directly rather than setting the standing constraint this item asked for. `PRODUCT/OPERATING_MODEL.md` Addendum B (verbatim, founder-supplied) and **DECISION-040**: no numeric ratio is set, because the founder does not monitor a ratio — Kenovis self-governs the instrumentation-versus-product balance inside its own bounded mechanisms (`Observe`, `Refine`, the findings queue's priority formula) and escalates only genuine decisions. Closed together with **OF-35**, which this item's own part 3 was always paired with — the same underlying question (how much of the noticing-burden sits with the founder) asked twice, on two axes, answered once.

**Item 40: DONE.** Part 1 (objective in checkable form) was absorbed into item 41 on 2026-08-13; parts 2 and 3 close here. Findings this closing did not fix: none.

41. DONE (2026-08-13 → 2026-08-18, via /next across five rounds, independently confirmed by a sixth) — the framework conforms 1:1 to `PRODUCT/OPERATING_MODEL.md`, section by section, or records where it deliberately does not.

Founder instruction, given in the session that produced items 40's update and OF-41/42/43: *"el propósito es que alinees 1:1 con lo que espero de kenovis"*. The specification is `PRODUCT/OPERATING_MODEL.md`, authored by the founder, on disk since 2026-08-13. This item is the alignment work; item 40 is the diagnosis of why rounds drift away from it. **Item 40 part 1 is absorbed here** (step 2 below is its checkable form); item 40 parts 2 and 3 stay where they are, as founder calls.

Why one item rather than thirteen: OF-28 through OF-43 are each correct and each scoped to a mechanism. None of them is scoped to the specification, so executing them in priority order optimises thirteen mechanisms and never answers the question the founder keeps asking. The unit of work here is a section of the operating model, and the finding ids are its parts.

**The conformance table has moved to `PRODUCT/OPERATING_MODEL.md` → "Conformance"** (2026-08-14, step 2 below, DECISION-032). It is not reproduced here: a second copy would be the failure DECISION-031 had just finished fixing one level up, and this item's own step 2 says its permanent home is not this document.

It was **moved, not copied, and re-verified rather than carried over** — every row re-read off the tree on 2026-08-14 before it was written into its new home. Four figures had moved in one day and are recorded in the new table: §3 14 → **22** founder-raised instances, §6 272,438 → **320,100** archived bytes, §13 17 → **54** `Open` queue rows, §14 ~120 KB → **164,762 bytes**. The shape held: five `Present`, nine `Partial`, three `Absent`, the three absent still §3, §12 and §15.

Two figures moved against the direction of the work, which is the table earning its keep on the first pass after it was given a home: rounds closed real gaps in that window and the two headline symptoms of §3 and §14 both got worse.

Target, in this order. Steps 1-3 are cheap and unblock the rest; do not start step 4 before them.

1. ~~**Rank the specification.**~~ — **DONE (2026-08-14, DECISION-031).** One hierarchy, in `.kenovis/AI/SYSTEM.md`, with `PRODUCT/OPERATING_MODEL.md` at rank 1 and on the session-initialization path; `CLAUDE.md` cites it and states no ordering; a business rule outranks a recorded decision. Closed OF-39 and the this-repository half of OF-50. The round reached it as a founder call, stopped, presented it and resumed on the answer — OF-47's branch running end to end for the first time. Residues were **OF-72** and **OF-73**, both closed in step 2.
2. ~~**The conformance table moves to `PRODUCT/OPERATING_MODEL.md`**~~ — **DONE (2026-08-14, DECISION-032).** It is a standing section there, one row per section, with the standing criterion carried by `commands/next.md` (2.6 → 2.7) Step 13: a closing round names the operating-model section its work served and updates that row, or states it served none, and Step 15's summary restates it. That is item 40 part 1's checkable form and OF-43's home in one artifact rather than two.

   **OF-72 and OF-73 closed with it**, as the pointer sequenced them. OF-72 was the founder's call and the founder answered in session: every Installation authors `PRODUCT/OPERATING_MODEL.md` at setup, before `COMPANY_OS.md`, from a new template the AI may never answer on the owner's behalf — `/init-project` 1.10 → 1.11 Steps 1-2, `/adopt-project` 1.10 → 1.11 Steps 2-3, `SYSTEM.md` 1.8 → 1.9 (rank 1 stops being conditional), `CLAUDE.md` 2.4 → 2.5, `bootstrap.md` 2.8 → 2.9. OF-73 was the third Source Of Truth ordering, in `COMPANY_OS.md` → "Knowledge Hierarchy": that section is now "What Each Document Defines", states no precedence, and cites `SYSTEM.md` — in this repository's copy **and** in the template, which OF-73's row did not know existed.
3. ~~**Reconcile `COMPANY_OS.md` with §1**~~ — **DONE (2026-08-13, DECISION-028).** Settled ahead of steps 1 and 2 because the founder answered it directly in the session that found it, and because the contradiction was sharper than this step assumed: `SYSTEM.md` did not merely omit §1's claim, its Final Principle asserted the negation (OF-57). §1 wins. `SYSTEM.md` 1.6 → 1.7, `COMPANY_OS.md` 2.0 → 2.1, and the operating model is ruled to outrank a framework document *for this conflict* — the general hierarchy is still OF-39 and still step 1. What remains of this step is **OF-58**: "the AI is not an autonomous decision maker" is unscoped in both `SYSTEM.md` and `CLAUDE.md`, and DECISION-028 deliberately did not settle it.
4. **Close the gaps in dependency order**, one round each, each round naming its section:
   - §2 — **OF-38 with OF-28 and OF-29, one change.** The rule's trigger becomes an event that occurs (write the finding when it is found, not before a terminus that never arrives), and the pre-setup window gets an instruction. Cheapest, and it closes ABSOLUTE PRIORITY #1's remaining edges.
   - §12 then §1 — **OF-31 then OF-32.** Routing by role, then refinement. In that order: nothing can refine a finding until a role owns it. This is the largest single piece of work in the item and the one that changes what Kenovis *is*.
   - §15 — **OF-44**, below. Hardest, and honest about it: the invariant may not be fully enforceable, in which case that is recorded rather than claimed.
   - §1/§16 — **OF-33.** Observation. Design question first: a cadence must not become an eleventh guard (DECISION-026) and must not be tool-specific (DECISION-010, the reason OF-20 was rejected).
   - §6/§9/§17 — **OF-37.** One instruction naming when an archive is opened; retrieval is item 23 and stays behind item 22.
   - §4 — **OF-41** before item 40 part 3, then **OF-42** with **OF-35**.
5. **Every closing round from step 2 onward states which operating-model section its work served**, against the table. A round that served none says so. That is the standing criterion item 40 says does not exist.

Explicitly out of scope, and stated so it is not folded in: no new CI guard (DECISION-026, OF-21, and item 37 is still mid-flight); no hosted service, backend or telemetry (DECISION-013, `COMPANY_OS.md` → What The Company Will NOT Become); no rewriting of `PRODUCT/OPERATING_MODEL.md`'s text, which is the founder's and is recorded verbatim by construction.

Precondition worth taking first, not scope creep: **OF-23's archive pass.** This document is at ~120 KB against a 60 KB threshold, it is the vehicle for every step above, and this item added ~9 KB to it. The pass has a written procedure and has been run twice.

Validated when: `PRODUCT/OPERATING_MODEL.md` carries the conformance table with no row reading `Absent` that is not accompanied by a finding carrying it or a recorded decision to leave it so — corrected 2026-08-18 to match the table's own "What A Row Means" rule (step 2 below), which this line originally understated by naming only a decision — and the founder can read that table instead of asking the question a fifteenth time.

Findings this item did not fix: **OF-90** (2026-08-18, found while re-verifying §12). Corrected 2026-08-18: this line previously also named **OF-44**, which closed 2026-08-14 (DECISION-037) — four days stale by the time this item itself closed.

Progress — step 2 (2026-08-14, via /next): the table's home, the criterion that keeps it alive, and OF-72 and OF-73 with it, as the pointer sequenced them.

Premises checked against the tree before scoping, per [[Learning-023]]. Three held and one was larger than its row said:

- `grep -c "conformance" PRODUCT/OPERATING_MODEL.md` → **0**: the table was not there.
- `grep -c "OPERATING_MODEL" .kenovis/AI/commands/init-project.md .kenovis/AI/commands/adopt-project.md` → **0, 0**, and `ls .kenovis/AI/templates/product-layer/PRODUCT/` returned four templates, none an operating model. OF-72 held exactly as written.
- **OF-73 was understated.** Its row calls the third ordering a Product-layer contradiction that `sync` will never reach, which is true and is not all of it: `grep -rln "Knowledge Hierarchy" --include="*.md" .` returns `COMPANY_OS.md` **and** `.kenovis/AI/templates/product-layer/COMPANY_OS.md`. The framework ships the third ordering to every Installation that has not yet been set up. Both are fixed; the row is corrected in place rather than re-queued, since it is the same finding measured properly.
- `wc -c PRODUCT/ROADMAP.md` → **164,762**, against the 129,094 the pointer anchors. Sixth consecutive round to find its own item's figure stale, which is OF-04.

The two decisions this round had to make rather than look up, both recorded in DECISION-032 with what they cost:

**Where the operating model is authored in setup, without renumbering.** `init-project.md` Steps 8, 11 and 12 and `adopt-project.md` Step 13 are cited by id from `AI/memory/learnings.md`, DECISION-018 and DECISION-022, so inserting a new Step 1 would break four citations silently and none of them would fail a check. The existing step is titled "the top of the hierarchy" and was writing rank 2 while rank 1 did not exist — extending it is what its own title already claimed.

**The table moved rather than being copied.** This item's inline table is replaced by a pointer and the four figures that changed. A conformance table in two places is DECISION-031's failure one level down, in the round that was completing DECISION-031.

Validation, run rather than reasoned about: the ten CI guards all pass, and `check_artifact_destinations.py` earned its keep by failing first — the new Confirm Intent questions produce answers and named no destination for them, which is the exact defect DECISION-024 exists to prevent, in a step this round wrote. Both steps now say the answers go into `PRODUCT/OPERATING_MODEL.md` and that nothing produced by that step is a file of its own. The three greps that anchored the findings all flipped, read back off the artifacts:

- `grep -ci "conformance" PRODUCT/OPERATING_MODEL.md` → 0 → **3**
- `grep -c "OPERATING_MODEL" .kenovis/AI/commands/init-project.md .kenovis/AI/commands/adopt-project.md` → 0, 0 → **4, 4**
- `grep -rn "^# Knowledge Hierarchy" --include="*.md" .` → 2 → **0**. Four files still contain the phrase and all four are references to this closed finding, in `COMPANY_OS.md`'s own historical note, `DECISIONS.md`, this document and `learnings.md` — which is why the check is on the heading rather than the string.

**The behavioural half is unvalidated and is stated as unrun**, per OF-30 and [[Learning-031]]: whether a closing round actually names its operating-model section is a claim about agent behaviour, and this round wrote the instruction. The first real evidence is the next `/next`, in a fresh thread.

**Section served by this round: §3** — "behaves like a real development team; the founder never has to ask". That is what the table is for, and it is item 41's own `Validated when`. **The row does not move.** Its measure is `grep -c "founder-raised\|founder asking\|raised by a human\|founder-flagged" PRODUCT/ROADMAP.md` → **22**, up from 14, and building the artifact a founder could read instead of asking is not the same as the founder no longer asking. §3 stays `Absent`. Four rows were re-measured (§3, §6, §13, §14) and no row changed state, which is the first application of this round's own rule that a row does not move because a rule was written.

Also closed here, because the round was editing the paragraph anyway and it was its fourth correction: **OF-68**. `DECISIONS.md` → "Document Layers" no longer states how many decisions carry `‡` and `★`; it names what the marks mean and gives the two commands that count them. The numbers had gone "Eight" → "Fifteen"/"ten" → "Nineteen"/"thirteen" → "Twenty-one"/"fourteen" in eleven decisions, each correction made by a round that had to touch the paragraph anyway, each stale before the next decision landed.

Archive pass, run as a step of this round per `commands/next.md` Step 13 rather than when someone noticed: three closed queue rows (OF-68, OF-72, OF-73) compacted, and the superseded `Next` ordering block moved — all four verbatim, each asserted as a byte-substring of `PRODUCT/ROADMAP-ARCHIVE.md` by the script that moved them, none of them re-typed. `PRODUCT/ROADMAP.md` **164,762 → 165,080** after the compaction alone, and **166,984** at the end of the round: the pass moved out roughly 9 KB and the round put more back than it removed. That is OF-62 holding exactly as written — the archive rule has no lever left, and the bound from here depends on findings being resolved rather than moved. Recorded, not re-queued.

Findings this step did not fix: **OF-76**, **OF-77**, **OF-78**.

Progress — step 2's residue (2026-08-14, via /next, fresh thread): **OF-76**, **OF-77** and **OF-70**, closed as the pointer ranked them — the criterion step 2 shipped is now populated in every Installation and declared in every round.

Premises checked against the tree before scoping, per [[Learning-023]]. All four held, and one of them is the first thing this round measured because it decides whether the item is real:

- `grep -c "^| [0-9]" .kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md` → **1**. One placeholder row. OF-76 exactly as written.
- `grep -cn "Operating model section served\|Section served" .kenovis/AI/commands/next.md` → **0**. Nothing required the declaration.
- `grep -cin "founder\|human decision\|cannot execute\|who executes" .kenovis/AI/commands/next.md` → **2**, unchanged from step 2 — the stop-branch is still there and this round did not need it.
- `wc -c PRODUCT/ROADMAP.md` → **166,646** at the start of this round, against the 164,762 the pointer anchors. Seventh consecutive round to find its own item's figure stale, which is OF-04, and the smallest drift yet (1,884 bytes against 33,417 four rounds ago) because step 2 ran the archive pass.

**Where the population instruction actually was, which is the part the row understated.** `init-project.md` line 190 already said the table is *"filled the first time a round reads the document end to end against the framework"*, and the template repeated it. Both are true and neither is executable: a setup command is read once, by the session that runs setup, and never again by the rounds it describes. So the instruction existed in two places and reached its executor in neither — [[Learning-036]]'s third failure mode (an instruction whose sink is never read) with the sink being a *command a round does not load*. The fix is not new text, it is the same requirement moved to the step that runs every round.

`commands/next.md` **2.7 → 2.8**, Step 13:

- **The closing round builds the table when it is still the form**, one row per numbered section, with `unmeasured` in the State column of every row it did not verify. `unmeasured` is defined in the template as the state a row is born in — deliberately not a fourth grade, because a row that stays `unmeasured` across rounds is itself the finding.
- **`Operating model section served:`** is a required line in this document, naming the section or the word `none`.
- **`Next:`** is a required line likewise, carrying the ranked objectives and the reasoning, or `none` with why. Step 3's fallback for "no pointer exists yet" is what made an omission read as intended behaviour; a round that means `none` now has to write it.

`DECISION-033` carries the reasoning and the three rejected alternatives, including the one that looked cheapest — having setup fill the rows.

`.kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md` **1.0 → 1.1**: names Step 13 as the owner of the population rather than repeating the instruction, and defines `unmeasured`. That second half was not in the plan — it was caught in this round's own review, which found the round shipping a state name the document it ships to does not define.

Validation, run rather than reasoned about. Ten CI guards pass. The three anchoring greps flipped, read back off the artifacts: table-population clause in `next.md` **0 → 1**, `Operating model section served` **0 → 1**, the `Next` pointer declaration heading **0 → 1**; the template names `next.md` Step 13 **2** times and `.github/` still appears **0** times anywhere under `.kenovis/AI/`.

**And this round is the independent evidence step 2 said it could not produce.** Step 2 wrote *"the first real evidence is the next `/next`, in a fresh thread"*, and item 42 parts 2-3 wrote the same condition for OF-46 and OF-47. This is that thread — no conversational inheritance, a different session from the one that wrote either instruction. What it did, stated as behaviour rather than as intent: Step 3 started from the `Next` pointer, took item 1 as ranked, did not re-derive the order over the document, and did not descend. **OF-46 is behaviourally validated.** OF-47's stop-branch did **not** fire and is still unrun — this round's objective was executable, so the branch was never reached, and that is a different claim from "it works". Item 41 step 2's own behavioural half — that a closing round names its operating-model section — is validated by this block existing, one instance, by a session that did not author the rule. That is what OF-30 asks for and it is one data point, not a closed class.

**Operating model section served: §15** — *"the system must be designed so that violating this invariant is difficult or impossible"*. **The row does not move; it stays `Absent`**, and the reason is this round's own rule: three required lines make three omissions visible, which is not the same as making a violation difficult. Measured for the row rather than recalled: `ls .github/scripts/*.py` → **10** guards, reaching **0** Installations, unchanged. What did change is that the framework's one working enforcement shape — a required line whose absence is exact — now covers three of its load-bearing rules instead of one. OF-44 still carries the row.

Archive pass, run as a step of this round per `commands/next.md` Step 13 rather than when someone noticed: three closed queue rows (OF-70, OF-76, OF-77) compacted and the superseded `Next` ordering block moved — **8,102 bytes**, all four asserted as byte-substrings of `PRODUCT/ROADMAP-ARCHIVE.md` by the script that moved them, none re-typed. `PRODUCT/ROADMAP.md` **166,646 → 158,544** after the pass, **172,347** at the end of the round: the round put back **13,803** against **8,102** removed, a net **+5,701**. Smaller than the previous round's overrun and the same direction, which is OF-62 unchanged — the archive rule has no lever left and the bound depends on findings being resolved.

Two sizes worth carrying forward rather than queuing, because both are the rules working: `AI/memory/learnings.md` is **60,604 bytes** against the 61,440-byte threshold — **836 bytes of headroom**, so the next round that appends a learning triggers its archive pass in Step 13. And `PRODUCT/ROADMAP-ARCHIVE.md` is **216,103 bytes**, over threshold by design and classified `archive_of`, which is OF-15 as written.

Findings this step did not fix: **OF-79**, **OF-80**.

42. DONE (2026-08-13 → 2026-08-14, via /next across four rounds) — the three commands the founder actually runs (`/next`, `/analyze`, `/explain`) work under the one-thread-per-`/next` cadence: the archive pass ships as a Step 13 trigger alongside the stale-branch guard (OF-51/OF-23, OF-60), `/next` Step 3 reads its own `Next` pointer and stops rather than descends on a human-only item (OF-46, OF-47, DECISION-030), and `/explain`, "Autonomous Mode" and Completion Criteria stop contradicting the findings rule and the founder's cadence (OF-48, OF-52, OF-54, OF-56, OF-80, OF-49, DECISION-034). → `PRODUCT/ROADMAP-ARCHIVE.md`

OF-53, 2026-08-14, via /next — `/next` Step 11 splits validation by the round's own artifact kind.

Premise checked before scoping, per Learning-023: `grep -cin "artifact is a document\|artifact is code\|read back off the artifact" .kenovis/AI/commands/next.md` → **0**, confirming OF-53's row as written — the majority of rounds change markdown under `.kenovis/AI/` and Step 11's four checks (tests, type checks, linting, build) touch none of it.

`commands/next.md` **2.9 → 2.10** Step 11: the code checks move under an explicit "when the round's artifact is code" heading; a new "when the round's artifact is a document" heading — the OF-53 row's own plausible shape — names `.kenovis/AI/policies/documentation.md` → "A Claim Is Read Back Off The Artifact" as the validation form and requires the round to state which one ran.

Validation, run rather than reasoned about — and this round's own artifact is a document, so this paragraph is Step 11 applied to itself. The anchoring grep above flipped: **0 → 2**. Read back rather than assumed: `grep -c "Type checks" .kenovis/AI/commands/next.md` → **1**, confirming the code-artifact branch is intact rather than replaced.

Found while running this round's own Step 13, dispositioned rather than left in prose: **OF-19** gains a second instance — this round could not branch from `development` per `policies/git.md`, because the previous round's PR (#114) sat open, unreviewed, one commit ahead of `development`. Resolved by merging it with `gh pr merge --rebase --admin --delete-branch`, the same command OF-19 already names as this repository's standard, with the founder's explicit confirmation in this session. **OF-85** is new: the previous round's own archive pass updated six queue rows' disposition to `Fixed` and copied them to the archive, but left their Finding and Source columns at full length instead of compacting them to a clause, so the active document kept the weight it had before archiving. OF-53 and OF-60 below are compacted properly in this round's own pass, for comparison.

**Operating model section served: §16** — *"Observe → Analyze → Detect → Refine → Plan → Implement → Test → Review → Learn → Persist → Update roadmap"*. **The row does not move; it stays `Partial`.** What changed: the "Test" step of that loop had no definition for a document-artifact round, which is most rounds on this board; it now does, and this round is the first application of it — a data point, not a closed class (Learning-031, OF-30). Observe and Refine are still absent, which is why the row does not move.

Findings this round did not fix: **OF-85**. OF-19 gained a second instance rather than a new id, per DECISION-029's "cite the id and add what this session learned to it."

Archive pass, run as a step of this round per `commands/next.md` Step 13 — triggered by `wc -c PRODUCT/ROADMAP.md` → **186,168 bytes** at the start of this round, against the 60 KB threshold. Item 42's own body, all five parts now complete, and the superseded `Next` ordering block it wrote move to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim, each asserted as a byte-substring of the archive by the script that moved it, none re-typed; the OF-53 and OF-60 queue rows compact to a clause each — the pattern OF-85 above says the previous pass did not follow. `PRODUCT/ROADMAP.md` **186,168 → 156,416 bytes** immediately after the pass, before OF-85, OF-19's addendum and this narrative were written; **164,537 bytes** at the end of the round.

OF-81 & OF-82, 2026-08-14, via /next — one workflow deleted, one measurement convention added.

Premises checked before scoping, per Learning-023: `grep -rn "workflows/roadmap" .kenovis/AI/ CLAUDE.md README.md` → **0** — nothing points at `workflows/roadmap.md`, confirming it as an orphan rather than a converging candidate. `grep -rln "templates/feature-plan.md\|templates/adr.md" .kenovis/AI/` → **7** hits outside the file itself, so its dependents are unaffected by removing it.

**OF-81:** `.kenovis/AI/workflows/roadmap.md` (2.3) claimed the same `/next` trigger as `.kenovis/AI/commands/next.md` (2.10) — a second, incompatible definition of the same command, with no findings queue, no `Next` pointer, no disposition rule, no operating-model declaration and no empty-roadmap handling. None of that is a choice this workflow made; it simply predates all four rounds that built them. Deleted rather than converged: bringing it to parity would mean re-deriving `commands/next.md` inside a second file, which `policies/documentation.md` → "Single Source of Truth" already forbids. Its templates stay, independently referenced elsewhere.

**OF-82:** `policies/documentation.md` **3.7 → 3.8** → "A Claim Is Read Back Off The Artifact" gains a bullet: a reach-measuring command cites the rule's exact section title, not an improvised keyword — the shape that produced OF-52's wrong count in both directions.

Validation, run rather than reasoned about. `grep -rn "workflows/roadmap" .kenovis/AI/` → **0** after deletion, same as before — nothing else referenced the file. `grep -rl "A Finding Is Fixed, Scheduled, Or Rejected" .kenovis/AI/commands/*.md .kenovis/AI/workflows/*.md | wc -l` → **12**, matching the corrected count item 42 part 4's own progress block already established; this round's new rule is what lets a future round reproduce that number by citation instead of re-deriving it from a keyword.

**Operating model section served: none.** Deleting dead code and correcting a measurement convention advance no single numbered behaviour in `PRODUCT/OPERATING_MODEL.md` — the release/dependency-bump/typo-fix class Step 13 names as a legitimate "none".

Findings this round did not fix: none.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — triggered by `wc -c PRODUCT/ROADMAP.md` → **166,625 bytes** at the start of this round, against the 60 KB threshold. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-81 and OF-82 compact to a clause each, in the same round they closed — the pattern OF-85 found missing, applied this time rather than deferred. `PRODUCT/ROADMAP.md` **166,625 → 160,826 bytes** immediately after the pass, before this narrative was written; **166,011 bytes** at the end of the round.

OF-58, 2026-08-14, via /next — the constitution stops denying the autonomy the operating model gives Kenovis.

Premise checked before scoping, per Learning-023: `grep -rn "not an autonomous decision maker" .kenovis/AI/SYSTEM.md CLAUDE.md` → both lines present, unscoped, confirming OF-58's row as written. OF-57, the sibling finding this row was originally paired with, is already Fixed (DECISION-028); nothing in this round reopens it.

`.kenovis/AI/SYSTEM.md` **1.9 → 1.10** and `CLAUDE.md` **2.5 → 2.6**: both scope the sentence to the product's own direction, strategy and business decisions, and name `PRODUCT/OPERATING_MODEL.md` as where that split actually lives — not this repository's own §4/§17, which is this repository's specific content and would be wrong the moment another Installation's operating model numbers its sections differently. The template's own generic shape (`templates/product-layer/PRODUCT/OPERATING_MODEL.md` → "2. What the AI-OS owns" / "3. What the owner owns") confirms every Installation has this split somewhere; only the number differs.

Validation, run rather than reasoned about. `grep -rn "not an autonomous decision maker" .kenovis/AI/SYSTEM.md CLAUDE.md DECISIONS.md` → the two live files carry the scoped form, `DECISIONS.md` still quotes the old unscoped sentence twice inside DECISION-028's own body — correctly: a decision log is a historical record of what was and was not settled at the time, and rewriting a past entry to match a later fix is exactly the compression `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" forbids. Ten CI guards pass.

**Operating model section served: §4** — *"The founder should primarily be responsible for... The founder should not be responsible for manually managing the engineering team's memory. Kenovis should own: Engineering awareness..."*. **The row does not move; it stays `Partial`.** What changed: the framework's own constitution no longer contradicts this section — it did, unconditionally, in the two files every session and every Installation loads first. What the row's gap still names (the founder's own model of this repository's layers differing from the tree, with nothing comparing them — OF-35, OF-42) is untouched by this fix and is not what OF-58 was ever scoped to close.

Findings this round did not fix: none.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **166,011 bytes** at the start of this round. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-58 compacts to a clause in the same round it closed. `PRODUCT/ROADMAP.md` **166,011 → 162,675 bytes** immediately after the pass, before this narrative was written; **168,073 bytes** at the end of the round.

OF-38, OF-28 & OF-29, 2026-08-14, via /next — item 41 step 4 begins: the disposition rule fires at an event that occurs, and reaches the one window it did not.

Premises checked before scoping, per Learning-023: `grep -n "before the session ends" CLAUDE.md .kenovis/AI/SYSTEM.md .kenovis/AI/policies/documentation.md` → three hits, confirming OF-38's row. `grep -n "PRODUCT/OPERATING_MODEL.md.*section\|What the AI-OS owns" .kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md` checked before citing a section number in the fix, after OF-58's own near-miss on the same mistake last round — this repository's founder-authored document numbers its sections differently from the generic template every other Installation gets, so the fix cites no section number at all, only the commands.

**OF-38:** `CLAUDE.md` **2.6 → 2.7**, `.kenovis/AI/SYSTEM.md` **1.10 → 1.11**, `policies/documentation.md` **3.8 → 3.9**. All three re-anchor the disposition rule's trigger from "before the session ends" — an event nothing detects, since a thread simply stops — to the moment of discovery, and name the one real exception: the window before `init-project.md`/`adopt-project.md` has authored the Product layer, where OF-28 and OF-29 apply.

**OF-28:** `cli/src/domain/installation.ts` → `claudeStubContent`. The pending stub (written between `kenovis init`/`kenovis add` and the end of setup) unconditionally listed `PRODUCT/ROADMAP.md`, `DECISIONS.md` and three more destinations that `runInit` never creates (DECISION-021) — so the one file Claude Code autoloads told a pre-setup session to route findings to paths that did not exist. It now says so, and names the setup command as where they go until it creates the real destinations. Two new unit tests in `installation.test.ts`; a pre-existing test in `init.test.ts` asserted the bug (`stub names PRODUCT/ROADMAP.md as a destination`, on a stub `runInit` always leaves pending) and is corrected to assert the fix instead.

**OF-29:** `.kenovis/AI/commands/init-project.md` **1.11 → 1.12** gains Step 13, `adopt-project.md` **1.11 → 1.12** gains Step 14 — both placed after the existing final step rather than renumbering it, since that step is cited by id from `learnings.md`, DECISION-018 and DECISION-022 (the same blast-radius reasoning item 41 step 2 already applied once). Each closes the run with a disposition pass now that every destination it authored actually exists. `adopt-project.md`'s version is the one this framework expects to matter — it is the session that reads an entire pre-existing codebase end to end, once, and item 41 step 4 already ranks its class of finding as the highest-yield unscheduled work on this board (OF-02/OF-03's precedent).

Validation, run rather than reasoned about. `grep -n "before the session ends"` → 0 across the three files. `cd cli && npm run typecheck && npm test` → typecheck clean, **112/112** tests pass (up from 111/112 before the `init.test.ts` correction). `grep -n "^# Step 12 - Record The Initialization" init-project.md` and `"^# Step 13 - Record The Adoption" adopt-project.md` → both present, unchanged — the four external citations to those steps still resolve.

**Operating model section served: §2** — *"ABSOLUTE PRIORITY #1: nothing discovered may be lost... Whenever Kenovis discovers something... it MUST be planned. There are NO exceptions."* **The row does not move; it stays `Partial`**, on the same discipline OF-30 and Learning-031 require: the design is now correct by construction (unconditional load, a trigger that fires, a named pre-setup path), but nothing has run `/init-project` or `/adopt-project` end to end since Step 13/14 landed, so the behavioural half — does a real setup session actually catch something and disposition it — is asserted by the round that authored it and is not evidence. That run is item 41 step 4's own next natural validation point, not a new finding.

Findings this round did not fix: none.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **168,073 bytes** at the start of this round. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-38, OF-28 and OF-29 compact to a clause each, in the same round they closed. `PRODUCT/ROADMAP.md` **168,073 → 162,556 bytes** immediately after the pass, before this narrative was written; **169,384 bytes** at the end of the round.

OF-31 & OF-55, 2026-08-14, via /next — item 41 §12: findings route by role, and this repository names its own.

Premise checked before scoping, per Learning-023: `grep -rn "Open Findings\|disposition" .kenovis/AI/agents/*.md` → **0** across twelve files, confirming OF-31's row. `grep -n "framework\|policy\|markdown" .kenovis/AI/agents/*.md` → **3** hits, confirming OF-55's row.

Caught mid-draft, before it shipped: the first version of this fix named `.kenovis/AI/agents/cto.md` as OF-55's destination and `.kenovis/AI/` as an example inside the framework-layer disposition rule. Both are wrong the moment this text reaches a second Installation — `cto.md` is never rewritten per product, and "you own `.kenovis/AI/`" is true of this repository specifically (DECISION-020's self-modification exception) and false of every customer Installation, where `.kenovis/AI/` is a read-only mirror no role ever touches. Corrected before writing, not after — the same class of near-miss OF-58's own round caught in itself the round before this one, on the same document.

**DECISION-035** carries the full reasoning and four rejected alternatives. Two changes, split exactly where the mistake above says they must be:

`.kenovis/AI/policies/documentation.md` **3.9 → 3.10** — an `Open` finding names the role that owns analysing it, from the existing Agent Roster, as a fourth dimension beside Pain/Frequency/Cost. Which role owns which *kind* of finding is stated nowhere in this framework-layer rule — that fact belongs to each product's own roster and its own maintained areas.

`ENGINEERING/ARCHITECTURE.md` **1.6 → 1.7** — this repository's own instance: the CTO role owns findings whose destination is `.kenovis/AI/`, recorded beside the CI-guards section that already states a this-repository-only fact in the same voice.

**The roster question OF-31 also asked — are `ceo`/`marketing`/`finance`/`legal` a gap or a deletion — is answered, not deferred:** a gap, not a deletion. `COMPANY_OS.md` already names Marketing, Finance and Legal analysis as areas AI agents support, and `PRODUCT/ROADMAP.md` → Product Strategy places that work in Phase 2 and beyond. Dormant by product stage, unlike `workflows/roadmap.md` (OF-81), which had zero conceptual justification and a superseding replacement already four rounds old.

Validation, run rather than reasoned about. `grep -n "cto.md" .kenovis/AI/policies/documentation.md` → **0** — the corrected draft cites no framework-layer agent file by name. `grep -c "^| 2 |\|ownership" ENGINEERING/ARCHITECTURE.md` confirms the new section landed once, not duplicated. `check_decision_index.py` → 28 decision bodies, 28 index lines. Ten CI guards pass.

**Operating model section served: §12** — *"The role that owns the responsibility must process the discovery."* **The row does not move; it stays `Absent`.** What changed: a finding can now be tagged with an owning role, and this repository's own framework-layer ownership is named — but nothing routes an active session to the tagged role, no agent file reads its own tag, and the twelve roster files still carry zero findings-awareness of their own. Naming an owner is not the same as the owner processing anything; OF-32 (refinement) is the very next step in this same sequence and is still fully open.

Findings this round did not fix: none.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **169,384 bytes** at the start of this round. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-31 and OF-55 compact to a clause each, in the same round they closed. `PRODUCT/ROADMAP.md` **169,384 → 165,186 bytes** immediately after the pass, before this narrative was written; **171,330 bytes** at the end of the round.

OF-32 & OF-66, 2026-08-14, via /next — item 41 step 4 continues: §1, the chain gains its REFINE step.

Premises checked before scoping, per Learning-023: OF-32's own citation (`grep -rin "refine" .kenovis/AI/{commands,workflows,policies,agents}` → 2, both `designer.md`, both visual-design) held unchanged going into this round. OF-66's six ids (OF-02, OF-03, OF-04, OF-10, OF-11, OF-14) were re-read in full before backfilling, not assumed from the row's own summary. OF-45's premise did **not** hold: `git status --short PRODUCT/OPERATING_MODEL.md` → empty, and `git log -- PRODUCT/OPERATING_MODEL.md` shows it committed in `72deb8b`, before this round started — found while refining OF-02 turned into a sweep of the queue's oldest rows rather than a read of OF-32 alone.

`policies/documentation.md` **3.10 → 3.11** and `commands/next.md` **2.10 → 2.11** (Step 3) ship the mechanism DECISION-036 records: every round refines the lowest-id `Open` row as a second action alongside its own objective. No new field, counter or CI guard — DECISION-026 and OF-21 forbid an eleventh guard while item 37 stays mid-flight, and row id order already carries the age signal for free.

Demonstrated live rather than left as a written rule with no instance (OF-30, Learning-031's own standard): the lowest-id `Open` row at the start of this round was **OF-02**. Refined in place — premise re-checked (`/architect` still never run end to end; no evidence found in `CHANGELOG.md` or `PRODUCT/ROADMAP.md` of a closed run since Learning-020/021/022), Pain/Frequency/Cost/Role added. Backfilling OF-02 is also OF-66's own opening item, so the same pass backfilled the other five rows OF-66 named rather than stopping at one — OF-66's own text invited exactly that ("take it with any round already editing the queue").

Validation, run rather than reasoned about. `grep -c -i "refine" .kenovis/AI/commands/next.md .kenovis/AI/policies/documentation.md .kenovis/AI/agents/designer.md` → **3, 1, 2** (was 0, 0, 2 before this round) — `designer.md`'s two hits are unchanged and still about visual design. `check_decision_index.py` → 29 decision bodies, 29 index lines (DECISION-036 added).

**Operating model section served: §1** — *"It observes the product continuously... identifies problems... raises those findings to the appropriate role. The appropriate role analyzes and refines them. The finding is then converted into planned work..."* **The row stays `Partial`.** Route (OF-31) and refine (OF-32) now both have a shipped mechanism; observe (OF-33) still has none, which is why the row cannot move to `Present`. Refine's own behavioural evidence is one instance, authored by the round that wrote the rule — the exact caveat OF-30 and Learning-031 require stated rather than smoothed over. Independent evidence is the next `/next`, in a fresh thread, refining a row unprompted.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **170,939 bytes** at the start of this round, against the 60 KB threshold. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-32, OF-66 and OF-45 compact to a clause each, in the same round they closed.

Findings this round did not fix: none.

OF-44, 2026-08-14, via /next — item 41 step 4's hardest gap: §15 gets a decided position instead of an open one.

Premises checked before scoping, per Learning-023: OF-44's own citations re-verified before writing anything — `ls .github/scripts/*.py` → still 10 guards, item 37 still mid-flight (three guards from done), OF-19 still `Open`. One premise did not survive contact: this round's own bootstrap needed a third `gh pr merge --rebase --admin --delete-branch` (PR #121, blocked on required review, `development` a commit behind it) — live evidence gathered before OF-44 was scoped, not after.

`DECISIONS.md` **DECISION-037**: "impossible" is ruled out by DECISION-010 (tool-agnostic markdown has no standing process to refuse an action) and DECISION-013 (no backend, no shipped runtime) — reversing either is out of this decision's scope. "Difficult" is partial: the required-line pattern (DECISION-033) is checkable in a way voluntary compliance is not, and its two gaps — CI reach (item 37) and admin-merge bypass (OF-19) — stay open on their own terms rather than being folded in or waited on. `PRODUCT/OPERATING_MODEL.md` conformance row 15 cites the decision; its grade stays `Absent`, now with a decision behind it rather than only findings.

Refined in the same pass (DECISION-036's own mechanism): the lowest-id `Open` row untouched by the previous round's refinement was **OF-19**. Its premise did not need re-verification — this round supplied a live, unplanned third instance itself, on entry, before OF-44 was even chosen. Added as evidence rather than a new row: three for three in one day is no longer worth a fourth per-instance paragraph.

Validation, run rather than reasoned about. `check_decision_index.py` → 30 decision bodies, 30 index lines. Ten CI guards pass. `grep -n "^| 15 " PRODUCT/OPERATING_MODEL.md` read back after the edit confirms the row cites DECISION-037 and both live gaps, not a restated measurement.

**Operating model section served: §15** — *"The system must be designed so that violating this invariant is difficult or impossible."* **The row does not move — `Absent` stays `Absent`,** per the same rule DECISION-035's round applied to itself: a decision does not promote a row, it only changes whether the row's state is explained or merely observed. What changed is that §15 now has both halves `Absent`'s own definition asks for — findings (OF-19, item 37) and a recorded decision (DECISION-037) — where before this round it had only the findings.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **173,479 bytes** at the start of this round, against the 60 KB threshold. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-44 compacts to a clause in the same round it closed.

Findings this round did not fix: none.

OF-33, 2026-08-15, via /next — item 41 step 4 continues: §1/§16 gets a bounded `Observe` step.

Premises checked before scoping, per Learning-023: `ls .kenovis/AI/commands/*.md | wc -l` → still **11**, every one keyed to a human intention; OF-06 still `Deferred`, `/framework-review` still unrun. `grep -l "Framework-layer home: not yet dispositioned" .github/scripts/*.py` → **5** of 10 (`check_decision_index`, `check_document_size`, `check_learning_promotions`, `check_links`, `check_changelog`) — item 37 still mid-flight, confirming both OF-33's own premise and the premise of the row refined below.

`DECISIONS.md` **DECISION-038**: "continuous" is ruled out by the same architectural reasoning DECISION-037 already applied to §15 — DECISION-010 (tool-agnostic, no scheduler) and DECISION-013 (no backend, no runtime) leave no place to stand a process that runs between sessions. The achievable form is a required, bounded `Observe` subsection inside `/next` Step 3 (`commands/next.md` 2.11 → 2.12), scoped on its first instance to document-weight drift — the same class OF-23, OF-51 and OF-62 already show this repository hitting unnoticed, three separate times, on one document alone. A new standalone command was considered and rejected on this repository's own evidence: OF-06 already shows an existing, correct, unrun audit command (`/framework-review`) producing zero audits across seven opportunities — a second command of the same shape would repeat it.

Refined in the same pass (DECISION-036's own mechanism): the lowest-id `Open` row untouched by any prior refinement pass is **OF-21** — OF-02, OF-03, OF-04, OF-10, OF-11, OF-14 were refined per OF-66's backfill and OF-19 was refined in the OF-44 round; OF-21 carried no `**Refined**` annotation and is next in line. Restated in the compact Pain/Frequency/Cost/Role form and re-verified against the tree rather than carried over from its own prose.

Validation, run rather than reasoned about. `grep -c "^## Observe" .kenovis/AI/commands/next.md` → 0 → **1**. `grep -c -i "observe" .kenovis/AI/commands/next.md` → 0 → **5**. `grep -n "^| 1 \|^| 16 " PRODUCT/OPERATING_MODEL.md` read back after the edit confirms both rows cite DECISION-038 and OF-87, not a restated measurement. Ten CI guards pass; `check_decision_index.py` → 31 decision bodies, 31 index lines.

**Operating model section served: §1** — *"It observes the product continuously... identifies problems, risks, inconsistencies, technical debt, missing work, and opportunities."* **The row stays `Partial`.** Route (OF-31), refine (OF-32) and observe (OF-33) now all have a shipped mechanism; none is complete, and observe's own first instance covers document-weight drift only. §16's row is updated with it, same reasoning, same caveat — the loop diagram names Observe as its own node and this is that node's first implementation.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **176,380 bytes** at the start of this round, against the 60 KB threshold. The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-33 compacts to a clause in the same round it closed.

Findings this round did not fix: **OF-87**.

OF-37, 2026-08-15, via /next — item 41 step 4 continues: §6/§9/§17 gets its "when to open" instruction.

Premises checked before scoping, per Learning-023: `grep -l "ARCHIVE" .kenovis/AI/commands/*.md .kenovis/AI/workflows/*.md` → still no match. `wc -c` the three archives → **450,149 bytes** against **285,380** active across the same three pairs — up from 272,438/155,242 when this row was written, confirming Pain "rising with every archive pass."

This framework already solved the identical shape once, for `DECISIONS.md`: `.kenovis/AI/SYSTEM.md` → "Context Loading Rules" already required a decision body to be "opened on demand... citing a decision requires opening it." **`SYSTEM.md` 1.11 → 1.12** extends that same sentence to the three archives — read in full every session, so the rule reaches every task with nothing to invoke (DECISION-026's test). `policies/documentation.md` 3.11 → 3.12 points its own "read on demand" clause at that rule instead of restating it, per "Single Source of Truth." No new command, no CI guard, no retrieval tooling — retrieval (item 23) stays exactly where it was, behind item 22.

Refined in the same pass (DECISION-036's own mechanism): the lowest-id `Open` row untouched by any prior refinement is **OF-22** — OF-21 was refined last round. Premise re-checked (`grep -rn "item 25" .github/scripts/*.py` → all four instances still correctly say "item 25 is rejected"); restated in the compact Pain/Frequency/Cost/Role form.

Validation, run rather than reasoned about. `grep -n "opened on demand" .kenovis/AI/SYSTEM.md` → **1 → 2** occurrences (the decision-body sentence, now paired with the archive sentence). Ten CI guards pass; `check_decision_index.py` → 31 decision bodies, 31 index lines, unchanged — this fix needed no new decision, only an extension of an existing rule.

**Operating model section served: §6** — *"the product-layer is the long-term memory of Kenovis... the persistent product-layer must therefore reconstruct the relevant state of the product."* **The row stays `Partial`.** What changed: the row's own citation — `grep -l "ARCHIVE" ... → 0`, "nothing opens an archive" — now has an instruction telling a round when to open one. What did not change: whether a round actually does it is unvalidated by construction (OF-30/Learning-031), same caveat every recent behavioural fix in this document carries. §9 and §17 cite the same underlying fact and are not moved separately — updating three rows for one fix would be the row-inflation OF-79 already warns against; the next full conformance pass re-verifies all three together.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **183,046 bytes** at the start of this round, against the 60 KB threshold (registered split, satisfied structurally per OF-62). OF-37 compacts to a clause in the same round it closed; its full row moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim.

Findings this round did not fix: none.

OF-41, 2026-08-15, via /next — item 41 step 4's last sub-gap, first half: the denominator that fed a founder decision gets corrected before that decision is made.

Refined in the same pass (DECISION-036's own mechanism): the lowest-id `Open` row untouched by any prior refinement was **OF-23** — OF-21 and OF-22 were refined in the two rounds before this one. Premise re-checked: the trigger OF-23 asked for is now codified (`policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline") and demonstrated repeatedly (item 42 part 1, and every one of this session's five item-41 closures ran its own archive pass). Re-dispositioned **Fixed**, with what it does not fix carried forward under OF-51 and OF-62's own ids rather than restated.

Validation, run rather than reasoned about. `wc -l .github/scripts/*.py` → **1225**. `git ls-files .kenovis | wc -l` → **64**, all markdown (`grep -v '\.md$' | wc -l` → 0), **466,003 bytes**. `git log --oneline -30 --name-only` re-tallied at commit time (the window moves as this round commits) → `.github/scripts` **1** commit, `PRODUCT/` 51, `.kenovis/AI/policies` 11. Ten CI guards pass.

**Operating model section served: none.** A measurement correction feeding a founder decision advances no single numbered behaviour in `PRODUCT/OPERATING_MODEL.md` on its own — the same "none" class item 42 part 4's OF-81/OF-82 round used for a comparable audit-and-correct round.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **185,026 bytes** at the start of this round, against the 60 KB threshold. OF-41 and OF-23 compact to a clause each, in the same round they closed.

Findings this round did not fix: none.

Item 40 part 3 / OF-42 / OF-35, 2026-08-15, via /next — the round the pointer ranked first is a founder decision; presented, not executed.

Premise checked before scoping, per Learning-023: branch currency re-verified (`git fetch` via `gh auth git-credential` over HTTPS, since `origin` is SSH with no key loaded in this session — OF-65/OF-86) — `development` level with `origin/development`, 0 commits behind. The pointer's own ranking was re-checked rather than re-derived: item 40 part 3 is still the top of Step 3's inputs, and DECISION-030's stop-branch — descending the priority order to find something executable is forbidden — applies exactly here.

**Observe (required, `commands/next.md` Step 3), run before ranking anything:** `python3 .github/scripts/check_document_size.py` → all 7 governed documents hold: 2 archives over threshold by design, `DECISIONS.md` over threshold on its registered exemption citing item 22, which still exists and is `SCHEDULED` — not `Fixed` or `Rejected`. No new finding from this pass.

**Refine (second action, DECISION-036):** the lowest-id `Open` row untouched by any prior refinement pass was **OF-25** — OF-19, OF-21, OF-22 and OF-23 were refined in the four rounds before this one, in that order. Refined in place in the queue above: the premise held and improved (`grep -rn "\.github/" .kenovis/AI/` → **0**, was 2), and refining it surfaced a second thing — this document's own previous `Next` pointer, written the same day, asserted "item 37 is closed" while OF-21's refinement in that same round already said "item 37 still mid-flight" (5 of 10 guards still `not yet dispositioned`). Corrected in OF-25's row and not repeated in the pointer below; another instance of OF-04's standing pattern, no new id.

**The decision itself, presented rather than assumed.** Item 40 part 3 asks the founder to set a standing constraint: what fraction of rounds may close on instrumentation (framework bookkeeping — guards, document lifecycle, the findings machinery itself) before external validation (item 33) runs again. The input, gathered by the OF-41 round immediately before this one, re-checked rather than carried over per Learning-023: guards are **1,225 lines** against a **466,003-byte, 64-file** bundle (**0.26×** its size, not the 46%-of-a-different-thing figure item 40 opened with). The commit figure moved with the rolling window, as it will every round: `git log --oneline -30 --name-only | grep -c "^\.github/scripts"` → **1** now, not the 0 the previous round read one commit ago — that one commit is `442f287`, which extended the archive rule and touched a guard's docstring, not new guard-authoring work. The instrumentation-heavy window this item was raised against stays closed on the figure that matters (one touch in thirty, all housekeeping, none of it a new guard). **OF-42** (the founder's own model of this repository's layering not matching the tree) and **OF-35** (every structural miss on record found by the founder asking, not by the system) are taken with it per the pointer's own sequencing — the same "is detection achievable at all" question, decided together if the founder is already in the loop.

Presented to the human in this session below, not decided here. **This round stops rather than descending to item 37 round 3, OF-25, item 24 or anything else ranked behind item 40 part 3** — DECISION-030's own reasoning: a board whose top item needs a human silently becomes a board of whatever the AI could do alone if a round skips past it, defensibly, every time.

**Operating model section served: §4** — *"The founder should primarily be responsible for... strategic decisions... Kenovis should own: Engineering awareness..."*, and its own interaction model: *"Kenovis discovers → analyzes → refines → plans. Founder decides → approves/rejects."* **The row does not move; it stays `Partial`** — the gap it names (OF-42, untouched by this round) is unchanged. What this round adds is the first real behavioural evidence for DECISION-030's stop-branch itself: item 41 step 2's residue round found it unfired — *"OF-47's stop-branch did **not** fire and is still unrun... this round's objective was executable, so the branch was never reached"* — and this round's objective was not executable, the branch fired, in a session that did not author the rule. Same independent-evidence bar OF-30 and Learning-031 hold every other behavioural claim to; one instance, not a closed class.

Findings this round did not fix: none. OF-25 refined; the stale "item 37 is closed" claim corrected in place as an OF-04 instance, no new id.

Update — the founder continued in the same session, 2026-08-15: OF-42 stops being a detection question and becomes scheduled work.

Presenting OF-42 (per this round's own narrative above) opened a live exchange rather than a wait. Two rounds of clarification, verbatim: the founder first restated `.kenovis/` as *"un ejemplo de COMO SE VE EL PRODUCTO"*, then — once this session's own restatement risked conflating it with the customer-Installation case — corrected sharply: *"no exactamente, .kenovis es la capa framework pero ES LA BUILD, es decir en este repo eso deberia estar hasta gitignoreado... me da mucho la sensacion de que hemos ido metiendo cosas ahi que no deberia porque es una puñetera build."* Audited live, in-session, before scoping anything: `find .kenovis -type f | wc -l` → 64, `git ls-files .kenovis | wc -l` → 64 (all tracked, none stray), `grep -n "kenovis" .gitignore` → one comment, no ignore rule. Content-wise nothing has leaked in; the founder's objection is structural, not about stray files — confirmed directly rather than assumed, in the same exchange that surfaced it. Asked to confirm scope before scheduling anything (a customer Installation's own `.kenovis/` stays committed, injected content — unaffected), the founder answered: *"Si claro; PLANIFICA TODO ESTO COMO PROXIMO Q HACER COMO MAXIMA PRIORIDAD."*

**Item 43 is scheduled below, ranked above item 40 part 3, on that instruction.** This is a live instance of `/next` Step 3's own branch — *"if the human answers within the same session, continue from Step 4 with their answer"* — except the answer given was not item 40 part 3's own question; it was a founder-flagged maximum-priority redirect discovered while presenting a different one. Not implemented from this thread: the item requires `/architect` and an ADR, the same bar item 22 already holds for a comparable structural change, and DECISION-020 is what it reopens.

**Operating model section served (restated for the continuation): §1 and §4 together.** §1 — *"it identifies problems... raises those findings to the appropriate role... the finding is then converted into planned work"* — this exchange is that chain end to end, in one session: discovered by the founder, clarified, converted into item 43 rather than left as OF-42's own standing question. §4 — *"Kenovis discovers → analyzes → refines → plans. Founder decides → approves/rejects"* — the founder decided and Kenovis is recording the plan, not deciding the architecture itself. Neither row moves: §1 stays `Partial` (this is the founder finding it, again, not the system), §4 stays `Partial` for the same reason stated above and unchanged by this addendum.

43. DONE (2026-08-15, via `/architect` + `/next`, founder-directed) — this repository's own `.kenovis/` becomes generated build output; Framework source moves to root `framework/`. DECISION-039. → `PRODUCT/ROADMAP-ARCHIVE.md`

44. DONE (2026-08-18, via `/next 44` — founder-instructed, departing the previous pointer's ranking; see below) — the Product layer namespaces under one visible root directory, `company-os/`, in every Installation and in this repository's own tree. Re-root only, no internal renames: the seven elements (`COMPANY_OS.md`, `DECISIONS.md`, `PRODUCT/`, `DOMAIN/`, `ENGINEERING/`, `AUTOMATIONS/`, `AI/memory/`) moved under `company-os/` (`git mv`, full history preserved) so a brownfield customer's own files are never overwrite candidates and the Collision Guard's surface drops from seven generic names to one.

Scope actually touched, measured against the tree rather than carried over from decision time: this repository's own root layout (7 elements, `git mv`); 45 `framework/` files (agents, commands, policies, workflows, templates, `SYSTEM.md`) plus the 16 real product-layer templates' own same-layer citations (the decision's own ~460-line count included both; only the first 45 are cross-layer citations proper, but the templates ship verbatim into a customer's `company-os/`, so their internal citations were brought into conformance with `policies/documentation.md` → "A Path In These Documents Is Written Repository-Relative, In Backticks" too — see Learning-045 for why that distinction mattered and how it was kept from over- or under-firing); root `README.md`/`CLAUDE.md`/`CONTRIBUTING.md`; `DOMAIN/BUSINESS_RULES.md` RULE-INST-01/RULE-INST-03's path lists (hand-edited, not scripted — same-layer self-citations elsewhere in that file were deliberately left alone); the 7 CI guards with functional path constants (`check_markers.py`, `check_document_size.py`, `check_item_findings.py`, `check_future_actions.py`, `check_learning_promotions.py`, `check_decision_index.py`, `check_artifact_destinations.py` — the last needed no edit, its regex matches by substring); `cli/src/domain/installation.ts` (`claudeStubContent()`, `NON_EVIDENCE_ENTRIES`) and comment-only citations across `cli/src/`; and — found only by running the actual CLI and two fresh behavioral validations, not by any static grep — `check_links.py`-caught broken markdown links in `CHANGELOG-ARCHIVE.md`/`cli/README.md`/`.github/PULL_REQUEST_TEMPLATE.md`/`.github/ISSUE_TEMPLATE/bug_report.md`, and `cli/assets/framework/README.md` (the hand-authored file bundled as every Installation's own `.kenovis/README.md`), which the original citation-rewrite scope missed entirely because it lives outside `framework/`.

**Deliberately not touched, and why:** same-layer citations already living inside the moved `company-os/` documents themselves (`DECISIONS.md` citing `PRODUCT/ROADMAP.md`, etc.) — DECISION-043 states re-root only, no internal renames, and rewriting a 257 KB decision log's and a 207 KB roadmap's own historical cross-references was never in the decision's measured scope (its own "~556 citation lines" figure would have been an order of magnitude larger had it included them). `.claude/settings.local.json`'s four stale Bash-permission-allowlist entries — cosmetic, not functional, named below rather than silently left.

Validated, each bullet from the original text, run rather than reasoned about:
- `grep -rE "(^|[^-a-zA-Z/])((PRODUCT|DOMAIN|ENGINEERING|AUTOMATIONS)/|COMPANY_OS\.md|DECISIONS\.md|AI/memory/)" framework/` → **2 hits, both the product-layer templates' own document-title lines** (`# COMPANY_OS.md`, `# DECISIONS.md` — a title names the document, it does not cite a path; deliberately left un-prefixed). No other hit anywhere in `framework/`.
- All 11 `.github/scripts/check_*.py` guards pass locally against the migrated tree.
- `cli`: `npm run typecheck` and `npm test` (112/112) pass — no test changes needed, since `installation.test.ts`'s pinned-path assertions use `content.includes()`, which still matches the prefixed paths as substrings.
- **Fresh `kenovis init` + `/init-project` run, in a scratch fixture, by a subagent with zero conversational inheritance from this thread:** every authored Product-layer file landed under `company-os/`; `find` after the run showed nothing at bare repo root but `CLAUDE.md`; zero leftover un-migrated instruction found in `init-project.md`; `.setup-pending` correctly deleted and the stub correctly reverted to steady state.
- **Fresh `kenovis add` + `/adopt-project` run, in a scratch brownfield fixture that owned its own pre-existing root `PRODUCT/ROADMAP.md`, by a second independent subagent:** the Collision Guard never fired on the customer's file — structurally unreachable, since every Step 3-8 destination is now `company-os/`-prefixed and the guard only checks destinations before writing them. `git diff --stat -- PRODUCT/ROADMAP.md` after the run: empty. Kenovis's own Product layer landed at `company-os/PRODUCT/ROADMAP.md`, alongside, not on top of, the customer's file.

Both validations found real defects unrelated to path citations, dispositioned rather than silently left: **OF-93** (new, `Open`, queued this round — `init-project.md`/`adopt-project.md` Step 11/12's verify-grep is unconditional against `[ANSWER:` while Step 2/3 explicitly tells the same run to leave the Conformance table's row example unfilled; both independent agents hit this and both deviated from Step 2/3 to route around it). The broken links and the missed `cli/assets/framework/README.md` (above) were fixed in this same round rather than queued, since they were squarely inside this item's own scope once found.

**Operating model section served: §10** — *"The AI-OS layer belongs to Kenovis itself... The product-layer belongs to the product being developed."* **The row does not move; it stays `Present`** (it already was — `sync` already never touched the Product layer). What this round adds is not a new guarantee but a sharper one: the two layers were already separated in behavior, and are now separated in a way `ls` answers directly — one directory name instead of seven generic ones a brownfield customer could plausibly already own. DECISION-043's own Consequences section names this: "one mount point makes 'what does Kenovis manage here' answerable by `ls`."

Findings this item did not fix: **OF-93** (new, `Open`, queued this round, dimensioned above).

Origin: OF-92. → DECISION-043.

OF-86 & OF-88, 2026-08-16, via /next — bootstrap found the repository desynced, fixed before anything else could safely proceed.

Bootstrapping a fresh `/next` round after item 43 closed: `git status` on `development` showed the checkout 4 commits behind `origin/development`, with `ENGINEERING/ARCHITECTURE.md` and three CI guards reverted to their pre-item-43 content on disk — **OF-86's third instance**, live, on entry to this round, merging item 43's own PR #127. Recovered with the row's own prescribed fix (`git -c credential.helper='!gh auth git-credential' fetch origin` then `git merge --ff-only origin/development`), then re-ran the full guard suite as a health check on the recovered state, which is how **OF-88** was found: `check_links.py` fails against a genuinely `.kenovis`-absent checkout — 17 broken links across README.md, CHANGELOG.md, CONTRIBUTING.md and cli/README.md, all correct customer-facing citations of `.kenovis/AI/...` that no longer resolve in this repository's own tree without a build, which the CI job running this guard never performs (Python-only, no Node). Neither finding was descended to from the ranked queue — both blocked the round's own ability to safely read and validate the tree, the same class of precondition `commands/bootstrap.md` Step 5 already treats as required rather than optional.

**OF-86 fixed**, three-for-three no longer left as an accumulating row: `policies/git.md` → "Rebasing" now names the HTTPS-credential-helper fetch-and-relevel sequence to run immediately after every `gh pr merge --delete-branch`, not only at next session's bootstrap.

**OF-88 fixed**: `check_links.py` retries a `.kenovis/AI/...` link against `framework/` when the direct path doesn't resolve — what it always mirrors 1:1 in this repository since DECISION-039. Validated against the real failure mode this time, not the always-just-built state: `.kenovis/` removed entirely, all 10 guards re-run, all pass.

**Operating model section served: none.** Bootstrap-blocking bug fixes, not a numbered behaviour in `PRODUCT/OPERATING_MODEL.md` — the same "none" class item 40's OF-41 round and item 42 part 4's OF-81/OF-82 round both used for comparable audit-and-correct work.

Findings this round did not fix: none.

OF-27, 2026-08-16, via /next — third consecutive round to reach item 40 part 3 unanswered; Observe and Refine run, nothing else executed.

Branch currency and Observe both held: `git rev-list --count HEAD..origin/development` → 0; `check_document_size.py` → all 7 governed documents hold, `DECISIONS.md`'s exemption still citing item 22, still `SCHEDULED`. Refined in the same pass (DECISION-036): the lowest-id `Open` row untouched by any prior refinement was **OF-27** — premise re-checked against `cli/src/domain/installation.ts`, holds exactly as written, unaffected by DECISION-039 (which relocated the `AI/` subdirectories, not the CLAUDE.md/README.md exemption OF-27 is about).

**This is the third consecutive `/next` invocation in this session to reach item 40 part 3 and stop, per DECISION-030.** Not treated as licence to descend: the pointer's own ranking is re-verified each time rather than assumed, and it holds each time. Recorded plainly because a pattern this repeated and still unaddressed is itself information for whoever next opens this document.

**Operating model section served: none.** Observe/Refine are routine per-round mechanics, not a numbered behaviour advanced.

Findings this round did not fix: none.

OF-25 & OF-40, 2026-08-16, via /next — item 37's own first candidate, closed; Learning-028's other half folded in from the same file.

Premise re-checked before scoping, per Learning-023, since OF-25's own row said not to carry it over: `grep -rn "\.github/" .kenovis/AI/ framework/` → **0** (was 2 on 2026-08-13, both fixed in item 37 round 2). Item 37 itself re-confirmed genuinely closed: all ten `.github/scripts/check_*.py` carry a real `Framework-layer home:` line, none reading "not yet dispositioned". Both premises held; OF-25 was unblocked exactly as its own row predicted.

**OF-25:** `.github/scripts/check_github_citations.py`, an eleventh check — a literal-substring grep over `framework/**/*.md` with no classifier, the exact form this row specified. Its framework-layer home is new: `framework/policies/documentation.md` → "The Framework Layer Never Names A Repository-Local Script As Where A Rule Holds", stating the citation from both directions — a repository-local check names its framework-layer home or records none (already this repository's own item-37 practice, now written down instead of only practiced), and the framework layer never names a repository-local script as a customer's enforcement mechanism. Phrased without the literal path string throughout, deliberately: the new guard would otherwise trip on its own rule, and hardcoding one CI provider's path into a section every Installation receives would itself have violated `SYSTEM.md` → "Project Context" ("must never hardcode a stack... or a repository topology").

**OF-40, folded in while already editing this file for OF-25** — the same precedent OF-68's round used for a fourth correction found mid-edit. Its proposed fix, a supplied-artifact clause in `policies/documentation.md` → "A Finding Is Fixed, Scheduled, Or Rejected", was named in [[Learning-028]] and deliberately held back there pending OF-36's reconciliation half. That half is now settled (OF-39/DECISION-031, item 41 step 3/DECISION-028) — checked, not assumed, before picking this up. Learning-028's *other* candidate destination, asking for the founder's statement of purpose at setup, turned out already fixed independently by DECISION-032/OF-72 on 2026-08-14, unnoticed until this round re-checked the learning's own premise (another OF-04 instance — stale premise carried forward — caught here rather than left for later). Added: the new supplied-versus-found paragraph, citing Learning-028 and OF-40. Learning-028 closes: **Fixed as a rule**, no unpromoted half.

Validation, run rather than reasoned about. `python3 .github/scripts/check_github_citations.py` → exits 0 on the current tree — the new rule's own prose was written to avoid tripping it, and this is that claim checked, not assumed. All eleven guards run directly, individually: zero failures. `grep -c "Learning-028" framework/policies/documentation.md` → 1.

**Operating model section served: §4** — *"Kenovis discovers → analyzes → refines → plans. Founder decides → approves/rejects."* OF-25 itself is none-class audit-and-correct work, the same shape item 40's OF-41 round and item 42 part 4's OF-81/OF-82 round both used "none" for. What moves the row is OF-40/Learning-028: the failure it names — a supplied artifact returned to its own author as a question, the exact "Not:" branch §4 states — now has a written rule forbidding it, where before this round it only had two prior instances and a queued row. **The row does not move; it stays `Partial`.** What changed is mechanism-level: nothing has tested the new clause against a live supplied artifact since it was written, the same caveat OF-30/Learning-031 require of every fresh instruction. The gap the row still names — OF-42, the founder's own model of this repository's layering not matching the tree — is untouched by this round.

Findings this round did not fix: none.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **191,877 bytes** at the start of this round, against the 60 KB threshold (registered split, satisfied structurally per OF-62). The superseded `Next` ordering block moves to `PRODUCT/ROADMAP-ARCHIVE.md` verbatim; OF-25 and OF-40 compact to a clause each, in the same round they closed.

Item 41 §3/§12 re-verification, 2026-08-18, via /next — the previous pointer's own flagged staleness resolved; the conformance table's three `Absent` rows each carry a live citation again.

Premise checked before scoping, per Learning-023: branch currency re-verified (`git fetch` via `gh auth git-credential` over HTTPS, since `origin` is still SSH with no key loaded this session — OF-65/OF-86) — `development` level with `origin/development`, 0 commits behind.

**Observe (required, `commands/next.md` Step 3), run before ranking anything:** `python3 .github/scripts/check_document_size.py` → all 7 governed documents hold: 2 archives over threshold by design, `DECISIONS.md` index-bounded on its own registered category (DECISION-042) — checked directly rather than assumed, since an earlier round's own Observe note still described the pre-DECISION-042 item-22 exemption this document no longer carries. No new finding from this pass.

**This round's objective, exactly as the previous pointer named it:** item 41's first move — re-verify §3 and §12 against the tree rather than re-read the item's already-settled target list. Both rows' cited findings had all closed: OF-35 for §3; OF-31, OF-55, OF-32 for §12. Re-running each row's own command showed the underlying measurement essentially unchanged — §3: `grep -c "founder-raised\|founder asking\|raised by a human\|founder-flagged" PRODUCT/ROADMAP.md` → **21**, was 22, a counting artifact from archive compaction, not a behavioural shift; §12: `grep -rln "Open Findings\|disposition" framework/agents/*.md` → **0** across 12 files, unchanged. Neither row's truth-value moved. What had gone stale was only the citation: a `Fixed` finding is not the same claim as "this row's own bar is still met by it." **§3** re-points to DECISION-040, itself the recorded decision that closed OF-35 as a governance answer (founder-as-detector rejected outright) rather than a shipped mechanism. **§12** had no citation left that actually targeted its own literal claim — OF-31/OF-55 fixed *naming* a role, OF-32/DECISION-036 fixed a *generic, round-driven* refinement, and none of the three ever addressed *role-driven* processing, which is what the row's text asserts. That residual is queued fresh as **OF-90**, dimensioned (Pain medium, Frequency high, Cost unknown, Role cto).

**Found while doing this, not assumed going in: item 41's own `Validated when` bar may now be met.** Its own line read "no row reading `Absent` that is not accompanied by a recorded decision to leave it so" — narrower than the table's own "What A Row Means," which the item's own step 2 built to allow *either* a finding *or* a decision. Read against the item's original wording the bar was unmeetable by design, since an `Open` finding was never going to satisfy a "decision" requirement; read against the rule the table actually operates on, all three `Absent` rows now carry a valid citation — §3 → DECISION-040, §12 → OF-90, §15 → DECISION-037 (re-checked here rather than assumed, unchanged). Item 41's own line is corrected in place to match the rule its own step 2 shipped, rather than left asserting a stricter bar nothing on this table was ever going to clear. **This round does not declare item 41 closed.** Doing so in the same round that made the fixes is the self-report shape OF-30 and [[Learning-031]] hold every other behavioural claim in this document to; the disciplined move is to flag it as ready and let a fresh round confirm independently, the same pattern this document has used every other time a round found its own work might close something (item 41 step 2, OF-46/OF-47).

**Refine (second action, DECISION-036):** the lowest-id `Open` row untouched by any prior refinement pass was **OF-61** — refined in place. `check_item_findings.py` re-run: population currently non-empty (2 narratives), which does not contradict the row's own steady-state claim (the guard returns to zero the next time those two items' own archive pass runs). Item 37, cited as a blocking precondition, confirmed `DONE`; OF-21 is the one dependency left standing.

**Operating model section served: §12** — *"The role that owns the responsibility must process the discovery."* **The row does not move; it stays `Absent`.** What changed is a citation repair, not new behaviour: the row now names the thing actually still open (OF-90) instead of three findings that closed on narrower claims than the row's own text. §3 served the same way, restated rather than repeated: **the row does not move; it stays `Absent`**, now grounded in DECISION-040 instead of a closed id.

Findings this round did not fix: **OF-90** (new, `Open`, queued this round).

Item 41 — independent confirmation and closure (2026-08-18, via /next, a fresh thread with no authorship stake in item 41, item 44, or the immediately preceding §3/§12 re-verification round): the previous pointer named this as the top candidate for exactly this reason — two consecutive rounds in the same thread had deferred it on self-report grounds (OF-30/Learning-031), and this thread is the "genuinely new thread" that pointer said the item needed.

Premise checked before scoping, per Learning-023: `development` level with `origin/development` at session start, 0 commits behind.

Re-ran each of the three `Absent` Conformance rows' own citations against the tree rather than trusting the previous round's write: **§3 → DECISION-040** — `grep -n "^# DECISION-040" company-os/DECISIONS.md` confirms it present, Status `Accepted`, closing OF-35 as the governance answer the row cites it for. **§12 → OF-90** — confirmed still `Open` in the queue below, dimensioned, not silently dropped since 2026-08-18. **§15 → OF-19, item 37** — OF-19 confirmed still `Open`; item 37 confirmed `DONE` (2026-08-16), checked that this does not orphan the citation: item 37's closed scope was naming a Framework-layer home for every CI guard, not eliminating the fact that a guard here reaches zero Installations, which DECISION-026 and DECISION-037 both already treat as a permanent architectural fact rather than a gap item 37 was ever meant to close. The citation still points at something real.

All three `Absent` rows carry a live citation — a finding still `Open`, or a recorded decision — meeting item 41's own `Validated when` bar as corrected 2026-08-18. **Item 41 is closed on this round's confirmation.** The founder can read `PRODUCT/OPERATING_MODEL.md` → Conformance directly, per the item's own stated success criterion, instead of the tree needing a further independent pass to state the same thing again.

**Operating model section served: none.** No Conformance row's State changes as a result of this round — all three `Absent` rows were re-verified, not altered. Closing item 41 confirms the mechanism the previous rounds built; it does not itself advance any of the seventeen numbered behaviours.

**Recovered this round, before any of the above:** `PRODUCT/ROADMAP.md` carried an uncommitted addition on entry to this round — **OF-94**, fully dimensioned — sitting in the working tree on `development` with no open PR, left by a round whose thread had ended before its own git steps ran (OF-80's rule, `Fixed`, still failing to fire in practice when a thread is externally interrupted before reaching it). Folded into this round's own branch and committed as its own distinct change rather than discarded or re-derived, per ABSOLUTE PRIORITY #1 (`PRODUCT/OPERATING_MODEL.md` §2). The gap this exposes — a discovering round has no *stated* recovery instruction, only the detection duty bootstrap Step 5 already carries — is queued fresh as **OF-95**.

Findings this round did not fix: **OF-95** (new, `Open`, found this round). OF-94 is not new to this round — it was already fully dimensioned by the round that wrote it; this round only recovered its durability.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — this round appended to `company-os/AI/memory/learnings.md` (Learning-045), which `wc -c` now puts at 62.0 KB against its 60 KB threshold, its `LEARNINGS-ARCHIVE.md` split sibling. Checked rather than assumed: no currently-inline learning is eligible — "a learning closes when its rule has been promoted," and every `Fixed` entry in the file either already names the policy section it landed in (already archived, per `check_learning_promotions.py`'s clean 30/30/23 run) or is `Fixed as an instance`, which is not the same as promoted and stays inline by design. Nothing archived this pass; the size is a fresh entry, not un-shed weight.

Archive pass, run as a step of this round per `commands/next.md` Step 13 — `wc -c PRODUCT/ROADMAP.md` → **223,905 bytes** at the start of this round, against the 60 KB threshold (registered split, satisfied structurally per OF-62). Checked, not re-derived: OF-62 was refined in the immediately preceding round (item 44, this same thread) and nothing in this round's own work changes its finding — no closed queue row was compactable beyond what that pass already did, and the DONE-item narrative this round added (item 38's own closure) is exactly the kind of weight OF-62 already names as the thing archiving cannot reach. Nothing archived this pass.

45. DONE (2026-08-18, via `/architect` + implementation, founder-directed throughout) — `kenovis init`/`add`/`sync` generate per-AI-tool scaffolding from a data-driven adapter registry, closing OF-96.

Problem: verified against the code, `runInit` wrote only `.kenovis/`, `.kenovis/.setup-pending`, root `CLAUDE.md` and its hash sidecar — no `.claude/` directory, despite `ENGINEERING/ARCHITECTURE.md` → Hard Rules already stating as settled fact that `.claude/` "stay[s] at repo root because Claude Code requires it." Claude Code, DECISION-010's own named primary tool, got zero native slash commands after install; every other AI tool got nothing at all beyond DECISION-010's undocumented-in-code "manually load `AI/SYSTEM.md`" fallback. Founder's binding constraint on the fix: no hardcoded, enumerable list of AI tools/models that a human must keep updating as new ones ship — the mechanism must be genuinely data-driven and agnostic, or it recreates the exact problem it closes.

Decision: **DECISION-046**. `framework/tool-adapters/<id>/` holds each supported tool as data (`adapter.json`'s `commandsDir`, `command-wrapper.md.tmpl`), never as CLI branching; `kenovis init`/`add` gain a non-interactive `--tools=<id>,...` flag defaulting to `claude`; the `claude` adapter's command wrappers are derived by listing `framework/commands/*.md` at bundle time, not by a maintained name list. The always-on baseline the decision named (a generic, tool-agnostic entrypoint pointing at `.kenovis/AI/SYSTEM.md`, present regardless of `--tools`) needed no new file — root `CLAUDE.md` already is that entrypoint, unconditionally written since DECISION-017/018; this item did not touch it.

Shipped, Phase 1 exactly as scoped — the registry mechanism plus exactly the `claude` adapter:
- `framework/tool-adapters/claude/adapter.json` (`{"id": "claude", "commandsDir": ".claude/commands"}`) and `command-wrapper.md.tmpl`, both plain data.
- `cli/scripts/bundle-framework-assets.mjs` renders one `.claude/commands/<name>.md` per `framework/commands/*.md` at build time, generic across adapters and commands — it names no tool id and no command name; `extractPurpose()` pulls each wrapper's `description:` from the source command's own `# Purpose` section.
- `cli/src/domain/installation.ts` gains `TOOL_ADAPTERS_DIRNAME`, `DEFAULT_TOOLS = ["claude"]`, `toolsMarkerContent`/`parseToolsMarker` (the `.kenovis/.tools` marker, same `INSTALL_TIME_OWNED_ENTRIES.preserved` shape as `.setup-pending`), and `isKenovisManagedCommandWrapper` — a per-file marker check, not the stronger hash-sidecar `CLAUDE.md` uses, since a customer editing a short generated wrapper is a much narrower risk than editing root instructions.
- `cli/src/application/toolAdapters.ts` (`installSelectedToolAdapters`) applies whichever tools are selected, per-file: an existing file that doesn't carry the marker is left untouched and reported in `skipped`, never overwritten — `.claude/commands/` is a shared namespace a customer may already use for their own commands, unlike `.kenovis/`, which is mirror-replaced wholesale because Kenovis owns all of it.
- `init.ts`/`sync.ts`/`bin.ts` wire `--tools` (`init`/`add` only; `sync` re-applies the persisted `.kenovis/.tools` selection automatically, no flag needed) and report `toolsInstalled`/`unknownTools`/`skippedToolFiles`.

Validated by simulation against the artifact, not reasoned about: a real `kenovis init` run in a scratch fixture produced `.claude/commands/` with all 11 `framework/commands/*.md` wrapped, each carrying the extracted `description:` and pointing at `.kenovis/AI/commands/<name>.md`; `.kenovis/.tools` contained `claude`. A second run seeded `.claude/commands/bug.md` with the customer's own unrelated content first — `kenovis sync` left it byte-for-byte untouched and reported it in "Left untouched (already existed, not Kenovis-managed)", while every other wrapper still refreshed. A third run seeded no `.claude/commands/` and passed `--tools claude,grok` — `claude` installed, `grok` reported via "Warning: this Framework Release ships no adapter for: grok." rather than silently ignored or failing the whole install. `cd cli && npm run typecheck && npm test` → clean, **146/146** (up from 129; 17 new/updated tests across `installation.test.ts`, `toolAdapters.test.ts` (new), `init.test.ts`, `sync.test.ts`, `bin.test.ts`).

Validated when (from the original scheduling text, re-checked rather than assumed): a fresh `kenovis init` (default, no `--tools` flag) produces a working `.claude/commands/<name>.md` for every file under `framework/commands/` — **confirmed**, above. `ENGINEERING/ARCHITECTURE.md` line 95 is no longer an unimplemented claim — **confirmed**, the code now does exactly what that Hard Rule already asserted. Adding a second adapter requires no edit to `cli/src/application/commands/init.ts`'s control flow, only a new `framework/tool-adapters/<id>/` directory — **confirmed by construction**: `init.ts`/`toolAdapters.ts` never reference `"claude"` or any other tool id by name; only `bundle-framework-assets.mjs`'s `renderToolAdapters()` and `installation.ts`'s `DEFAULT_TOOLS` constant would need touching to change the *default*, never to add support for a new id.

Not shipped this item, by design (Phase 2+, per the decision): adapters for Cursor, GitHub Copilot, Gemini CLI or any other tool — none exist in `framework/tool-adapters/` yet, so `--tools cursor` today reports `unknown`, correctly. Each is future content-only work under `framework/tool-adapters/`, reaching every existing Installation via `kenovis sync` without a CLI release.

Findings this item did not fix: none.

**Operating model section served: none** — a CLI/framework capability gap the founder raised and routed through `/architect`, not a numbered `PRODUCT/OPERATING_MODEL.md` behaviour.

Origin: OF-96. → DECISION-046.

Origin: OF-96. → DECISION-046.

Next (updated 2026-08-18, after item 32/OF-10/OF-11 closed, the full Open-findings audit, and item 45 scheduled via `/architect`, founder-directed throughout, no `/next` invoked):

**Departure from the previous pointer:** none on the ranking. This round was not `/next` work — the founder gave two direct decisions (OF-10, OF-11), a standing instruction to audit that every finding in the tree carries a real disposition, and separately raised and ran OF-96 through `/architect` to a decision (**DECISION-046**, item 45). The previous round's ranking (full table in `PRODUCT/ROADMAP-ARCHIVE.md`, "superseded `Next` ordering block, written after OF-93 fixed and OF-69 closed") is **unchanged and still the order to follow** — OF-83 is still top candidate; item 45 is scheduled, not ranked ahead of it.

**Closed this round, founder decisions:**
- **Item 32 — `DONE`.** OF-10 (`Fixed`, **DECISION-044**: no per-session context budget) and OF-11 (`Fixed`, **DECISION-045**: no installations target, ever, as a `1.0.0` gate — completes DECISION-041). `MVP Success Metrics` → Usage corrected to state the decision directly. `AI/memory/learnings.md` Learning-050 carries the accumulation-vs-memory reasoning DECISION-044 rests on.

**The audit, run per the founder's standing instruction (2026-08-13 block, below) rather than assumed satisfied:**
1. Mechanical: `check_future_actions.py` (48 future actions, all dispositioned) and `check_item_findings.py` (closed items' declared un-fixed findings all present in the queue) both re-run, both hold.
2. Manual: every genuinely-`Open` row (27, after this round's two closures) re-checked for Pain/Frequency/Cost — all carry them — and Role — four post-DECISION-035 rows were missing it (**OF-78, OF-79, OF-83, OF-84**) and are backfilled in place; older pre-DECISION-035 rows stay without it, per that decision's own explicit grandfather clause, not an oversight.
3. Prose sweep for findings never given an id: `company-os/ENGINEERING/`, `DOMAIN/`, `AUTOMATIONS/`, `COMPANY_OS.md`, `PRODUCT/OPERATING_MODEL.md` and `DECISIONS.md` grepped for gap-signalling language ("not yet implemented," "left for later," "should be revisited," etc.) uncited by an existing id. Two hits, both benign on inspection — `DOMAIN_MODEL.md`'s "Legal, Accounting — planned, not yet built" is COMPANY_OS.md's own stated Long-Term Market Vision, not a defect; `ENGINEERING/ARCHITECTURE.md`'s "if `.kenovis/` has not been built yet" describes the self-heal mechanism working as designed. No new finding surfaced.

This is the honest limit of the check, stated by `check_future_actions.py`'s own docstring: prose-only findings have no mechanical detector (item 6/8 rejected building one, correctly — see the queue's own closing paragraph), so step 3 is a targeted grep sweep, not an exhaustive read of every Product-layer byte. **"Planned" here means every finding carries a disposition — Fixed, Rejected, Scheduled, Deferred-with-reason, or `Open` fully dimensioned and queued — not that every finding is fixed.** 27 `Open` rows remain, all planned in that sense, ranked in the previous pointer's table.

1. **OF-83 remains the top candidate.** Unchanged reasoning: cheapest real fix in the cluster, no design decision blocking it.
2. Everything else from the previous pointer's ranking is unchanged — see the archived table for OF-95/84/94/85/78/90/79/87's positions and reasoning.
3. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged.

Not `/next` work: item 33 needs an external party and ranks last by founder instruction. Item 25 (the numbered roadmap item) is rejected as originally scoped. Item 32 is no longer on this list — closed this round.

Per Learning-023, checked before writing this pointer: this round closed one item (32) and fixed two findings via founder decision (OF-10, OF-11), so the figure below is read after this round's own edits.

**Operating model section served: §4** — *"The founder decides; Kenovis owns engineering awareness"* and, more specifically, `PRODUCT/OPERATING_MODEL.md` §2 (ABSOLUTE PRIORITY #1: nothing discovered may be lost) — this round exists because the founder invoked that exact standing instruction and asked for it to be re-verified, not assumed. **The row does not move; it stays `Partial`** (unvalidated behavioural claim per OF-30/Learning-031 — one audit instance, not a closed class) — but its own residual note ("the founder's own model of this repository's layers differs from the tree, with nothing comparing them") is exactly what this round's audit was: a comparison, requested and run.

Findings this round did not fix: none.
---
Twelve round narratives, 2026-08-18 to 2026-08-19 — between item 45's closure and the round that fixed OF-74/OF-75 — archived verbatim below, per `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" → "Superseded planning prose": each round's own `Next:` pointer was superseded the moment a later round wrote a new one, which made the whole block closed work, not only its ranking tail. This is the first pass to archive full round narratives rather than only the trailing `Next:` ordering block (compare the entries above, each scoped to the tail alone) — see OF-51/OF-62.

The disposition of record for every fix named below lives in its own Open Findings queue row (or its own `DECISIONS.md` body, for a design decision); these lines are a locator only, not a second copy of the disposition.

- OF-83 fixed, OF-02 refined (2026-08-18, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-84 fixed, OF-71 refined (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-97 fixed, OF-74 refined (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-94 fixed, OF-75 refined (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-78/OF-79/OF-90 fixed via `/architect` (2026-08-19, founder-directed). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-91 fixed, OF-85 refined, two stale dispositions corrected (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-71 fixed, OF-87 refined, two stale dispositions corrected (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-65 fixed, OF-02 refined, OF-99 opened (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-42/OF-96 disposition drift corrected; OF-100 queued (2026-08-19, founder question in-thread). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-21/OF-22/OF-27 fixed via `/architect` (2026-08-19, user-directed). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-67 fixed, OF-19 refined (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-99 fixed, OF-03 refined (2026-08-19, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
---
- OF-74/OF-75 fixed, OF-04 refined (2026-08-21, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`
- OF-51 fixed, OF-62/OF-30 refined — twelve superseded round narratives archived (2026-08-21, via `/next`). → `PRODUCT/ROADMAP-ARCHIVE.md`

OF-62/OF-69 fixed, OF-02 refined — queue rows compacted, via `/next` (2026-08-21):

**Premise re-checked before scoping, per Learning-023 and `kenovis_shared_working_directory_concurrency`:** the primary working directory (`/Users/barve/projects/kenovis`) was 4 commits behind `origin/development` at bootstrap (`ListAgents` showed 1 other concurrent `kenovis-*` session active, plus unrelated `zyveris-*`/`leyias-*` sessions). All reading and every edit for this round done in a fresh isolated clone (`kenovis-work`), fetched immediately before branching and re-checked before this closing block — `0` commits behind `origin/development`, level at `0e3ac9d` (the OF-51 fix) throughout. No race this round.

**Started from the previous pointer's own top candidate**, unchanged from Step 3's fallback: OF-62's own residual — compact every remaining Fixed/Rejected queue row that still carried its full disposition text with no archive pointer, the identical shape the same round's own OF-51 fix had just executed for round narratives. No design decision pending, per the pointer's own text.

**Scoped independently rather than trusting the cited count.** The previous round's own OF-62 text estimated "33 rows by direct count, 75,899 bytes" via a script over the queue table. Re-derived from scratch rather than carried over (Learning-023): a script matching every queue row whose disposition column's *final* bolded status word (after correctly walking each row's own refinement history, not its first mention) is `Fixed` or `Rejected` and whose disposition text carries no `→ PRODUCT/ROADMAP-ARCHIVE.md` pointer found **36** rows, 76,518 bytes — close to but not identical to the cited estimate. A 37th, **OF-69**, was found only while cross-checking the Refine action below: it is genuinely `Fixed` (confirmed by reading its row directly) but a naive "last bolded status word in the cell" pass misreads it as `Open`, because its own disposition text quotes its own stale-row history verbatim (`"it sat reading \`**Open.**\` through two further rounds"`) — OF-87's own row had already caught and named this exact trap in its text, one round before this one used it as a safety check rather than a citation.

**Executed, verified before writing:** for each of the 37 rows, the original four-column table row was preserved verbatim under its own `### queue row OF-NN` heading in `PRODUCT/ROADMAP-ARCHIVE.md`, and the active row's Id/Finding/Source columns were left untouched — only the Disposition column was rewritten to one clause plus the pointer, matching the shape existing compacted rows (OF-13 and others) already used. Byte-for-byte verification ran after the rewrite: each of the 37 archived entries compared equal to the exact pre-compaction line pulled from `git show HEAD:company-os/PRODUCT/ROADMAP.md`. `wc -c PRODUCT/ROADMAP.md`: **299,418 → 254,269 bytes**, a **45,149-byte (15.1%) reduction** — smaller than OF-51's own 68,393-byte cut and than OF-62's own ~76 KB estimate for this specific lever (the estimate counted disposition-column bytes across all 33-37 rows; the actual reduction nets that against what a compact one-clause replacement still costs per row, which the estimate did not subtract), but the same class of lever. Zero Fixed/Rejected rows now carry disposition text without a pointer, script-checked.

**OF-62 — `Fixed`.** The residual this row named is executed in full; see the row's own updated text for the exact count and figures. **OF-69 — `Fixed`, corrected in place** (it already was, since 2026-08-18 — this round only compacted and corrected the stale-quote trap, it did not change the underlying disposition). **OF-85 — corrected, not refined**, a direct consequence of OF-69's own compaction: its population of live uncompacted instances drops from two to one (item 41's own closing narrative, unchanged, for the non-contiguous-content reason already on that row).

**This round's own Refine action (DECISION-054): `OF-02`.** Least-recently-touched, checked directly against the remaining `Open` rows and their own most recent `Refined <date>` marker: OF-02/OF-03/OF-19 (2026-08-19), OF-04/OF-30/OF-62 (2026-08-21, all touched by the two preceding rounds), OF-64/OF-85/OF-87 (2026-08-19), OF-100 (2026-08-19, never refined, discovery date stands). Seven rows tie at 2026-08-19 — OF-02, OF-03, OF-19, OF-64, OF-85, OF-87, OF-100 — tiebreak to lowest id per DECISION-054's own text selects **OF-02**. Premise re-checked, not carried over: a second `/architect` run landed since OF-02's own previous refinement (closing OF-21/OF-22/OF-27, commit `074f079`, which the previous refinement's own text had not yet accounted for), also against this repository's own dogfooded instance — the identical caveat already on the row. Two `/architect` runs since item 45's own dogfooded run, zero against a real published Installation. Still `Open`, dimensions unchanged, item 33 still the actual close. See the row's own text.

**Validated per `policies/documentation.md` → "A Claim Is Read Back Off The Artifact":** all 12 CI guards re-run clean after every edit (`check_document_size.py`, `check_item_findings.py`, `check_links.py`, `check_decision_index.py`, `check_markers.py`, `check_rejection_citations.py`, `check_claude_stub_sync.py`, `check_future_actions.py`, `check_github_citations.py`, `check_learning_promotions.py`, `check_template_refs.py`, `check_artifact_destinations.py` — full output captured, all pass). The 37-row/45,149-byte figures are a direct script count and a direct `wc -c` diff, not estimates. This round touched only `company-os/PRODUCT/ROADMAP.md`, `company-os/PRODUCT/ROADMAP-ARCHIVE.md` and `company-os/PRODUCT/OPERATING_MODEL.md` — no `framework/` or `cli/` change, so no rebuild and no `cli` test run is needed, same scope class as OF-51's own round.

**Observe, run per `commands/next.md` Step 3:** `check_document_size.py` → 7 governed documents hold (2 archives over threshold by design — `ROADMAP-ARCHIVE.md` now 542.6 KB, up from 525.9 KB before this block's own edits, exactly the weight this round moved — 1 index-bounded, 0 exempted). No new finding from this pass; the OF-62 count discrepancy and the OF-69 misclassification are both corrected in place on their own existing rows, not queued as new ids, the same OF-04-class handling this document already uses for a stale premise found while executing other work.

**Operating model section served: §6** — *"the persistent product-layer must therefore reconstruct the relevant state of the product"* — and **§14** — *"the roadmap is the complete representation of known future work."* Same two rows OF-51's own round served, continuing the same lever: `PRODUCT/OPERATING_MODEL.md` Conformance rows 6 and 14 re-verified, not carried over, both stay **Partial** — a second consecutive real reduction landed (6.1× → 4.9× → 4.1× the 60 KB threshold across the two rounds), and the underlying document is still well over threshold with no further named lever currently queued, so neither row moves to `Present`.

Findings this round did not fix: none — the OF-62 count and OF-69 classification are corrections to existing rows, not new findings.

**Next:**

1. **No new top candidate asserted.** OF-51 and OF-62 are now both `Fixed`; the compaction lever that ranked the last two rounds is exhausted for now. The next `/next` round re-derives from Step 3 — OF-100 (design question, `/architect`-shaped, no founder input needed) is the most concrete remaining candidate; OF-19 and item 33 need a founder decision or an external party, present rather than pick.
2. **OF-100 needs a design decision on which item-side citation form is authoritative before a guard can regex it.** Unchanged — candidate for the same `/architect` treatment the OF-21/OF-22/OF-27 and OF-78/OF-79/OF-90 bundles already closed from. No founder input required to *decide*, only to *scope the option set*, the same shape those two bundles used.
3. **OF-19 is a repository-settings call, not code.** Present to the founder when a round reaches it; do not silently pick either side. Unchanged.
4. **OF-30's own vehicle question is open** (item 24 cannot retroactively serve as it) — worth folding into whichever round next validates a different agent-behaviour claim by self-report, not a dedicated round for this alone. Unchanged.
5. **OF-64 stays open but is not currently actionable.** Unchanged.
6. **OF-87 stays unranked, deliberately, per its own text — evidence window extended, not concluded.** Unchanged.
7. **Item 41's own closing narrative (OF-85's one remaining live instance) is low-priority, non-contiguous content** — take with a dedicated archive-pass round if one occurs, not itself worth scoping a round around. Unchanged.
8. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged.

Not `/next` work, unchanged: item 33 needs an external party and ranks last by founder instruction; OF-19 needs a founder decision on repository settings.

Per Learning-023, checked before writing this pointer: this round fixed two findings (OF-62, OF-69) and refined one row via its Refine action (OF-02), plus two in-place corrections (OF-62's own count, OF-85's own population), so the figures above are read after this round's own edits.
---
OF-100 fixed, OF-64 refined, via `/next` (2026-08-21):

**Premise re-checked before scoping, per Learning-023 and `kenovis_shared_working_directory_concurrency`:** the primary working directory (`/Users/barve/projects/kenovis`) was 4 commits behind `origin/development` at bootstrap (`ListAgents` showed 1 other concurrent `kenovis-*` session active). All reading and every edit for this round done in a fresh isolated clone, fetched immediately before branching — level with `origin/development` at `bc550ff` (the previous round's own OF-62/OF-69 fix). **A first attempt to scope OF-74/OF-75 as this round's own objective was abandoned mid-read, before any edit, when this same fresh-fetch discipline showed a concurrent session had already shipped OF-74, OF-75, OF-51 and OF-62 in the minutes since bootstrap** — re-derived the actual current top candidate from the leveled tree's own `Next:` pointer rather than proceed on stale state, the exact recovery `kenovis_shared_working_directory_concurrency` names for this shape.

**Followed the re-derived top candidate.** The previous round's own `Next:` pointer named OF-100 directly: "the most concrete remaining candidate... No founder input required to decide, only to scope the option set" — the same `/architect`-shaped-but-CTO-answerable class OF-21/OF-22/OF-27 and OF-78/OF-79/OF-90 already closed from.

**OF-100 — `Fixed`.** `.github/scripts/check_item_findings.py` gains a third check, the reverse direction: every Open Findings queue row whose own last bolded disposition word is `Scheduled` names an item, and that item cannot already be `DONE`. **DECISION-055** settles the design question the row's own text named as blocking (which citation form is authoritative): the queue row's own already-structured `**Scheduled — item N**` text, not a new required declaration on the item side — rejected because every one of the five confirmed drift instances (OF-01, OF-42, OF-69, OF-92, OF-96) was already-closed prose that a going-forward-only item-side line would not have caught retroactively. `framework/policies/documentation.md` (3.15 → 3.16) states the same rule in prose.

**Validated per `policies/documentation.md` → "A Claim Is Read Back Off The Artifact":** the new check verified against a synthetic instance before this row closed — replacing the one currently-live `**Scheduled — item 33.**` citation (OF-12) with a reference to an already-closed item (`item 1`) made the guard fail with exactly the named drift (`OF-12: disposition still reads Scheduled — item 1, but item 1 is closed (DONE)...`, exit 1); reverted (byte-identical to the pre-edit file, confirmed via `diff`), guard passes clean (exit 0, `1 queue row(s) currently Scheduled, cross-checked against 42 closed item(s)`) — the one genuinely live Scheduled row (OF-12, item 33) correctly does not fire, since item 33 has not closed. All CI guards re-run clean after every edit (`check_item_findings.py`, `check_document_size.py`, `check_decision_index.py`). This round touched no `cli/` code, so no rebuild or `cli` test run is needed.

**Refine (second action, DECISION-054):** **OF-64** — least-recently-touched, checked directly against every genuinely-`Open` row's own most recent `Refined <date>` marker: OF-64's own last `Refined 2026-08-18` predates every other `Open` row (OF-02/OF-04/OF-30, all 2026-08-21 from the two immediately preceding rounds; OF-03/OF-19/OF-85/OF-87, all 2026-08-19; OF-100, discovery date 2026-08-19, now `Fixed` and out of the population) by at least a day — no tie to break. Premise re-checked, not carried over: `sed -n '/^## \[Unreleased\]/,/^## \[0\./p' CHANGELOG.md | grep -c "^### "` → **1**, unchanged since the row's own 2026-08-19 correction; this round's own CHANGELOG entry (below) is itself a live instance of the choice the row worries about, and it merged into the existing header rather than opening a new one. Truth-value and dimensions unchanged, still `Open`.

**Observe, run per `commands/next.md` Step 3:** `check_document_size.py` → 7 governed documents hold (2 archives over threshold by design, 1 index-bounded, 0 exempted). No new finding from this pass.

**Operating model section served: §2** — *"ABSOLUTE PRIORITY #1 — nothing discovered may be lost."* OF-100/DECISION-055 close a third, distinct blind spot in the one mechanical check on this section: the first two (DECISION-051) guarded a finding from never being recorded or from being invisible to the check; this one guards an already-recorded finding from silently going stale after the fact — the exact five-instance drift this section exists to prevent. `PRODUCT/OPERATING_MODEL.md` Conformance row 2 re-verified, not carried over — stays `Partial`, same reasoning as before (mechanism, not yet an independently-exercised behavioural instance, per OF-30/Learning-031).

Findings this round did not fix: none.

**Next:**

1. **No new top candidate asserted.** OF-100 is now `Fixed`; the design-decision-shaped items this pointer's own predecessor named are exhausted. The next `/next` round re-derives from Step 3 — OF-19 and item 33 are the only two remaining named candidates, and both need input this round cannot supply: OF-19 a founder decision on repository settings, item 33 an external party.
2. **OF-19 is a repository-settings call, not code.** Present to the founder when a round reaches it; do not silently pick either side. Unchanged.
3. **OF-30's own vehicle question is open** (item 24 cannot retroactively serve as it) — worth folding into whichever round next validates a different agent-behaviour claim by self-report, not a dedicated round for this alone. Unchanged.
4. **OF-64 stays open but is not currently actionable** — re-confirmed this round. Unchanged.
5. **OF-87 stays unranked, deliberately, per its own text — evidence window extended, not concluded.** Unchanged.
6. **Item 41's own closing narrative (OF-85's one remaining live instance) is low-priority, non-contiguous content** — take with a dedicated archive-pass round if one occurs, not itself worth scoping a round around. Unchanged.
7. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged.

Not `/next` work, unchanged: item 33 needs an external party and ranks last by founder instruction; OF-19 needs a founder decision on repository settings.

Per Learning-023, checked before writing this pointer: this round fixed one finding (OF-100) and refined one row via its Refine action (OF-64), so the figures above are read after this round's own edits.
---
OF-19 fixed (founder decision), OF-03 refined, via `/next` (2026-08-21):

**Premise re-checked before scoping, per Learning-023 and `kenovis_shared_working_directory_concurrency`:** the primary working directory was 6 commits behind `origin/development` at bootstrap (`ListAgents`: 0 other `kenovis-*` sessions active). All reading and every edit for this round done in a fresh isolated clone, fetched immediately before branching — level with `origin/development` at `ddb851b` (the previous round's own OF-100 fix), no race.

**Followed the previous pointer's own top candidate.** OF-100 was `Fixed` (previous round); the only two remaining named candidates both needed input this AI cannot supply — OF-19 (a founder decision on repository settings) and item 33 (an external party, ranks last per DECISION-041). Per `commands/next.md` Step 3 → "When The Next Objective Is Not Yours To Execute": presented OF-19 to the founder with the input the row already named (GitHub's own branch-protection API for `development`, re-verified live: `required_pull_request_reviews.required_approving_review_count: 1`, `enforce_admins.enabled: false`, no `required_status_checks` key at all), three options laid out (leave as-is; wire CI only, keep the unsatisfiable review requirement; drop review to 0 and wire CI). Founder chose the third, in-session.

**OF-19 — `Fixed`.** **DECISION-056.** `development`'s branch protection reconfigured live via the GitHub API: `required_pull_request_reviews` removed entirely; `required_status_checks` now names both CI jobs (`Markdown links, markers, template references and artifact destinations`; `cli/ build, typecheck and tests`) with `strict: false`. `enforce_admins` unchanged (`false`). No `framework/` or `cli/` file touched — this is a repository settings change, not code, so `check_changelog.py`'s watched-path rule does not apply and no `CHANGELOG.md` entry is owed.

**Validated per `policies/documentation.md` → "A Claim Is Read Back Off The Artifact":** re-fetched the live protection state after applying it — `gh api repos/rafabarranco/kenovis/branches/development/protection --jq '{required_status_checks, enforce_admins, required_pull_request_reviews}'` → `required_pull_request_reviews: null`, `required_status_checks.checks` lists both CI job names, `enforce_admins.enabled: false` — matches the row's own claim exactly. `check_decision_index.py` re-run clean (49 bodies, 49 index lines) after DECISION-056's addition.

**Refine (second action, DECISION-054):** **OF-03** — least-recently-touched, tied at 2026-08-19 with OF-85/OF-87, tiebreak to lowest id (OF-02/OF-04/OF-30/OF-64 all touched 2026-08-21 by the two preceding rounds and this round's own edits, out of contention). Premise re-checked: no `/release` invocation since the row's own last refinement. Still `Open`, unchanged.

**Observe, run per `commands/next.md` Step 3:** `check_document_size.py` → 7 governed documents hold (2 archives over threshold by design, 1 index-bounded, 0 exempted). No new finding from this pass.

**Operating model section served: §15** — *"Core invariant: violating it must be difficult or impossible."* DECISION-037 (2026-08-14) named OF-19 as one of §15's two separately-tracked "difficult" gaps (reach, item 37; bypass, OF-19). OF-19's own bypass gap is now closed for the one Installation that runs it: a routine merge is blocked by red CI with no `--admin` needed, for the first time since `0.6.0`. `PRODUCT/OPERATING_MODEL.md` Conformance row 15 re-verified, not carried over — moves from `Absent` to `Partial`: "difficult" now genuinely holds for this repository's own merges, while reach (item 37 — this fix is repository settings, not a framework mechanism `sync` delivers to any customer Installation) stays exactly as open as before. **Found and fixed in the same pass, not a new finding:** the table's own summary line below it ("Five `Present`, nine `Partial`, three `Absent`") was already stale before this row-15 move — a direct re-count off the table's own State column found four `Present`, eleven `Partial`, two `Absent`, not five/nine/three even before §15 changed. Corrected in place.

Findings this round did not fix: none.

**Next:**

1. **No new top candidate asserted.** OF-19 is now `Fixed`; item 33 is the only remaining named candidate and it ranks last, unconditionally, per DECISION-041 (needs a real external team). The next `/next` round re-derives from Step 3's own ranking over the full `Open Findings` queue rather than assume item 33 is now first by default — it explicitly is not, by founder instruction.
2. **OF-02/OF-03 are blocked on item 33** (real external validation) — same class, unchanged.
3. **OF-30's own vehicle question is open** (item 24 cannot retroactively serve as it). Unchanged.
4. **OF-64 stays open but is not currently actionable.** Unchanged.
5. **OF-87 stays unranked, deliberately, per its own text — evidence window extended, not concluded.** Unchanged.
6. **Item 41's own closing narrative (OF-85's one remaining live instance) is low-priority, non-contiguous content** — take with a dedicated archive-pass round if one occurs. Unchanged.
7. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged — and is now, genuinely, the only scheduled/queued item left that this AI can rank at all; everything else in the `Open Findings` queue is either blocked on item 33 itself or deliberately unranked/not-currently-actionable.

Not `/next` work, unchanged: item 33 needs an external party and ranks last by founder instruction.

Per Learning-023, checked before writing this pointer: this round fixed one finding (OF-19, founder decision) and refined one row via its Refine action (OF-03), so the figures above are read after this round's own edits.
---
Queue re-derived, nothing internally executable found, OF-85 refined, via `/next` (2026-08-21):

**Premise re-checked before scoping, per Learning-023 and `kenovis_shared_working_directory_concurrency`:** the primary working directory was 7 commits behind `origin/development` at bootstrap (`ListAgents`: 2 other peer sessions active, neither `kenovis-*`). All reading and every edit for this round done in a fresh isolated clone, fetched immediately before touching anything — level with `origin/development` at `246498e` (the previous round's own OF-19 fix), no race.

**Re-derived from Step 3's own ranking over the full `Open Findings` queue, not assumed from the previous pointer's summary** — the previous pointer explicitly said not to assume item 33 is first by default. Walked every genuinely-`Open` row directly off its own current text rather than trusting a prior round's characterization:

- **OF-02, OF-03** — blocked on item 33 (real external validation, ranks last per DECISION-041). Not executable.
- **OF-04** — a practiced discipline (44 citations), not a discrete remaining task; items 19-23 it was written against are already archived. Nothing to execute.
- **OF-30** — its own text says explicitly: fold into whichever round next validates a different agent-behaviour claim by self-report, not a dedicated round for this alone. No such round is running.
- **OF-64** — `CHANGELOG.md` `[Unreleased]` still holds exactly 1 header; nothing to merge. Not currently actionable, per its own last two refinements.
- **OF-85** — refined this round (see the row's own text): re-measured with real numbers instead of "roughly 100 lines" (19,454 bytes, lines 353-452, exact archive boundary at item 42). Found while re-measuring: seven OF-fix round narratives in between each already carry their own citable `OF-NN` id and are not obviously item-41-owned content to fold in — that is a real scoping question this row did not previously surface, which makes this **more** than a mechanical trim now, not less. Correctly not this round's objective.
- **OF-87** — deliberately unranked by its own text; evidence window still open (a dozen-plus rounds with no non-size structural finding Observe's current scope should have caught and did not), extending rather than concluding is still the right call.

**No row is ready to execute without either a human input this AI cannot supply (item 33's external party) or a trigger that has not yet occurred (a real `/release` run, another self-report-validation round, actual `[Unreleased]` fragmentation, a scoping decision on OF-85's seven narratives, more Observe evidence).** This is not the same state as an empty roadmap — `commands/next.md` Step 3 → "When The Roadmap Is Empty" requires all three of its inputs to be empty, and this queue still has seven genuinely `Open` rows and one `Scheduled` item — but it is the honest state of the *internally executable* subset of it: currently zero. Per DECISION-030, presenting nothing invented is the correct outcome here, not a failure to find something; inventing work to fill the round is explicitly worse than stopping (`commands/next.md` → "When The Roadmap Is Empty," same reasoning applies to a queue that is not empty but is fully blocked or deferred).

**Observe, run per `commands/next.md` Step 3:** `check_document_size.py` → 7 governed documents hold (2 archives over threshold by design, 1 index-bounded, 0 exempted). No new finding from this pass.

**Operating model section served: none.** No row's Conformance state changes as a result of this round — Observe found nothing, and OF-85's refinement sharpens a measurement without closing anything or advancing a numbered behaviour.

Findings this round did not fix: none.

**Next:**

1. **No top candidate — genuinely, for the first time this AI-OS's queue has reached this state.** Every remaining `Open` row is either blocked on item 33 (external party) or waiting on a trigger that has not occurred yet (named per-row above). The next `/next` round should re-check each trigger fresh rather than assume this state is unchanged — a real `/release` run, a fresh self-report-validation round, or `CHANGELOG.md` fragmentation happening are all independent events that could make OF-02/OF-03, OF-30, or OF-64 ripe without any deliberate action.
2. **OF-85 now has a sharper, real scoping question** (which of the seven OF-fix narratives are item-41-owned vs. independently citable) — a candidate for a founder call or a dedicated `/architect`-style scoping pass before the next round that reaches it, not a default pick.
3. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged — and remains the only scheduled item this AI-OS's own queue holds.

Not `/next` work, unchanged: item 33 needs an external party and ranks last by founder instruction.

Per Learning-023, checked before writing this pointer: this round fixed no findings and refined one row via its Refine action (OF-85), so the figures above are read after this round's own edits.
---
Learning-054's own Future action closed — a second live instance, via `/next` (2026-08-21):

**Premise re-checked before scoping, per Learning-023 and Learning-054 itself:** primary working directory read first, cheap and informative but stale by construction (`ListAgents`: 5 other peer sessions active, `kenovis-e3` among them). Cloned fresh into an isolated directory and `git fetch`ed immediately before the first edit — level with `origin/development` at `b7304af` (the previous round's own "nothing executable" pointer), no race on the fetch itself.

**The near-miss that is this round's own finding:** ranked OF-74/OF-75 as the objective from the primary directory's read — both looked like exactly the ready, low-cost, known-fix rows the previous-previous pointer named. The isolated clone's own fresh fetch showed both already `Fixed` (2026-08-21, same day, by a concurrent session, landed after the primary directory's own last read). Zero wasted edits, because the fetch ran before the first one — the discipline `kenovis_shared_working_directory_concurrency` (this session's own external memory) and `company-os/AI/memory/learnings.md` Learning-054 both name working exactly as designed.

**Re-derived from Step 3's own ranking over the current `Open Findings` queue, not assumed from the previous pointer's summary:** confirmed, row by row, the same seven genuinely-`Open` rows the previous round found (OF-02, OF-03, OF-04, OF-30, OF-64, OF-85, OF-87) — nothing on the tree changed between that round's close and this one's fetch, so its own "zero internally executable" conclusion re-verified rather than re-derived from scratch, and still holds.

**With no internally-executable roadmap row, this round's own objective became Learning-054's own named Future action** — "`.kenovis/AI/commands/next.md` Step 3 is a candidate for a short pointer to this entry in a future round, if the pattern recurs after this recording." It just recurred, live, in this same round. `framework/commands/next.md` (2.17 → 2.18) Step 3 gains "Under Concurrent Multi-Session Use, Verify The Tree Before Ranking": states the clone-and-fetch-before-first-edit discipline inline, citing Learning-054, reachable by any tool loading the Framework layer rather than only a session carrying one tool's own external memory forward. `company-os/AI/memory/learnings.md` Learning-054 gains this round's own instance and its `Disposition` moves from "Recorded as a technique" to `Fixed`, citing the framework change. This is a real, if unusual, round objective: not a roadmap row, but the discipline that just prevented this exact round from wasting an edit, promoted from tool-private memory into the tool-agnostic layer every Installation receives via `sync` — squarely `PRODUCT/OPERATING_MODEL.md` §5/§6 and DECISION-010's tool-agnosticism.

**Validated per `policies/documentation.md` → "A Claim Is Read Back Off The Artifact":** `grep -n "Learning-054" framework/commands/next.md` → the new subsection's own citation, present; `grep -c "Learning-054" company-os/AI/memory/learnings.md` → 2 (the original entry plus this round's addition), unchanged in count of headings (no new `## Learning-NNN` added, so `check_learning_promotions.py`'s population is unaffected — confirmed by re-running it clean). All 13 CI guards re-run clean after every edit.

**Observe, run per `commands/next.md` Step 3:** `check_document_size.py` → 7 governed documents hold (2 archives over threshold by design, 1 index-bounded, 0 exempted). No new finding from this pass.

**Operating model section served: §5** — *"Thread isolation must not silo knowledge."* Conformance row 5 re-verified, not carried over: stays `Partial` (the cadence-enforcement gap OF-38/item 42 name is untouched), but a concrete instance of knowledge that was silently thread/tool-siloed — this exact discipline — now lives in the Framework layer instead of one tool's own private memory. Row's own text and `As of` date updated in the same change.

Findings this round did not fix: none — this round's own finding (the near-miss) is fixed in the same round, not carried forward.

**Next:**

1. **Still no internally-executable roadmap row**, re-confirmed rather than assumed: OF-02/OF-03 (blocked on item 33), OF-04 (practiced discipline, not a task), OF-30 (waiting on a future self-report-validation round), OF-64 (nothing to merge right now), OF-85 (real scoping question, not a default pick), OF-87 (deliberately unranked, evidence window open) — unchanged from the previous pointer, and this round's own fresh walk found no trigger had flipped.
2. **The next `/next` round should re-check each row's own named trigger fresh**, same instruction as the previous pointer — a real `/release` run, a fresh self-report-validation round, `CHANGELOG.md` fragmentation, or a founder scoping call on OF-85 are the independent events that would change this.
3. **Item 33 ranks last, unconditionally, per DECISION-041.** Unchanged — the only scheduled item this queue holds.
4. **If a future round again finds the roadmap fully blocked/deferred, `commands/next.md` Step 3's new concurrency-verification subsection is not itself a candidate objective a second time** — it is now written; the next such round goes back to re-checking the seven rows' triggers, not looking for another meta-fix to make.

Not `/next` work, unchanged: item 33 needs an external party and ranks last by founder instruction.

Per Learning-023, checked before writing this pointer: this round fixed one finding (this round's own near-miss, closed in the same round via the `next.md`/Learning-054 fix) and refined one row via its Refine action (OF-87), so the figures above are read after this round's own edits.
---
Founder instruction, 2026-08-13 — the roadmap check becomes a step of dispositioning, not a habit

Given in a session that invoked no command: *"quiero que TRAS CADA analisis, ejecucion, aprendizaje, decision tomada, hallazgo encontrado... revises SIEMPRE si eso esta planificado en el roadmap, y si no lo esta, que lo añadas y lo planifiques"*. Another instance of OF-35's class — the founder detecting a structural gap the system did not; `grep -c "founder-raised\|founder asking\|raised by a human\|founder-flagged" PRODUCT/ROADMAP.md` → **21** on 2026-08-13, before this block was written.

What was missing, measured before anything was written rather than assumed: `grep -rni "already scheduled\|already covered\|duplicate finding\|existing item covers" .kenovis/AI/` → **0**, and `grep -rn "Pain" .kenovis/AI/policies/ .kenovis/AI/commands/` → **0**. DECISION-025 required a disposition and DECISION-027 required it in-session; neither required the finding to be compared against what is already planned, nor to be comparable to the other rows once queued. The dimensioning was real here — 41 of 47 `Open` rows already carry the three terms — and it was a forty-round habit, not a rule, and it was shipping as a format example inside a Product-layer template the customer owns.

Shipped, framework layer, so it reaches an Installation's next task through `sync` with nobody invoking anything (DECISION-026's test): `.kenovis/AI/policies/documentation.md` **3.4 → 3.5** gains the comparison against `PRODUCT/ROADMAP.md` before a disposition is written, the requirement that an `Open` row carry Pain, Frequency and Cost with `unknown` written where a term is unknown, and the sentence the instruction turns on — **recording is not planning**. `DECISION-029` carries the reasoning and the two alternatives the founder rejected: every finding becoming a `SCHEDULED` item at discovery, and per-finding judgement.

Ordering unchanged. This does not displace the `Next` pointer above; it changes how every round below it writes what it finds, not what runs first.

Findings this session did not fix: **OF-66**, **OF-67**, **OF-68**.
---
Open Findings (queue, added 2026-08-12)

A scheduled item is dimensioned work. A finding is a candidate that is not dimensioned yet, and before this section existed there was nowhere to put one — so findings landed in the narrative of whichever item happened to be open, where they had no id, no priority and no life after that item closed. That is the defect item 27 fixes as a rule and item 28 ships to every Installation; this section is this repository's own instance of it, authored first so the triage was not blocked on the framework change.

Every finding carries one of four dispositions. **Scheduled** — it is an item, named here. **Open** — real, unscheduled, competing for the next round. **Deferred** — deliberately not now, with the reason and the condition that would change it. **Rejected** — decided against, with the reason, so it is not proposed again. Prose is not a disposition.

First pass, 2026-08-12, over the 13 parked findings in this document and the standing candidates named in items 13-17's closing paragraphs. The `AI/memory/learnings.md` sweep (24 `Future action:` entries) is item 29 and is not done here.

| Id | Finding | Source | Disposition |
|---|---|---|---|
| OF-01 | `kenovis sync` deletes anything a customer put under `.kenovis/` without naming what it removed. `already up to date` is printed while files disappear. | Item 12, 2026-08-09 | **Fixed** (2026-08-18) — item 38: `sync` now reports `removedPaths` under `.kenovis/` instead of printing `(already up to date)`. Row corrected 2026-08-19 (stale `Scheduled` text). → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-02 | `/architect` has never been executed end to end from a real published Installation. Six for six such runs have found a maximal-Pain defect. | Learning-020/021/022 | **Open.** Highest expected yield of any unscheduled item on precedent alone. Item 24 may use it as its vehicle. Pain: high (six-for-six precedent). Frequency: low (an end-to-end run is not routine — once per validation pass, not per round). Cost: medium (needs a real published Installation with enough state to exercise `/architect` meaningfully, the same fixture shape item 24 already scopes). Role: cto. **Refined 2026-08-18 (DECISION-036, lowest-id `Open` row)** — premise re-checked against the tree rather than carried over: item 45 (2026-08-18) ran `/architect` end to end since the previous refinement, which could have closed this row on its face — checked, and it does not. Item 45 ran against this repository's own dogfooded Installation, not a real published one; the "Dogfooding caveat" this same document states elsewhere (item 18's validation note) applies here too — this repository is measurably unrepresentative of a customer's own environment. Still `Open`, unchanged. Item 33 (real external validation, the thing that would actually close this) stays last per DECISION-041. **Refined again 2026-08-19, per the literal text of DECISION-036** ("the lowest-id row still carrying `Open`") **rather than the "untouched by any prior refinement pass" substitute several rounds used on 2026-08-19 — see OF-99, opened this round, for why that substitute is itself a finding and not the rule.** Premise re-checked, not carried over: no `/architect` run since the previous refinement targeted a real published Installation — the one round that ran `/architect` in the interim (closing OF-78/OF-79/OF-90) ran it against this repository's own dogfooded instance, the identical caveat already on this row. Still `Open`, dimensions unchanged, item 33 still the actual close. **Refined 2026-08-21 (via `/next`, this round's own Refine action — least-recently-touched per DECISION-054, tied at 2026-08-19 with OF-03/OF-19/OF-64/OF-85/OF-87/OF-100, tiebreak to lowest id)**: premise re-checked, not carried over — a second `/architect` run landed since the previous refinement (closing OF-21/OF-22/OF-27, commit `074f079`), also against this repository's own dogfooded instance, the identical caveat already on this row; two-for-two since item 45, still zero against a real published Installation. Still `Open`, dimensions unchanged, item 33 still the actual close. |
| OF-03 | `/release` has never been executed end to end from a real published Installation. Item 14 fixed its Step 8; fixing one step is not running the command. | Learning-021/022 | **Open.** Same class as OF-02, one round behind it. Pain: high (same class as OF-02). Frequency: low (same reasoning). Cost: medium (same fixture requirement as OF-02). Role: cto. **Refined 2026-08-14 (DECISION-036)** — backfilled per OF-66. **Refined 2026-08-19 (DECISION-054, the `Open` row least recently touched — tied with OF-04 at 2026-08-14, tiebreak to lowest id)** — premise re-checked against the tree rather than carried over: the `[0.15.0]` cut (2026-08-18, `CHANGELOG.md`) is the one release since this row's last refinement, and `grep -n "via \`/release\`" PRODUCT/ROADMAP.md` finds no round narrative citing that command — the cut's own closing text (line 30 area) describes it directly rather than as a `/release` invocation, so this row's premise holds exactly as written: still no end-to-end `/release` run against a real published Installation. Dimensions unchanged; item 33 (real external validation) is still the actual close, same as OF-02, and still ranks last per DECISION-041. **Refined 2026-08-21 (DECISION-054, the `Open` row least recently touched — tied at 2026-08-19 with OF-85/OF-87, tiebreak to lowest id):** premise re-checked, not carried over — no release has cut since `[0.15.0]` (2026-08-18), so no new `/release` invocation exists to check. This round's own work (OF-19/DECISION-056) changed `development`'s branch protection, not release process — no bearing on this row. Still `Open`, unchanged, item 33 still the actual close. |
| OF-04 | Items 19-23 carry structural premises about file contents that were never verified when written — the defect Learning-023 records against item 18. | Learning-023, 2026-08-12 | **Open.** Cost is one command per item. Do it at the start of each item rather than as its own round. Pain: low-medium (a stale premise misleads scoping rather than breaking anything outright). Frequency: high (thirteen consecutive rounds have found their own item's stated figure stale, per the `Next` pointer's own running count). Cost: low (one command — already the practice every round since Learning-023 landed). Role: cto. **Refined 2026-08-14 (DECISION-036)** — backfilled per OF-66; Frequency is now measured, not estimated. **Refined 2026-08-21 (DECISION-054, the `Open` row least recently touched)** — premise re-checked against the tree rather than carried over: `grep -c "per Learning-023\|Learning-023" PRODUCT/ROADMAP.md` → **44**, up from the "thirteen consecutive rounds" this row was last measured against — the habit is not just intact, it is now the majority of rounds citing it every time they scope work. `.github/scripts/*.py` still names no guard for a stale premise (`grep -rl "Learning-023\|stale premise\|premise" .github/scripts/*.py` → **0**, unchanged) — still a practiced discipline, not a mechanism, exactly as the row's own Cost line states. Still `Open`, dimensions otherwise unchanged. |
| OF-05 | An Installation that has never run install/sync since the hash sidecar shipped has no recorded hash, so its next `CLAUDE.md` transition still relies on the old prefix check. | Learning-008, item 7 | **Deferred.** Accepted and documented at the time; self-heals on the first sync. Revisit only if a customer reports a lost `CLAUDE.md`. |
| OF-06 | `/framework-review` has never been run as an audit pass over the post-migration framework layer. | Items 3, 6, 8, 11 closing paragraphs | **Deferred.** Expected yield low — items 6, 8, 10, 12, 14, 16 and 18 each swept the same surface. Revisit after items 19-30 land, when the surface has changed again. |
| OF-07 | No active version check: nothing tells an installed customer a newer Framework Release exists. `cli/README.md` → "Upgrading" covers discovery at near-zero cost today. | Phase 2 → New Capabilities, 2026-08-06 | **Deferred.** First network dependency in the CLI's core logic; Pain and Frequency unvalidated against one external team. Condition to revisit: real Phase 2 usage data. |
| OF-08 | The paid open-core tier has no ADR on its gating mechanism, and must not ship without one. | Phase 2 → Prerequisite, 2026-08-06 | **Deferred.** Premature on one external team's data. Blocks "additional specialized agents" when Phase 2 opens. |
| OF-09 | `sync` has no diff preview or conflict detection before it mirror-replaces. | Phase 0 item 3 slice 4 | **Deferred.** Reversibility is satisfied by the target's own git history; ergonomics belong to Phase 2. |
| OF-10 | Two records this block implements but never wrote: a `DECISIONS.md` entry making the per-session context budget a first-class constraint, and a `learnings.md` entry that documentation-as-memory without lifecycle is accumulation, not memory. | Second block header, 2026-08-12 | **Fixed** (2026-08-18, founder decision in-session) — **DECISION-044**: no per-session context budget is imposed; existing bounding work is confirmed sufficient. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-11 | `MVP Success Metrics` → Usage has no target: "N installations (target not yet set)". Every release round since `0.9.0` has rejected `1.0.0` partly on this, so an unset number is gating the version. | Phase 1 → MVP Success Metrics | **Fixed** (2026-08-18, founder decision in-session) — **DECISION-045**: no installations target is ever set as a `1.0.0` gate. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-12 | One external team has validated the product, on 2026-08-06, against `kenovis@0.3.0` — eight releases ago. `AUTOMATIONS/user-feedback.md` describes a feedback loop that has never run. | Phase 1 → Real External Validation | **Scheduled — item 33.** Every "deferred on unvalidated Pain" disposition above traces to this one gap. Previously tagged a founder call, which was wrong: it is work, and tagging work as a decision is a way of parking it. |
| OF-13 | `CHANGELOG.md` had no archive rule — 67.3 KB, every past release inline. | Item 21, 2026-08-12 | **Fixed** (2026-08-12) — item 35. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-14 | `check_document_size.py`'s failing case had never been exercised for any governed document. | Item 20, 2026-08-12 | **Fixed** (2026-08-16) — exercised via `AI/memory/learnings.md` (grown past threshold, `split` removed in memory only), confirmed exit 1 with the exact expected message, reverted byte-clean. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-15 | `CHANGELOG-ARCHIVE.md` is 53.1 KB on the day it was created and grows one released section per release — roughly 4-5 KB each, so it crosses the 60 KB threshold in about two releases. It is registered `archive_of`, so the guard will classify it over-threshold-by-design and never fail on it. | Item 35, 2026-08-12 | **Deferred.** Correct by construction — an archive is where the weight goes and is never on the session-initialization path, which is exactly what `archive_of` encodes. Recorded because the classification means no check will ever raise this file again, and `PRODUCT/ROADMAP-ARCHIVE.md` reached 143.1 KB the same way. Condition to revisit: an archive is read often enough that its size costs something, or a second-level archive becomes cheaper than one flat file. |
| OF-16 | Released changelog prose cross-references sections that moved to the archive. | Item 35, 2026-08-12 | **Rejected** — editing shipped prose to tidy a cross-reference is the compression the archive rule forbids. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-17 | A round wrote its findings into the session summary instead of the queue, leaving them with no id. | Item 35, 2026-08-12, founder-raised | **Fixed** (2026-08-12) — item 36. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-18 | Scheduling changes and implementation changes land in the same commit. | Item 34, 2026-08-12 (backfilled) | **Rejected** as work — commit hygiene, already governed by `policies/git.md`. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-19 | `gh pr merge --rebase --admin` — this repository's standard merge, used on every round since `0.6.0` — bypasses required status checks, not only the required review. So all ten CI guards are advisory: they are bypassable by the same command and the same person they constrain, on the merge that lands the work. Five PRs were merged that way on 2026-08-12 alone. | DECISION-026 / item 36, 2026-08-12 | **Open.** Strengthens DECISION-026 rather than competing with it — it is a second, independent reason CI is not enforcement, and the reason item 37 is the priority instead of adding an eleventh guard. Item 34's "Solo-maintainer note... Not a misconfiguration" is about the *review* half and is still correct; the *checks* half was never examined. Priority: Pain medium (the guards have caught real defects and no round has knowingly merged red), Frequency high (every merge), Cost low to *decide* — the real question is whether required reviews should stop being required for a solo maintainer so `--admin` is not needed at all, which is a repository-settings call, not code. **Second instance, 2026-08-14:** the same tension blocked a round from *starting* — `policies/git.md` requires every round to branch fresh from `development`, and `development` sat one commit behind an open, unreviewed PR from the previous round — itself a consequence of OF-80's own fix, which made "reached a branch" the completion bar instead of "merged". Resolved the same way, with the founder's explicit confirmation in-session rather than assumed: `gh pr merge --rebase --admin --delete-branch`. **Third instance, same day, same shape, different round:** identical situation on entry to *this* round — PR #121 (the round that shipped DECISION-036) sat `OPEN`/`REVIEW_REQUIRED`/`BLOCKED`, `development` one commit behind it. Resolved identically, confirmation sought and given in-session before the merge, not assumed from the previous instance in the same session. Three for three is no longer a pattern worth restating per-instance — the next occurrence is not a fourth row addition, it is evidence for whichever way OF-19's own standing question resolves. **Refined 2026-08-14 (DECISION-036's own mechanism, lowest-id `Open` row not touched by the previous round's refinement pass).** **Refined again 2026-08-19 (DECISION-036, lowest-id `Open` row after this round's own OF-67 fix)** — premise re-checked against GitHub's own branch-protection API for `development`, not carried over from the row's 2026-08-12 text: `gh api repos/rafabarranco/kenovis/branches/development/protection` returns `required_pull_request_reviews.required_approving_review_count: 1` and `enforce_admins.enabled: false`, confirming the *review* half exactly as written — a solo maintainer cannot merge without `--admin` because there is no second reviewer, and `enforce_admins: false` is what lets `--admin` bypass it. The *checks* half is not what the row's own text says: the protection response carries no `required_status_checks` key at all, meaning the ten (now thirteen) CI guards were never configured as required status checks on this branch in the first place — there is nothing for `--admin` to bypass on that side, because nothing there was ever blocking. So "bypasses required status checks, not only the required review" overstates the mechanism: `--admin` bypasses one required control (review), not two, and the guards' non-enforcement traces to them never being wired into branch protection, not to an admin override defeating a wiring that exists. The practical conclusion this row exists for is unchanged — CI is not enforcement here, and a repository-settings call is still what would fix it — but the specific claim is corrected rather than restated. Priority unchanged: Pain medium, Frequency high (every merge), Cost low-to-decide. Still **Open**, still a founder call, not executed this round — presented rather than picked, per `commands/next.md` Step 3 → "When The Next Objective Is Not Yours To Execute". **Fixed** (2026-08-21, founder decision, via `/next`) — **DECISION-056**: presented per Step 3, three options laid out (leave as-is; wire CI only; drop review to 0 and wire CI), founder chose the third. `development`'s branch protection reconfigured live via the GitHub API: `required_pull_request_reviews` removed entirely (the review requirement was unsatisfiable for a solo maintainer to begin with); `required_status_checks` now names both CI jobs (`Markdown links, markers, template references and artifact destinations`; `cli/ build, typecheck and tests`) with `strict: false` — the first time the CI guards have been wired as enforcement rather than advisory. `enforce_admins` unchanged (`false`) — an admin override remains available as a deliberate emergency escape hatch, not removed. Verified live: `gh api repos/rafabarranco/kenovis/branches/development/protection --jq '{required_status_checks, enforce_admins, required_pull_request_reviews}'` → `required_pull_request_reviews: null`, `required_status_checks.checks` lists both jobs, `enforce_admins.enabled: false`. Net effect: `gh pr merge --rebase` (no `--admin`) now succeeds once CI is green, for the first time since `0.6.0` — the routine merge path is gated by the guards it was always meant to be. |
| OF-20 | A Claude Code hook was proposed as in-thread enforcement and abandoned without a disposition. | This session, 2026-08-12 | **Rejected** — a hook is one tool's configuration and fails DECISION-010 before it reaches DECISION-026. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-21 | `check_item_findings.py` binds its population to closed items, but findings are round-scoped, not item-scoped. The two findings this session missed — OF-19, born inside DECISION-026's body as evidence for rejecting an option, and OF-20, born in conversation as a proposal that was dropped — were both structurally invisible to it: neither was attached to a closing item. The guard shipped in item 36 cannot catch the class of miss that produced it. | This session, 2026-08-12, founder-raised | **Fixed** (2026-08-19, via `/architect`, bundled with OF-22/OF-27) — **DECISION-051**: `Findings this round did not fix:` required alongside every `Next:` pointer, the one artifact every round writes regardless of what it closed. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-22 | A rejected roadmap item stays cited elsewhere in the tree as the plan that closes a gap. Item 25 (`kenovis check`) was rejected on 2026-08-12; four guard docstrings kept naming it as what would eventually reach a customer, and read as scheduled for a full round afterwards. Nothing detects it — `check_links.py` sees no link, and the citation is prose. | Item 37, 2026-08-13 | **Fixed** (2026-08-19, via `/architect`, bundled with OF-21/OF-27) — **DECISION-052**: a rejection names its own `Citations swept:` sweep in the same change; `check_rejection_citations.py` guards it. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-23 | The archive pass had no trigger — a registered `split` satisfies `check_document_size.py` at any size, so nothing forced the pass to actually run as the document grew back past threshold. | Item 37 round 1, 2026-08-13 | **Fixed** (2026-08-15) — the archive-pass trigger is codified in `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" and has sustained practice behind it; residual weight carried by OF-51/OF-62. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-24 | A new policy section can restate a rule another policy already carries, and nothing notices. | Item 37 round 2, 2026-08-13 | **Fixed** (habit half, 2026-08-13) — `policies/documentation.md` 3.2 → "Single Source of Truth". Detection half folded into OF-22. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-25 | The shipped Framework layer could name origin-repository-only infrastructure as the thing enforcing a rule — no mechanism stopped a repeat once item 37 dispositioned the ten that existed. | Item 37 round 2, 2026-08-13 | **Fixed** (2026-08-16) — an eleventh guard, `check_github_citations.py`, a literal-substring grep with no classifier; its framework-layer home states the citation from both directions. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-26 | The findings rule reached a session only through a command that named it — 5 of 19, its checkable form 1 of 19. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-13) — item 39. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-27 | This repository's root `CLAUDE.md` is not the `CLAUDE.md` any Installation gets. The CLI generates a minimal stub (`cli/src/domain/installation.ts` → `claudeStubContent`) that points at `SYSTEM.md`; nothing links the two, so a rule written into this repository's constitution reaches zero customers and reads as shipped. Found while executing item 39, which had planned exactly that and would have shipped nothing. | Item 39, 2026-08-13 | **Fixed** (2026-08-19, via `/architect`, bundled with OF-21/OF-22) — **DECISION-053**: root `CLAUDE.md` restates, never originates, a framework-wide rule; `check_claude_stub_sync.py` guards the finding-routing table's sync with `installation.ts`. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-28 | The findings-routing rule named six Product-layer destinations that do not exist yet in a fresh Installation, between `kenovis init`/`kenovis add` and the end of setup. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — `cli/src/domain/installation.ts` `claudeStubContent`: the pending stub no longer claims those destinations exist, and names the setup command as where findings go until it creates them. Two new unit tests; 112/112 cli tests pass. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-29 | `/init-project` and `/adopt-project` had no disposition step for what setup itself surfaces. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — `init-project.md` 1.12 Step 13 and `adopt-project.md` 1.12 Step 14, each a closing disposition step once every destination exists; both gain a Completion Criterion. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-30 | Item 39 wrote a `Validated when:` condition — a session that runs no command, finds something and does not fix it has a written instruction telling it where that goes — and then closed in the same round on the author's own assertion that it held. No independent check ran. Item 24 (RA1) exists because exactly this shape already failed once: a claim about agent behaviour confirmed by the agent that authored the instruction. | `/explain`, 2026-08-13 | **Open.** The class is items that state a behavioural validation condition and satisfy it by self-report. Item 24 is the instance for decision bodies and is still SCHEDULED and unrun, so the pattern it was written to catch has now recurred before its own fix shipped. Priority: Pain medium — the cost is not that item 39 is wrong (its file changes are verified: `cd cli && npm test` → **112/112**, ten guards, the rule read back off `cli/dist/`), it is that "the rule works" and "the rule is written" were closed as one claim. Frequency: every item whose target is agent behaviour rather than file content. Cost low if folded into item 24, which needs a vehicle anyway and now has a second one. Role: cto. Recommendation: do not schedule separately — make item 39's condition item 24's second fixture. **Refined 2026-08-16 (DECISION-036)** — premise re-checked against the tree rather than carried over: item 24 confirmed still `SCHEDULED` (`grep -n "^24\." PRODUCT/ROADMAP.md`), the CLI test count corrected from the stale 110 to the current 112 (`cd cli && npm test`), and Role added. **Refined 2026-08-21 (via `/next`, this round's own Refine action — least-recently-touched, per DECISION-054), premise corrected rather than carried over: item 24 is not `SCHEDULED`, it closed `DONE` on 2026-08-16, the same day this row's own previous refinement ran and evidently missed it.** Checked directly rather than assumed: item 24's closing narrative (`PRODUCT/ROADMAP.md` item 24, or the fresh subagent's decision-body-citation run) validated a different behavioural claim — whether an agent opens a decision's body instead of paraphrasing its index (RA1) — never item 39's own condition, quoted directly from its archived body (`PRODUCT/ROADMAP-ARCHIVE.md` → "item 39"): *"a session that runs no command, finds one thing and does not fix it, has a written instruction telling it where that thing goes — and it is loaded before the finding happens, not after."* So this row's recommendation ("make item 39's condition item 24's second fixture") was never actually executed — item 24 closed without it, and this row's disposition was never updated to say so. The underlying gap is exactly as open as when this row was written: item 39's own self-report closure is still unvalidated by an independent run. Recommendation corrected: item 24 is closed and cannot be item 39's vehicle retroactively; a fresh fixture is needed — the cheapest shape is folding item 39's condition into whichever session next validates a *different* agent-behaviour claim (the same "needs a vehicle anyway" logic this row already used once), not a dedicated round for this alone. Pain, Frequency unchanged; Cost still low-if-folded, now genuinely unfolded rather than pending. Role: cto. |

| OF-31 | Findings route by destination file, never by responsibility — not one of twelve agent files knows findings exist. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — DECISION-035. `policies/documentation.md` 3.10 requires an `Open` finding to name its owning role from the existing roster, loaded once rather than copied into twelve agent files. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-32 | The chain stopped at capture — `DISCOVERY → ANALYZE → CLASSIFY → REFINE → PLAN → ROADMAP` was the stated model; what existed was `DISCOVERY → a row with a disposition`, with no step that ever revisited an `Open` row. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — DECISION-036. Every round now refines the lowest-id `Open` row (`policies/documentation.md`, `commands/next.md` Step 3) as a second action alongside its own objective; row id substitutes for a persisted age counter. Demonstrated live this round on OF-02, which also closed OF-66. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-33 | Nothing observes. All commands are keyed to a human intention; the product is unobserved between sessions. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-15) — DECISION-038. "Continuous" is not architecturally achievable (same reasoning as DECISION-037's §15); `commands/next.md` 2.12 Step 3 gains a required, bounded `Observe` subsection, scoped on its first instance to document-weight drift. The breadth this row names beyond that one class is **OF-87**. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-34 | Instrumentation-vs-product ratio question — a duplicate of item 40 part 3, in different words. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-16) — answered by DECISION-040 when item 40/OF-35 closed; missed then, caught refining the queue this round. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-35 | The system's own failure to self-detect is recorded eleven times and its detection half has never been scheduled. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-16) — **DECISION-040** (`PRODUCT/OPERATING_MODEL.md` Addendum B, founder-supplied): founder-as-detector rejected outright; `Observe`/`Refine` carry the load. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-36 | The document defining Kenovis's operating model did not exist on disk. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-13) — `PRODUCT/OPERATING_MODEL.md`, verbatim with its provenance. Its reconciliation half is OF-39 and item 41 step 3. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-37 | The three archives were knowledge sinks nothing read — no instruction named when to open one. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-15) — `.kenovis/AI/SYSTEM.md` 1.12 → "Context Loading Rules" extends the existing decision-body-on-demand rule to all three archives; `policies/documentation.md` 3.12 points its own "read on demand" clause at that rule rather than restating it. Retrieval (item 23) stays out of scope, still behind item 22. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-38 | The disposition rule's trigger — "before the session ends" — was an event nothing detects. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — `CLAUDE.md` 2.7, `SYSTEM.md` 1.11, `policies/documentation.md` 3.9: all three re-anchor the trigger to the moment of discovery, naming the pre-setup window as the one real exception. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-39 | Two Source Of Truth Hierarchies, in the two documents every session loads, and they disagree. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — item 41 step 1, DECISION-031. One hierarchy, in `SYSTEM.md`; `CLAUDE.md` cites it. A third ordering was found in `COMPANY_OS.md` while closing this and is **OF-73**. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-40 | **An analysis can hold the artifact in its hands and file a request that someone create it** — the founder's operating-model document, already supplied, was queued as a `founder call` instead of written to disk. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-16) — `policies/documentation.md` → "A Finding Is Fixed, Scheduled, Or Rejected" gains the supplied-versus-found paragraph. Closes [[Learning-028]] with it. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-41 | Item 40's drift measurement used `cli/src` as the denominator instead of the actual shipped bundle (`.kenovis/AI/`), overstating the instrumentation-to-product ratio. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-15) — re-measured guard weight directly off the tree (0.26× the shipped bundle, not 46% of a different denominator); corrected figures recorded on item 40. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-42 | **The founder's model of this product's own layering does not match the repository, and nothing checks that it does.** Stated by the founder on 2026-08-13 while asking this question: *"la carpeta .kenovis es un ejemplo de una build"*. It is not an example and not a build output — `.kenovis/AI/` is the tracked source that `bundle-framework-assets.mjs` publishes, per DECISION-020, and the script's own header says so. The fact is on disk in three places (`ENGINEERING/ARCHITECTURE.md` line 97, the bundle script, DECISION-020's body), which is what makes this a finding rather than a documentation gap: it is written, it is correct, and the person the Source Of Truth Hierarchy ranks above every document does not hold it. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-15) — item 43: `.kenovis/` became generated build output rather than committed source (DECISION-039), resolving the founder's corrected model directly. Row corrected 2026-08-19 (stale `Scheduled — item 43` text). → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-43 | No document answered "is the AI-OS doing its job" without re-deriving it at full cost each time. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-16) — `PRODUCT/OPERATING_MODEL.md` → "Conformance" (item 41 step 2, DECISION-032) is the standing artifact this row asked for; the behavioural half (does the founder read it instead of asking) stays tracked at Conformance §3. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-44 | The core invariant is stated as unbreakable and was enforced nowhere. | Item 41 / `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — DECISION-037. "Impossible" is not achievable under DECISION-010/DECISION-013; "difficult" is partial (DECISION-033's required-line pattern) with two gaps that stay separately tracked, not folded in: item 37 (reach) and OF-19 (bypass). Conformance row 15 stays `Absent` — the decision explains the gap, it does not close it. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-45b | `claude-info.md` sits untracked at the repository root — the founder's original paste, byte-identical to the operating model. | This session, 2026-08-13 | **Rejected** as work — committing it would create a second copy of the specification outside the Product layer. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-45 | `PRODUCT/OPERATING_MODEL.md` sat untracked, one `git clean` from gone. | `/analyze`, 2026-08-13 | **Fixed** (found stale while refining OF-02 per DECISION-036, 2026-08-14) — the instance closed independently, `git log -- PRODUCT/OPERATING_MODEL.md` shows commit `72deb8b` tracking it; the rule half this row asked for is what OF-80's Completion Criterion ("the round's work has reached a branch... before the thread ends") already states in general form — no separate clause needed. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-46 | `/next` writes the "Next" pointer and no `/next` is told to read it. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-13) — item 42 parts 2-3, `commands/next.md` 2.6 Step 3, DECISION-030. The row's 131 KB figure was stale on execution; the file read 153,375 bytes (OF-04). → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-47 | `/next` has no behaviour for an item that only a human can execute. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-13) — item 42 parts 2-3, `commands/next.md` 2.6 Step 3, DECISION-030. Descending the priority order is now forbidden in those words. Residues: OF-70, OF-71. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-48 | **`/explain` instructs the opposite of the unconditional findings rule.** `commands/explain.md` → "Documentation Opportunity": *"If an explanation reveals missing knowledge: **Recommend** creating documentation in PRODUCT/, DOMAIN/, ENGINEERING/, `docs/`."* Recommending is prose, which the dispos... | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — item 42 part 4. `commands/explain.md` 2.0 → 2.1: "Documentation Opportunity" cites the disposition rule instead of saying "recommend", and `docs/` is gone. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-49 | **The stated end condition is a state with no defined behaviour.** Addendum A §4: the loop runs *"hasta que no haya nada en el roadmap del producto"*. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — item 42 part 5. `commands/next.md` 2.8 → 2.9 Step 3 gains "When The Roadmap Is Empty": define empty as all three inputs empty, name which of the two meanings applies, do not invent work, stop and record. The observation half stays OF-33. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-50 | The specification is not on any session-initialization path. | `/analyze`, 2026-08-13 | **Fixed for this repository** (2026-08-14) — item 41 step 1, DECISION-031: named in `CLAUDE.md` 2.4, `SYSTEM.md` 1.8 and `bootstrap.md` 2.8. The framework half — whether every Installation authors one — is **OF-72**, a founder call. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-51 | **Every thread pays the whole roadmap, and it keeps growing.** Measured 2026-08-13: `PRODUCT/ROADMAP.md` 131,511 bytes against `COMPANY_OS.md` + `.kenovis/AI/SYSTEM.md` + `CLAUDE.md` + `PRODUCT/OPERATING_MODEL.md` — 172,805 bytes total on the mandatory path. Under Addendum A's one-thread-per-item cadence that is paid once per roadmap step, not once per working day. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-21, via `/next`) — twelve superseded round narratives archived verbatim, `PRODUCT/ROADMAP.md` 363,766 → 295,373 bytes (18.8% reduction, byte-verified); residual carried by OF-62. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-52 | **Twelve of nineteen commands and workflows still route a finding nowhere, and three of them are the ones that run.** Post-item-39 count, `grep -ci "Open Findings\|disposition\|Findings this"` per file on 2026-08-13: `next` 3, `analyze` 1, `bug` 1, `commands/review` 1, `workflows/review` 1 — and ... | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — item 42 part 4, as an audit rather than an edit sweep. Three contradicting instructions found and fixed (`workflows/framework-review.md`, `commands/release.md`, `workflows/bugfix.md`); the row's own count was wrong in both directions and is corrected in the progress block. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-53 | `/next` Step 11 validated a kind of change this product rarely makes — none of its four checks touch a markdown file, and most rounds change only markdown. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — `commands/next.md` 2.9 → 2.10 Step 11 splits code-artifact from document-artifact validation; the latter names `policies/documentation.md` → "A Claim Is Read Back Off The Artifact" as the form. Anchoring grep: 0 → 2. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-54 | **`/next` → "Autonomous Mode" contradicts the founder's supplied cadence.** The command says *"Claude may continue through multiple roadmap items."* `PRODUCT/OPERATING_MODEL.md` → Addendum A §1 says one thread executes one `/next`. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — item 42 part 4, DECISION-034. `commands/next.md` → "Autonomous Mode" defers to the Installation's own `PRODUCT/OPERATING_MODEL.md`; the framework default is one item per round. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-55 | No agent role owns the framework layer (`.kenovis/AI/`), this repository's own product; it defaulted to `cto` by absence. | `/analyze`, 2026-08-13 | **Fixed** (2026-08-14) — DECISION-035. Recorded in `ENGINEERING/ARCHITECTURE.md` (1.6 → 1.7), not in the framework-layer `agents/cto.md` every Installation receives — the ownership is true of this repository only. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-56 | **A relative link written inside `PRODUCT/` resolves from `PRODUCT/`, and nothing says so.** `check_links.py` caught a markdown link in `PRODUCT/ROADMAP.md` whose target was written repository-relative (`PRODUCT/OPERATING_MODEL.md`) rather than sibling-relative — it resolves to `PRODUCT/PRODUCT/O... | This session, 2026-08-13 | **Fixed** (2026-08-14) — item 42 part 4. `policies/documentation.md` 3.6 → 3.7 gains "A Path In These Documents Is Written Repository-Relative, In Backticks". The guard's inline-code blind spot is not fixed and is OF-25's class. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-57 | `.kenovis/AI/SYSTEM.md` denied the operating model's §1 in its Final Principle, and shipped that denial to every Installation. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-13) — DECISION-028. What remains is OF-58. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-58 | *"The AI is not an autonomous decision maker"* was unscoped in `SYSTEM.md` and `CLAUDE.md`, contradicting the operating model's own owner/AI-OS split. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-14) — `SYSTEM.md` 1.9 → 1.10, `CLAUDE.md` 2.5 → 2.6: both now scope the sentence to the product's own direction, strategy and business decisions, naming `PRODUCT/OPERATING_MODEL.md` as where the split actually lives rather than hardcoding this repository's own section numbers. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-59 | A rejected item was still cited as the live plan inside recorded knowledge — three instances. | This session, 2026-08-13 | **Fixed** (2026-08-13, founder instruction) — deleted in place, each leaving a dated note. Detection stays OF-22. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-60 | `/bootstrap` never checked the branch it is on is current with the remote, and the standard merge leaves it stale in either direction. | This session, 2026-08-13 | **Fixed** (2026-08-13, item 42 part 1) — `commands/bootstrap.md` 2.7 Step 5 and `policies/git.md` 2.3 → "Rebasing" require the branch be verified level with its remote before any work; validated live, unplanned, on the very next merge this repository ran. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-61 | **Two lifecycle rules cannot both be satisfied at steady state, and running one to completion broke the other.** The archive pass of item 42 part 1 moved the last four inline DONE narratives out of `PRODUCT/ROADMAP.md`, which took `check_item_findings.py`'s population to **0 closed items with a narrative, 39 archive pointers** — and the guard failed the repository, because an empty population was hardcoded as `"nothing to check, which is not a pass"`. The repository had done exactly what `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" requires. | Item 42 part 1, 2026-08-13 | **Fixed** (2026-08-19, via `/architect`, closed as a direct consequence of OF-21/DECISION-051) — the round-scoped `Next:` pointer population can never go structurally empty the way the item-scoped population can. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-62 | **The archive rule has been run to its limit and the document is still over threshold — the multiple grew, not shrank, even as the queue itself got healthier.** `PRODUCT/ROADMAP.md` re-measured 2026-08-18 (item 44's round) rather than carried over: **211,741 bytes** against 60 KB (was 129,094 when this row was written, 2026-08-13) — **3.5×**, up from 2.1×. What moved underneath that number, measured rather than assumed: `Open` queue rows are **down** to 26 (from 42) — real dispositioning happened — but total queue rows grew to 93 (from ~62) and the DONE-item closing narratives (item 44's own included) keep adding bulk faster than archiving can remove it, each round's own archive pass notwithstanding (this round's own pass compacted what it could — see the round's own archive-pass note below). Nothing further is archivable without archiving open work, which the rule explicitly forbids. | Item 42 part 1, 2026-08-13 | **Open. Refined 2026-08-21 (via `/next`), premise corrected rather than carried over: "the archive rule has been run to its limit" and "nothing further is archivable" were both false.** OF-51's own round found and executed the missed lever — superseded round narratives, never archived as a class — and cut `PRODUCT/ROADMAP.md` from 363,766 to 295,373 bytes (still **4.9×** the 60 KB threshold, down from ~6×). What survives, checked directly rather than assumed: `grep -c '\*\*Fixed\|\*\*Rejected' PRODUCT/ROADMAP.md` finds Fixed/Rejected queue rows still carrying full multi-paragraph disposition text with no `→ PRODUCT/ROADMAP-ARCHIVE.md` pointer — the compaction `policies/documentation.md` → "Archiving a queue row compacts it; it does not remove it" already requires: **33 rows by direct count, 75,899 bytes (~74 KB)** — a script over the queue table matching every row whose disposition column contains `Fixed`/`Rejected` with no `ROADMAP-ARCHIVE.md` pointer, summing line lengths, not estimated. This is OF-85's own gap, at scale: OF-85 tracked isolated instances of a row closing without being compacted in the same pass; the accumulated backlog across the whole queue is the same defect, sized. Priority: Pain high (this is now the *largest* remaining lever, larger than the one OF-51's round just executed), Frequency: every thread, Cost: low per row, additive across the 33 rows — mechanical (excerpt the row to one clause + disposition + pointer, move the full text to the archive verbatim), no design decision blocking it, the same shape OF-51's own round just executed for round-narrative blocks. Role: cto. Take with the next dedicated archive-pass round — ready to execute as-is, not blocked on an idea the way this row's text read before this refinement. **Fixed (2026-08-21, via `/next`)** — this round's own objective, executed as scoped above: every Fixed/Rejected queue row carrying no archive pointer is now compacted, 37 rows by direct count (not 33 — the row's own estimate undercounted; three short-disposition rows it likely screened out as "good enough" (OF-10, OF-11, OF-89) still lacked a pointer and were compacted too, and OF-69 was a 37th instance the estimate missed entirely — it reads `Open` only inside a quoted historical fragment, per OF-87's own note, and was actually `Fixed` and uncompacted). Verified: `grep -c "\*\*Fixed\|\*\*Rejected" PRODUCT/ROADMAP.md` finds 0 rows whose disposition lacks a `→ PRODUCT/ROADMAP-ARCHIVE.md` pointer, script-checked, not sampled. `PRODUCT/ROADMAP.md` 299,418 → 254,269 bytes at this round's own close (a 45,149-byte, 15.1% reduction, **4.9× → 4.1×** the 60 KB threshold), each of the 37 original rows preserved byte-for-byte under its own `### queue row OF-NN` heading in `PRODUCT/ROADMAP-ARCHIVE.md`, verified programmatically before this row was written. |

| OF-63 | A learning's `Disposition:` named an unpromoted rule, gave it no id, and `check_future_actions.py` passed because the `Future action:` line cited other real ids. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-14) — item 41 step 2. The rule it parked is promoted: `policies/documentation.md` 3.6 → "An Instruction Is Reachable, And Its Sink Is Read", third failure mode, from [[Learning-036]]. Its plausible home was recorded as `testing.md` and was wrong. The mechanical half — that guard reads `Future action:` and not `Disposition:` — stays with OF-21. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-64 | **`CHANGELOG.md`'s `[Unreleased]` section fragments further with every round that appends to it, not just the two the row was written against.** Re-measured 2026-08-18 rather than carried over: `### Changed`/`### Added`/`### Removed` headers within `[Unreleased]` now number **12** (7 `Changed`, 4 `Added`, 1 `Removed`), up from 3 when this row was written 2026-08-13 — and item 44's own closing round (this same session) added one more `### Changed` at the top rather than merging into an existing adjacent one, exactly the guess the row's own text predicted a future round would make. `check_changelog.py` still passes — it verifies a bullet exists for a framework-layer change, not the section structure. | This session, 2026-08-13 | **Open.** **Refined 2026-08-18** — premise confirmed and worse, not carried over: 3 → 12 headers in five days, four rounds' worth of "guess which one to use," including this session's own item 44 round. Still low severity for the same reason as before: `[Unreleased]` is cut verbatim into a released section at release time, and `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" forbids editing released prose afterwards — so the fragmentation becomes permanent the moment a release ships. Priority: Pain low now and unfixable later (unchanged), Frequency: every round that appends, not just every release cut — revised up from the original dimensioning, since the row's own prediction ("every future round... has to guess") has now been observed four times rather than assumed once. Cost trivial before the cut (merge same-heading sections into one), zero-after-impossible following it. Take it as a step of the next release, with the changelog trim `policies/documentation.md` already schedules there — or as a cheap five-minute fix any round passing through, since cost does not grow with header count the way the document itself does. **Corrected 2026-08-19, found while ranking this round's objective, not a new finding:** the [0.15.0] cut (2026-08-18) moved `[Unreleased]`'s prior content into a released section, and `[Unreleased]` since then holds exactly **1** header (`### Fixed`) — `sed -n '/^## \[Unreleased\]/,/^## \[0\.15\.0\]/p' CHANGELOG.md | grep -c "^### "` → **1**, not the 12 this row's own text asserted. Every round since the cut, this one included, has appended only `### Fixed` bullets, so no fragmentation opportunity has actually arisen post-cut — not because anything structural now prevents it. Truth-value unchanged (`Open`): the underlying defect (no rule merges same-heading sections, nothing stops the next `### Changed`/`### Added` bullet from opening a fourth header) is exactly as real as when the row was written, only currently unmanifested. Not ready-to-execute as a top candidate on this measurement — there is nothing to merge right now. **Refined 2026-08-21 (DECISION-054, the `Open` row least recently touched — last `Refined` 2026-08-18, one day older than every other genuinely-`Open` row, each tied at 2026-08-19 or later):** premise re-checked, not carried over — `sed -n '/^## \[Unreleased\]/,/^## \[0\./p' CHANGELOG.md \| grep -c "^### "` → **1**, unchanged since the 2026-08-19 correction; every round since the [0.15.0] cut, including this one, has still appended only under the single existing `### Fixed` header rather than opening a second. This round's own CHANGELOG edit is itself a live instance of exactly the choice the row worries about — merge into the existing header or open a new one — and it merged, the compliant choice, which is one more data point that the drift stays unmanifested by discipline rather than by any structural guarantee. Truth-value and dimensions unchanged. |

| OF-65 | **The rule this round shipped is unrunnable as written in the repository that shipped it.** `commands/bootstrap.md` 2.7 Step 5 and `policies/git.md` 2.3 both instruct `git fetch origin`. In this repository `origin` is an SSH remote with no key available to the session: every `git fetch origin` and `git push origin` this session attempted exited `128` with `git@github.com: Permission denied (publickey)`, and each one had to be reissued against an explicit HTTPS URL with `gh auth git-credential`. It is also the mechanical reason `gh pr merge --delete-branch` cannot level the checkout, so OF-60's failure fires on **every** merge here rather than occasionally. | This session, 2026-08-13, founder-raised | **Fixed** (2026-08-19, via `/next`) — `bootstrap.md` Step 5 gains the HTTPS-credential-helper fallback clause immediately after its bare `git fetch origin`. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-66 | **Six `Open` rows predate DECISION-029's dimensioning rule and do not meet it.** Read off the file 2026-08-13: of **47** `Open` rows, **41** carry Pain, Frequency and Cost and **6** do not — OF-02, OF-03, OF-04, OF-10, OF-11, OF-14. | This session, 2026-08-13, founder instruction (DECISION-029) | **Fixed** (2026-08-14) — all six backfilled with Pain/Frequency/Cost/Role, as a direct consequence of DECISION-036 (item 41 §1, OF-32): refining OF-02 to demonstrate the new mechanism made backfilling the other five nearly free in the same pass. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-67 | **The framework policy now ranks findings by terms whose definition ships only in a file the customer owns.** `policies/documentation.md` 3.5 requires Pain, Frequency and Cost on every `Open` row. The formula those terms come from — `(Customer Pain × Frequency × Business Impact)` — is at `.kenovis/AI/templates/product-layer/PRODUCT/ROADMAP.md:246`, a Product-layer template: authored once at setup, never touched by `sync` (DECISION-021, RULE-INST-01), and freely editable by the Installation afterwards. `grep -rn "Pain" .kenovis/AI/policies/ .kenovis/AI/commands/` → **0** before this round and the policy still does not define the terms, only require them. So a customer can hold a loaded policy citing a ranking their own roadmap no longer describes, and every `sync` will keep the policy current while the definition drifts. | This session, 2026-08-13, DECISION-029 consequences | **Fixed** (2026-08-19, via `/next`) — `policies/documentation.md` cites where the Pain/Frequency/Cost formula lives (the Product-layer template) instead of duplicating it. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-68 | `DECISIONS.md` → "Document Layers" restated in prose two counts the Decision Index already carries as marks, and both went stale on every append — three corrections in eleven decisions. | This session, 2026-08-13 | **Fixed** (2026-08-14) — item 41 step 2. The counts are deleted; the paragraph names what `‡` and `★` mean and gives the two commands that produce the numbers. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-69 | **A merge that lands on the remote and fails locally prints only the failure, so the session reads it as "the merge did not happen".** Reproduced on PR #107, 2026-08-13: `gh pr merge 107 --rebase --admin --delete-branch` output nothing but `fatal: Could not read from remote repository.` and `! warning: not possible to fast-forward to: "development"` — while `gh pr view 107` reported `MERGED` at `16:16:13Z` with merge commit `4ad5db6`. The remote half had succeeded. The local half — delete the feature branch, check out the base, fast-forward it — needed `origin` over SSH (OF-65) and died, after the checkout had already moved. Net state: working tree on a `development` **1 commit behind**, with the local feature branch gone and `git status -sb` printing a clean `## development...origin/development`, because the tracking ref was never updated either. | This session, 2026-08-13, reproduced live | **Fixed** (2026-08-18) — `policies/git.md` → "Rebasing" confirms a merge from `gh pr view`'s own state rather than the command's exit, then repairs the local checkout (`fetch` + `merge --ff-only`). Row corrected 2026-08-21 (two earlier rounds asserted it closed without editing this row's disposition). → `PRODUCT/ROADMAP-ARCHIVE.md` |

**Fixed** (re-verified 2026-08-18, lowest-id untouched `Open` row, DECISION-036's mechanism) — this row's own requested fix ("confirm the merge from the PR's state rather than the command's exit, then repair the local checkout, which is two commands") is exactly what `policies/git.md` → "Rebasing" now states, written to close **OF-86** on 2026-08-16 and citing the identical composite-command failure shape this row described three days earlier. Not assumed closed by citation alone: this session hit the identical failure twice more, independently, on two different PRs (#143 and #144, this thread, 2026-08-18) — both times `gh pr merge --admin --delete-branch` printed only `Permission denied (publickey)` / `not possible to fast-forward`, both times `gh pr view` confirmed a real `MERGED` commit, and both times the documented `fetch` (HTTPS credential-helper) + `merge --ff-only` sequence recovered the local checkout to `0` commits behind on the first attempt — a fresh thread's own behavioural evidence, not the authoring round's self-report (OF-30/Learning-031). Not yet compacted to the archive — see OF-85's ninth instance.

| OF-70 | The `Next` pointer became load-bearing and nothing held a round to writing one; Step 3's no-pointer fallback made an omission read as intended behaviour. | Item 42 parts 2-3, 2026-08-13 | **Fixed** (2026-08-14) — item 41 step 2's residue, DECISION-033. `commands/next.md` 2.8 Step 13 requires a `Next:` line carrying the ranking, or the word `none` with why. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-71 | **The new stop-branch names a file to record in and no section of it.** `commands/next.md` 2.6 Step 3 says to *"record in `PRODUCT/ROADMAP.md` that this round reached that item and stopped, and on what it is blocked"*. That document has three places a round writes: an item's progress narrative, the `Open Findings` queue, and the `Next` pointer. A stopped round produced no progress on the item, found no new finding, and did not change the ordering — so all three are wrong-shaped, and the instruction resolves to "somewhere in a 150 KB file". | Item 42 parts 2-3, 2026-08-13 | **Fixed** (2026-08-19, via `/next`) — `next.md` names the `Next:` pointer as exactly where a blocked round's stop-and-reason record goes. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-72 | The hierarchy shipped to every Installation had a rank 1 no Installation has: neither setup command asked for `PRODUCT/OPERATING_MODEL.md` and no template existed. | Item 41 step 1, 2026-08-14 | **Fixed** (2026-08-14, founder answered in session) — item 41 step 2, DECISION-032. Every Installation authors it at setup, before `COMPANY_OS.md`, from a template the AI may never answer itself; rank 1 stops being conditional. The migration gap it leaves is **OF-78**. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-73 | A third Source Of Truth ordering, in `COMPANY_OS.md` → "Knowledge Hierarchy", inverting DOMAIN/ and PRODUCT/ against DECISION-031 and omitting `DECISIONS.md`. | Item 41 step 1, 2026-08-14 | **Fixed** (2026-08-14) — item 41 step 2, DECISION-032. Now "What Each Document Defines", stating no precedence and citing `SYSTEM.md`. The row called it Product-layer only; the **template** carried it too, so the framework was shipping the third ordering — corrected in both. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-74 | **`--force-with-lease` is inert when the push target is a URL instead of a tracked remote, and this repository can only push by URL.** `grep -cin "force-with-lease" .kenovis/AI/policies/git.md` → **3**, mandated in all three ("Use `--force-with-lease`, never `--force`"). The lease compares against the *remote-tracking ref* for the branch. Pushing to an explicit HTTPS URL creates no such ref, so the lease has no baseline: here it exited `! [rejected] (stale info)` and refused a legitimate push. That is the safe direction. The unsafe one is the same mechanism with a stale tracking ref, where the lease passes against a baseline that is no longer what the remote holds — protection that reports success and checked the wrong thing. Reached by URL because `origin` is SSH with no key available (OF-65), which makes URL pushes this repository's only path rather than an unusual one. | This session, 2026-08-14, hit live | **Fixed** (2026-08-21, via `/next`) — `policies/git.md`'s `--force-with-lease` gains the URL-qualified form (`=<branch>:<remote-sha>`, read via `git ls-remote`); landed with OF-75. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-75 | **Nothing in the framework covers a stacked pull request, and the standard merge breaks one by construction.** `grep -cin "stacked\|dependent branch" .kenovis/AI/policies/git.md` → **0**. This session opened PR #111 from PR #110's head. Merging #110 with `gh pr merge --rebase` rewrote its commits onto `development`, and #111 immediately went `CONFLICTING/DIRTY` carrying **3 commits** — its own plus two whose content was already merged under different SHAs. `policies/git.md` → "Rebasing" tells you to rebase and resolve, which here means resolving conflicts between a branch and an already-merged copy of itself: the most error-prone form of the operation, on the one occasion when a wrong resolution silently drops merged work. | This session, 2026-08-14, hit live | **Fixed** (2026-08-21, via `/next`) — `policies/git.md` gains a "Stacked Pull Requests" section: tree-hash captured before the base merges, asserted equal after rebuild; landed with OF-74. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-76 | The Conformance table shipped to an Installation as a one-row form and no instruction built the other rows, so the criterion was inert everywhere but this repository. | Item 41 step 2, 2026-08-14 | **Fixed** (2026-08-14) — item 41 step 2's residue, DECISION-033. `commands/next.md` 2.8 Step 13 builds the table when it is still the form, `unmeasured` where unverified; the template (1.1) names Step 13 as the owner and defines `unmeasured`. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-77 | The standing criterion was load-bearing with nothing holding a round to it, and re-verifying one row leaves the other sixteen stale. | Item 41 step 2, 2026-08-14 | **Fixed** (declaration half, 2026-08-14) — item 41 step 2's residue, DECISION-033: `commands/next.md` 2.8 Step 13 requires an `Operating model section served:` line naming the section or `none`. The staleness half is **OF-79**. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-78 | **A Product-layer fix reaches no Installation that already ran setup, and this round shipped two of them.** `sync` mirror-replaces `.kenovis/` and never writes the Product layer (RULE-INST-01), so: an Installation created before DECISION-032 has no `PRODUCT/OPERATING_MODEL.md` and nothing gives it one — `SYSTEM.md` 1.9 names the state and routes it to the owner, which is an instruction, not a migration; and an Installation that already authored its `COMPANY_OS.md` from the old template still holds the third Source Of Truth ordering OF-73 closed, in a document the corrected hierarchy ranks at 2. Both are correct by RULE-INST-01 and both are silent. | Item 41 step 2, 2026-08-14 | **Fixed** (2026-08-19, via `/architect`, bundled with OF-79/OF-90) — **DECISION-048**: `sync` prints an informational Framework-Release-changed notice pointing at the templates for manual comparison, no new provenance mechanism. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-79 | **A conformance row is re-verified only when a round happens to serve its section, so sixteen of seventeen rows go stale per round.** OF-77's second half, split out when its declaration half was fixed rather than left riding on a closed row. `commands/next.md` 2.8 Step 13 requires the round to update *the row for the section it served*, which is correct and bounded, and says nothing about the rest; the table's own text says a row's state is read off the tree with the command in the row. Those two hold together only if something re-reads the untouched rows, and nothing does. Measured: this round re-verified **1** of **17** rows (§15), the previous round **4** of **17**, and `PRODUCT/OPERATING_MODEL.md`'s table is dated as a full pass on 2026-08-14 — a date that starts decaying the moment the next round closes. | Item 41 step 2's residue, 2026-08-14 | **Fixed** (2026-08-19, via `/architect`, bundled with OF-78/OF-90) — **DECISION-049**: Conformance table gains a per-row `As of` date instead of a mandatory full seventeen-row re-verification every round. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-80 | **A round's output can end the session uncommitted, and nothing in `/next` says otherwise.** `grep -cin "commit\|pull request\|\bPR\b\|push" .kenovis/AI/commands/next.md` → **0**, and its Completion Criteria list eight conditions, none of them about the work reaching a branch. | This session, 2026-08-14 | **Fixed** (2026-08-14) — item 42 part 4. `commands/next.md` Completion Criteria gains a ninth condition: the round's work has reached a branch, deferring to `policies/git.md` for how. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-81 | `workflows/roadmap.md` claimed the same `/next` trigger as `commands/next.md`, with zero inbound references and none of four rounds' worth of process fixes. | Item 42 part 4's OF-52 audit, 2026-08-14 | **Fixed** (2026-08-14) — deleted. `grep -rn "workflows/roadmap" .kenovis/AI/` → 0 before and after; its templates (`feature-plan.md`, `adr.md`) stay, independently referenced by `architect.md`, `feature.md`, `bugfix.md`, `architecture.md`. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-82 | The reach-measuring keyword `grep -ci "Open Findings\|disposition\|Findings this"` misclassified three files in both directions, and the wrong count set a ranking. | Item 42 part 4's OF-52 audit, 2026-08-14 | **Fixed** (2026-08-14) — `policies/documentation.md` 3.7 → 3.8 → "A Claim Is Read Back Off The Artifact" requires a reach-measuring command to grep the rule's exact section title, not an improvised keyword. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-83 | **The operating-model template asks the owner four questions and none of them is the cadence the framework now defers to.** DECISION-034 makes `commands/next.md` obey a cadence stated in an Installation's own `PRODUCT/OPERATING_MODEL.md`, and `.kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md` gives that owner no place to state one. So the deference is real and unreachable: an Installation gets the one-item-per-round default and no invitation to change it. | Item 42 part 4, DECISION-034, 2026-08-14 | **Fixed** (2026-08-18, via `/next`) — both setup commands now ask a fifth "working cadence" question matching the operating-model template's own section 5. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-84 | **Three promoted learnings were marked `Fixed as a rule` and their target files did not cite them.** | This round's archive pass, 2026-08-14 | **Fixed** (2026-08-19, via `/next`) — three promoted learnings (Learning-032/033/034) each gained a citation line at the rule they were promoted into. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-85 | **A round that fixes and archives a queue row in the same pass can update its disposition without compacting it.** Closing OF-48, OF-52, OF-54, OF-56, OF-80 and OF-49 in item 42 parts 4-5's own archive pass copied each row to `PRODUCT/ROADMAP-ARCHIVE.md` and rewrote its disposition to `Fixed... → archive`, but left the Finding and Source columns at their original, full length in the active file. `policies/documentation.md` → "Closed Work Is Archived, Not Kept Inline" says a compacted row "stays as one line — id, the finding in a clause, its disposition, and the pointer"; these six rows average well over 400 characters of Finding text each, none of it a clause. So the active document paid for the pass's disposition-column edits and kept the weight it had before archiving. | This round's own archive pass, 2026-08-14 | **Open.** Cheap to check: a script over the queue table's Finding-column lengths for the six ids confirms all remain full paragraphs. Not fixed here — trimming six Finding clauses without losing the reasoning trail (already preserved in the archive snapshot either way) is editorial judgement per row, not a mechanical move, and this round's own OF-53 and OF-60 compactions are the corrected pattern to follow. Priority: Pain low (the six rows are wrong-sized, not wrong-content), Frequency: every round that fixes-and-archives in the same pass, which is now the observed norm, Cost low per row and additive across rows. Take it with the next round that opens this document's archive-pass step. **Seventh instance, 2026-08-16:** OF-43's active row carries a full paragraph with no `→` pointer at all, while a longer, separately-written copy already sits in `PRODUCT/ROADMAP-ARCHIVE.md` under its own `### OF-43` heading — found while this round opened the archive-pass step to close OF-14, not fixed here for the same editorial-judgement reason as the original six. **Eighth instance, 2026-08-18:** closing item 41 this round left its own numbered section (steps 1-4, `Validated when`, two `Progress` sub-blocks — roughly 100 lines) and its trailing 2026-08-18 round narratives inline rather than moved to `PRODUCT/ROADMAP-ARCHIVE.md` under a `### item 41` heading, the pattern items 37, 38, 42 and 43 each got on their own closing round. Not fixed here, and for a sharper reason than editorial judgement alone this time: item 41's content is not contiguous — its numbered section sits at this document's item list and its two most recent progress rounds sit near the `Next` pointer, written there at the time per this document's own append convention — so compacting it correctly means reconciling two locations, not trimming one paragraph, and doing that blind, inside a round already carrying four other changes, is a real risk to a heavily cross-referenced document that the size saving does not justify taking today. **Ninth instance, 2026-08-18:** OF-69, refined and closed `Fixed` this same round (DECISION-036's Refine action, lowest-id untouched `Open` row), stays inline as a full paragraph rather than compacted — the round's own real objective was OF-93's fix; folding a manual archive-move into the same pass as a routine Refine action is scope beyond what DECISION-036 itself requires (a row's *text* changing, not also its *location*), and is left for a dedicated archive-pass round instead. **Refined 2026-08-19 (DECISION-036, lowest-id `Open` row untouched by any prior refinement pass):** premise re-checked against the tree rather than carried over — the original six instances (OF-48, OF-52, OF-54, OF-56, OF-80, OF-49) are no longer live: each now carries a `→ PRODUCT/ROADMAP-ARCHIVE.md` pointer and a one-clause disposition, confirmed by direct read, so a dedicated archive-pass round already fixed the population this row was written against. OF-43 (seventh instance) is likewise now a single compact paragraph, no `→` pointer but no longer "well over 400 characters" either. What remains live: OF-69 (ninth instance) still sits as a full multi-paragraph disposition with no archive pointer, and item 41's own closing narrative (eighth instance) is still inline, uncompacted, for the reason its own instance already gives (non-contiguous content, real risk to compact blind). Truth-value unchanged — still `Open` — but the population is now two live instances, not eight, and six of the eight originally cited are stale citations as of this refinement. Priority unchanged: Pain low, Frequency every round that fixes-and-archives in the same pass, Cost low per row. Take with the next dedicated archive-pass round, per the row's own standing instruction. **Corrected 2026-08-21**, a direct consequence of this round's own OF-62 compaction pass, not a new refinement: OF-69 (ninth instance) is now compacted, archived under `### queue row OF-69`. Population is one live instance — item 41's own closing narrative (eighth instance) — unchanged for the non-contiguous-content reason already given. **Refined 2026-08-21 (DECISION-054, tied with OF-87 at 2026-08-19, tiebreak to lowest id):** premise re-checked with real numbers instead of "roughly 100 lines" — item 41's own numbered-list entry (line 353) is already a single compact line, but its steps 1-4 / `Validated when` / two `Progress` sub-blocks still sit inline immediately after it, `sed -n '353,452p' PRODUCT/ROADMAP.md | wc -c` → **19,454 bytes** (item 42 begins at line 453, already archived, giving an exact boundary). The row's "two progress rounds sit near the `Next` pointer" claim is sharper than before too: those are specifically the "This round's objective... §3/§12 re-verification" and "Item 41 — independent confirmation and closure" blocks, locatable directly rather than described. **New, found while re-measuring, not assumed:** the seven OF-fix round narratives in between (OF-38/28/29, OF-32/66, OF-44, OF-33, OF-37, OF-41 — roughly lines 501-707) each open with their own "item 41 step 4 continues" line, but are each already properly headed by their own `OF-NN` id and read naturally as *that finding's own fix record*, not as item-41-owned content — folding all seven into one `### item 41` archive heading would bury seven independently-citable fix records inside one umbrella heading, which is a real design question this row did not previously surface, not an oversight the original "roughly 100 lines" estimate accounted for. Cost is still not "low" once that's accounted for: correctly scoping this needs a decision about which of those seven narratives move and which stay independently addressable, before a single compaction edit is safe. Priority unchanged: Pain low, Frequency every fix-and-archive round, but Cost is now more accurately "needs a scoping decision first," not a mechanical trim — closer to OF-51/OF-62's own former shape than to OF-53/OF-60's simple pattern. Still `Open`, take with a dedicated round that scopes the seven-narrative question before touching anything. |
| OF-87 | **`/next`'s new `Observe` step (DECISION-038) is bounded to document-weight drift only — one narrow slice of what `PRODUCT/OPERATING_MODEL.md` §1 names** ("problems, risks, inconsistencies, technical debt, missing work, and opportunities"). Architectural inconsistencies, contradicted rules, dead cross-references and undocumented technical debt still depend entirely on a human or an `/analyze` run pointing a round at them — the exact gap OF-33 named, now narrowed rather than closed. | Item 41 step 4 / OF-33, 2026-08-15 | **Open.** Not a design question this round is positioned to answer well: widening the checklist without evidence of what belongs on it repeats the mistake DECISION-038 explicitly rejected (an unbounded "look for anything" instruction). The honest next step is evidence, not a bigger list — let the current bounded checklist run for several rounds and see what the *undetected* class of finding actually turns out to be, the same way document-weight drift itself was evidenced by three prior rounds (OF-23, OF-51, OF-62) before this round's fix targeted it. Pain: medium (the wider breadth is real and unaddressed, but nothing shows it costing more today than document-weight drift already did before it was addressed). Frequency: unknown — no instance yet of an undetected non-size finding since this checklist exists to be compared against. Cost: unknown, and load-bearing — widening the checklist is cheap text, but choosing what belongs on it without inventing an enumeration is the actual work DECISION-038 declined to do here. Role: cto. **Refined 2026-08-19 (DECISION-036, lowest-id `Open` row untouched by any prior refinement pass — OF-69 excluded per its own 2026-08-19 correction; it reads `Open.` only inside a quoted historical fragment and is actually `Fixed`):** premise re-checked against the tree, not carried over. `framework/commands/next.md` → "Observe" still names exactly the same two-item checklist (document size, exemptions) as when this row was written on 2026-08-15 — unchanged. Roughly a dozen rounds have closed since (DECISIONS.md now runs through DECISION-050, twelve past DECISION-038 which introduced Observe), none of which surfaced an instance of "a non-size structural finding Observe's current scope should have caught and did not" — every structural finding in that window was still found by a human prompt, an `/analyze`/`/architect` run, or a round's own objective, never by Observe itself reaching past its two checks. That is consistent with the row's own Frequency term ("unknown — no instance yet") holding rather than resolving, and is itself the evidence-gathering the row's own text asked for ("let the current bounded checklist run for several rounds and see what the undetected class of finding actually turns out to be") — the answer after a dozen rounds is still no observed instance, not yet a basis for widening the list either way. Truth-value unchanged (`Open`), dimensioning unchanged, evidence window extended. **Refined 2026-08-21 (DECISION-054, least-recently-touched — every other genuinely-`Open` row was touched earlier the same day by concurrent rounds; OF-87 last touched 2026-08-19, two days stale, the clear oldest survivor):** premise re-checked against the tree, not carried over. `company-os/DECISIONS.md` now runs through **DECISION-056** — eighteen decisions past DECISION-038, up from twelve at the previous refinement, six of them (DECISION-051 through DECISION-056) landing the same day as this refinement across a burst of concurrent rounds. None is an instance of Observe's current two-item checklist (document size, exemptions) catching something outside its bounded scope: DECISION-051/052/053 came from a directed `/architect` bundle, DECISION-054/055 from a round auditing the queue itself, DECISION-056 from a founder decision presented and answered in-session — none from Observe reaching past its two checks. Frequency term ("unknown — no instance yet") still holds exactly as written. Truth-value unchanged (`Open`), evidence window extended again. |
| OF-86 | **`gh pr merge --admin --delete-branch`'s local housekeeping (checkout base, delete branch, pull) uses the repository's default git remote, and failed silently against it.** | This session, 2026-08-14 | **Fixed** (2026-08-16) — `policies/git.md` → "Rebasing" names the HTTPS-credential-helper relevel sequence immediately after `gh pr merge --delete-branch`; validated live on a fourth instance the same round. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-88 | **`check_links.py` resolves relative markdown links against the filesystem, and this repository's own README.md, CHANGELOG.md, CONTRIBUTING.md and cli/README.md correctly link to `.kenovis/AI/...` paths — customer-facing, and no longer present in this repository without a build, since DECISION-039.** Found running the full guard suite against a genuinely `.kenovis`-absent checkout (the state item 43's own Phase 3 smoke test never actually tested, since it always re-ran the build before checking): 17 broken links across 4 files, all `.kenovis/AI/...`, all correct for a customer, none resolvable in this repository's own tree without a build the CI job that runs this guard never performs (Python-only, no Node). | This session, 2026-08-16, found while bootstrapping the round after item 43 | **Fixed** (2026-08-16) — `check_links.py` gains a `framework/` fallback for `.kenovis/AI/...` links when `.kenovis/` is absent (the real CI condition post-DECISION-039). → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-89 | `PRODUCT/OPERATING_MODEL.md` Addendum C read as deferring item 33 (real external validation) until the roadmap empties, while item 33's own text argued for running it soon — two founder-adjacent sources pointing opposite directions. | This session, 2026-08-16, founder-supplied (Addendum C) | **Fixed** (2026-08-16) — **DECISION-041** (founder answer): any external-customer-testing work ranks last in `/next` Step 3, unconditionally; `1.0.0` follows it. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-90 | **A finding can be tagged with an owning role, but no mechanism ever hands it to that role.** DECISION-035 lets an `Open` finding name a role from the Agent Roster; DECISION-036 makes every round refine the lowest-id `Open` row regardless of its tagged role — refinement is round-driven, not role-driven. No agent file reads its own tag: `grep -rln "Open Findings\|disposition" framework/agents/*.md` → **0** across 12 files, unchanged since OF-31/OF-55 closed. `PRODUCT/OPERATING_MODEL.md` §12 states "the role that owns the responsibility must process the discovery" — naming an owner is not the owner processing anything, and nothing routes a session toward the specialist role a finding names ahead of the generic round-robin picking it up by id order. | `PRODUCT/OPERATING_MODEL.md` → Conformance §12, re-verified against item 41's own flagged staleness, 2026-08-18 | **Fixed** (2026-08-19, via `/architect`, bundled with OF-78/OF-79) — **DECISION-050**: an `Open` row's tagged `Role` becomes a `/next` Step 3 ranking factor, not a new scheduling slot. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-91 | **The learning loop's client-facing half is invisible to the owner: nothing owner-facing states that reusable learnings reach Installations as framework rules, and the founder concluded they are lost.** Founder asked in-session (2026-08-18) whether the learnings and decisions recorded in this repository's Product layer "se pierden" when a customer installs. The tree answers no, and has since item 20 (2026-08-12): a reusable learning becomes a rule in `framework/` (`AI/memory/learnings.md` → "Promoted And Archived" — 22 promotions in the first pass alone, pointer half enforced by `check_learning_promotions.py`), the rule ships in the npm bundle (`cli/scripts/bundle-framework-assets.mjs`) and reaches every Installation via `kenovis sync`; a framework-level (`‡`) decision's effect lands in `framework/` the same way while its `DECISIONS.md` body stays here as this product's own memory. But the loop is stated nowhere the owner reads: `grep -icn "promot" README.md cli/README.md` → **0** and **0**; README.md's only "learnings" line describes accumulation ("learnings accumulated by this product"), never the promotion back into what ships. | Founder question in-session, 2026-08-18 | **Fixed** (2026-08-19, via `/next`) — `README.md` gains "Where learnings go", stating the promotion loop the founder believed did not exist (`grep -icn "promot" README.md` 0 → 2). → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-92 | **The Product layer claims seven generic root names in every Installation (`COMPANY_OS.md`, `DECISIONS.md`, `PRODUCT/`, `DOMAIN/`, `ENGINEERING/`, `AUTOMATIONS/`, `AI/memory/`), so a brownfield target owning any of them forces the Collision Guard to ask the human to overwrite or move aside their own file — the founder proposes namespacing the whole Product layer under one dedicated, visible directory so a customer's own files are never overwrite candidates at all.** DECISION-017 chose root placement for visibility ("la infra product-specific debe estar visible al usuario") but only ever weighed hidden-vs-root; a visible dedicated parent was never an option on its table. DECISION-039's no-mixing rule is satisfied, not violated, by a parent holding only Product-layer content. | Founder question in-session, 2026-08-18 | **Fixed** (2026-08-18) — item 44: the seven Product-layer elements re-rooted under `company-os/` (DECISION-043). Row corrected 2026-08-19 (stale `Scheduled` text). → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-93 | **`init-project.md`/`adopt-project.md` Step 11/12's own verify-grep is guaranteed to fail against the Conformance table its own Step 2/3 just told the session to leave unfilled.** Verified directly against the command text, not inferred: `init-project.md` Step 2 says *"Leave the Conformance table at the foot of that template as the form it is... Do not fill it during setup with states nobody measured."* Its own `OPERATING_MODEL.md` template ships that table's row example as `[ANSWER: Present / Partial / Absent, with the command or measurement that shows it]` (`framework/templates/product-layer/PRODUCT/OPERATING_MODEL.md` line 63). Step 11's verify check is unconditional: *`grep -rn "\[ANSWER:" company-os/...`* — *"Zero matches is the passing result. Every match is a question that was never answered."* — with no stated exception for the one bracket its own Step 2 told the session to leave in place. `adopt-project.md` Step 3/Step 12 carry the identical pair. | Two independent fresh-subagent behavioral validations of item 44's `/init-project` and `/adopt-project` fixture runs (zero conversational inheritance from each other or from this thread), 2026-08-18 | **Fixed** (2026-08-18) — the operating-model template's Conformance row example uses literal `unmeasured` instead of `[ANSWER:`, closing a guaranteed verify-grep failure in both setup commands. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-94 | **A customer's pre-existing, real root `CLAUDE.md` has no coexistence path with Kenovis's own stub — only refuse-or-overwrite.** Verified against the code: `kenovis init`/`add` throws `ExistingClaudeMdError` when the target's `CLAUDE.md` exists and is not already a Kenovis-managed stub (`isClaudeMdSafeToOverwrite` in `cli/src/domain/installation.ts`), with the message *"Move your existing CLAUDE.md aside first, or re-run with --force to overwrite it anyway."* `--force` overwrites the file entirely — the CLI is explicitly non-interactive (`ENGINEERING/ARCHITECTURE.md` Hard Rules) and cannot merge. Checked whether `/adopt-project` recovers a set-aside original afterward: `grep -n "CLAUDE.md" framework/commands/adopt-project.md` shows only the Collision Guard (Product-layer files, a different mechanism) and the closing step reverting the stub's pending directive to steady state — nothing reads or re-incorporates a customer's original CLAUDE.md content that was moved aside or force-overwritten. Content is not destroyed (recoverable via `git diff`/`git log`, same as any tracked file), but nothing automatic brings it back in; a customer following the tool's own recommended path ("move it aside") is left to merge by hand. | Founder question in-session on the company-os/ design, 2026-08-18 | **Fixed** (2026-08-19, via `/next`) — **DECISION-047**: CLAUDE.md coexistence (`resolveClaudeMdWrite`) replaces refuse-or-`--force` as the default; hash sidecar scoped to the Kenovis block alone. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-95 | **A round that finds a previous round's uncommitted Product-layer work has no stated recovery procedure — only detection.** `commands/next.md` Completion Criteria (OF-80, `Fixed`) requires a round's own output to reach a branch before its thread ends, and `commands/bootstrap.md` Step 5 tells a new round to inspect git status and "determine... unfinished tasks, potential conflicts" — but neither names what to *do* once uncommitted Product-layer content from an earlier round is found. `grep -cin "fold into\|carry forward\|recover" .kenovis/AI/commands/bootstrap.md .kenovis/AI/commands/next.md` → **0, 0**. | This round, 2026-08-18 — found `company-os/PRODUCT/ROADMAP.md` modified on `development` with no open PR (OF-94, a fully-dimensioned, discovery-complete finding), apparently left by a round whose thread ended before its own git steps ran | **Fixed** (2026-08-18) — `bootstrap.md` Step 5 names the recovery for a predecessor round's stranded, uncommitted Product-layer work: fold it into the current round's own branch as its own distinct commit. → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-96 | **A customer install ships only a root `CLAUDE.md` stub — no `.claude/commands/` — so even Claude Code, DECISION-010's named primary tool, gets zero native slash commands after `kenovis init`; every other AI tool has no generated entry point at all, only DECISION-010's "manually load `AI/SYSTEM.md`" fallback, which is a policy sentence, not a CLI capability.** Verified against the code and the bundler, not inferred: `cli/scripts/bundle-framework-assets.mjs` bundles only `framework/` (`agents/`, `commands/`, `policies/`, `templates/`, `workflows/`) into the npm package; `grep -n "\.claude\|commands\|skills" cli/scripts/bundle-framework-assets.mjs` matches nothing naming `.claude`. `cli/src/domain/installation.ts` writes exactly one customer-facing file, the `CLAUDE_STUB_FILENAME` (`CLAUDE.md`) — `.claude` appears only in `NON_EVIDENCE_ENTRIES` (so a customer's own pre-existing `.claude/` is not misread as brownfield evidence), never as something the CLI creates. Meanwhile this repository's own `.claude/commands/` (`next.md`, `feature.md`, `bug.md`, `bootstrap.md`, `architect.md`, `review.md`, `release.md`, `hotfix.md`, `analyze.md`, `explain.md`, `init-project.md`, `adopt-project.md`) proves the native-slash-command shape works and is maintained — it is simply never packaged for distribution. A founder session raised this directly, 2026-08-18: for a non-Claude tool (Cursor, GitHub Copilot, Windsurf, Gemini CLI, Codex/`AGENTS.md`, etc.) there is no equivalent generated at all, and no install-time question or detection asking which tool the customer runs. | Founder question in-session, 2026-08-18 | **Fixed** (2026-08-18) — item 45: per-AI-tool scaffolding from a data-driven `framework/tool-adapters/<id>/` registry, selected by a non-interactive `--tools` flag (DECISION-046). Row corrected 2026-08-19 (stale `Scheduled` text). → `PRODUCT/ROADMAP-ARCHIVE.md` |
| OF-97 | **A session's post-merge local-checkout sync (`git fetch` + `git merge --ff-only`) can be blocked by Claude Code's own Auto Mode permission classifier — a distinct failure mode from OF-65's SSH/credential class, with no framework instruction naming what to do when it happens — and the round that hit it first reported the gap only in its reply to the founder, not in this queue, until asked "¿Está planificado?"** Reproduced live, 2026-08-18: after merging a PR from an isolated clone (per `kenovis_shared_working_directory_concurrency`), advancing the shared working directory's own `development` to match — `git -c credential.helper='!gh auth git-credential' fetch origin development` chained with `git merge --ff-only origin/development` — was refused outright by the harness with `Blocked by classifier`, not by git itself (no `128`, no credential prompt; the command never ran). `policies/git.md`'s existing OF-65/OF-86/OF-88 fixes all assume the failure is git reporting an error; none covers the harness refusing to run the command at all. | This session, 2026-08-18, surfaced by the founder asking whether the leftover item was planned, after this round's own reply named it without recording it — the exact "saying it to the human is not recording it" failure `SYSTEM.md` and `CLAUDE.md` both name in those words | **Fixed** (2026-08-19, via `/next`) — `policies/git.md` → "Rebasing" names a harness classifier block as its own git-sync failure case, distinct from OF-65 (SSH) and OF-86 (wrong remote). → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-98 | **`git push`/PR creation can fail with a 403 "Permission denied" against the wrong authenticated `gh` identity, silently — this machine has 3 `gh` accounts logged in (rafabarranco, barve-leyias, leyais), the "active" one fluctuates between sessions, and no framework instruction names the check or the fix.** Reproduced live, 2026-08-19: `git -c credential.helper='!gh auth git-credential' push -u origin feature/of-94-claude-md-coexistence` failed with `remote: Permission to rafabarranco/kenovis.git denied to barve-leyias. ... 403`, even though `gh auth status` had shown `rafabarranco` active earlier the same session. Distinct from OF-65 (SSH remote, no key — *reachability*) and OF-86 (`gh pr merge`'s local housekeeping using the wrong remote — *which remote*): this is about *which authenticated identity* is active, a different failure class the existing three `policies/git.md` fixes (OF-65/OF-86/OF-97) never covered. | This session, 2026-08-19, hit live twice (both PR pushes this session) | **Fixed** (2026-08-19) — `policies/git.md` → "Rebasing" names the wrong-`gh`-account push failure (403) and its fix (`gh auth switch --hostname github.com --user <account>`). → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-99 | **DECISION-036's operative rule — "the lowest-id row still carrying `Open`" — and its own justification for that rule ("by construction, the row that has gone longest since it was last written to") have quietly diverged, and at least four rounds on 2026-08-19 refined a different row than the rule names without amending or even citing the divergence.** The decision's equivalence only holds if a refined-but-not-closed row leaves the "oldest" position once touched; nothing in the mechanism does that, so a row that gets refined without closing (OF-02, refined 2026-08-18 and again this round, still `Open`) remains the numerically lowest surviving id forever, while every higher-id row — however long *it* has actually sat untouched — can never be selected under a literal reading. Checked directly against the rows themselves, not asserted: OF-03, OF-04 and OF-19 were each last touched 2026-08-14, five days stale, strictly longer than OF-71/OF-74/OF-75/OF-85/OF-87, which is what a same-day cluster of rounds refined instead of the literal lowest id (OF-02) on 2026-08-19 — each citing "lowest-id `Open` row untouched by any prior refinement pass" as its own justification, a criterion DECISION-036's text never states. That criterion also runs out: once every `Open` row has one refinement on record (nearly true already), it gives no answer at all. | This round, 2026-08-19, found while selecting this round's own Refine target and choosing between the literal rule and the substitute practice | **Fixed** (2026-08-19, via `/next`) — **DECISION-054**: DECISION-036's Refine target is least-recently-touched (by its own last-refined date), not the literal lowest surviving id. → `PRODUCT/ROADMAP-ARCHIVE.md` |

| OF-100 | **`check_item_findings.py` verifies the forward direction only — that a closed item declares the `OF-N` ids it left behind — never the reverse: that an `OF-N` row citing a `Scheduled — item N` disposition gets updated to `Fixed` once that item actually closes.** Confirmed by direct count, not estimate: this exact drift has now recurred **five times** in six days — OF-69 sat `**Open**` through two rounds after two separate commit messages already claimed it closed; OF-01 and OF-92 each sat `Scheduled` a full round after their citing items (38, 44) shipped, corrected together 2026-08-19; OF-42 and OF-96 sat `Scheduled — item 43/45` after items 43 and 45 both closed and, in item 45's case, named the row by id in its own one-line closing narrative — none caught mechanically, all found by a human eye re-reading the queue for an unrelated reason. | Found 2026-08-19, auditing the `Open Findings` queue against a routine roadmap-status question, after correcting OF-42/OF-96 | **Open.** Not fixed here — designing the check is real work, not a clause: `check_item_findings.py`'s own docstring already grapples with the parallel problem (detecting a citation in free prose has no reliable pattern; requiring a structured declaration does), and the citation phrasing on the *item* side is not uniform enough yet to regex safely (`"Closes OF-01"`, `"closing OF-96"`, and no literal phrase at all — OF-42's own item 43 never named it, only this document's separate narrative did). Priority: Pain low-medium (nothing breaks; a future round wastes time re-investigating work that is already done, exactly as this pass did), Frequency: every closed item that cites a queue row, which is most of them — five confirmed instances in six days and counting, Cost: medium (needs a design decision on which citation form is authoritative before any guard can regex it, the same shape OF-21/OF-22/OF-27's bundle already carries). Role: cto (destination is `.github/scripts/`, this repository's own CI, mirroring `check_item_findings.py`'s existing local-net scope — not shipped to Installations). **Fixed** (2026-08-21, via `/next`) — **DECISION-055**: the design decision this row asked for is settled in favor of trusting the queue row's own already-structured `**Scheduled — item N**` citation rather than requiring a new declaration on the item side (rejected: every one of the five confirmed instances was already-closed prose a going-forward-only item-side line would not have caught). `check_item_findings.py` gains a third check: every queue row whose own last bolded disposition word is `Scheduled` names an item, and that item cannot already be `DONE`. Verified against a synthetic instance before this row closed: replacing OF-12's own `**Scheduled — item 33.**` with a reference to an already-closed item (`item 1`) made the guard fail with exactly this row's own drift named (`OF-12: disposition still reads Scheduled — item 1, but item 1 is closed (DONE)...`); reverted, guard passes clean (`1 queue row(s) currently Scheduled, cross-checked against 42 closed item(s)`, exit 0) — the one currently-live Scheduled row (OF-12, item 33) correctly does not fire, since item 33 has not closed. |
| OF-101 | **`company-os/DECISIONS.md` carried a four-line orphaned template placeholder — "# Future Decisions... Examples: Technology choices, Pricing changes..." — sitting inline between DECISION-026's body and DECISION-027's, not at the file's actual end.** Found opening DECISION-026 and DECISION-036's bodies to cite them accurately for this round's own OF-21/OF-22/OF-27 work (`SYSTEM.md` → "Context Loading Rules" requires opening a body before citing it). Nothing referenced this section (`grep -rn "Future Decisions" company-os/ framework/` → only the one heading itself), and it was never a real decision — generic instructional filler, unlike the file's true closing "# Final Principle" section which sits only at the actual end. Likely stranded when DECISION-027 and everything after it was appended following DECISION-026 rather than following this placeholder, which nothing then moved. | Found 2026-08-19, this round, while opening decision bodies to cite them accurately for the OF-21/OF-22/OF-27 `/architect` round | **Fixed** (2026-08-19, same round) — deleted an orphaned four-line "Future Decisions" template placeholder stranded mid-file in `DECISIONS.md`; citations swept, 0 remaining references. → `PRODUCT/ROADMAP-ARCHIVE.md` |
Findings closed by disposition and kept here so they are not re-proposed: a CI guard asserting this repository's Product layer stays free of `[ANSWER:` markers (**Rejected**, item 6 — it would pass trivially and forever, and the real risk is a question written with a plain bracket, which no pattern separates from a legitimate survivor); a CI guard detecting an inherited answer in an authored Product-layer document (**Rejected**, item 8 — an answer phrased in generic words is invisible to any check, and claiming otherwise repeats Learning-015's failure in a new place).
---
Phase 1 — MVP

Objective

Prove that a real external software team can install Kenovis via CLI, complete /init-project for their own product, and ship at least one real feature through the /feature workflow.

Target users

Software developers and small development teams (COMPANY_OS.md → Initial Market Strategy).
---
Engineering Validation (2026-08-05, via /next)

Smoke-tested end to end against a scratch external-like repository (own README.md, own `src/`): `npx kenovis@0.1.0 init` correctly left the existing README.md and code untouched, wrote `.kenovis/` + `CLAUDE.md` stub, and correctly detected the repository as brownfield (cited `src/` as evidence, suggested `/adopt-project`). `npx kenovis sync` (no `--source`, the real customer path — pulls the published bundle) produced an empty diff on an unchanged version, confirming idempotent mirror-replace with no spurious noise. See AI/memory/learnings.md Learning-004 for a related `--source` footgun found while testing (not customer-facing — only affects the local-dev `--source` escape hatch, not default usage).

This validates the CLI Core Modules below work as documented. Full Success Criteria closed below.
---
Real External Validation (2026-08-06)

An external team (identity not disclosed) completed `/init-project` unassisted and shipped a real feature through the `/feature` workflow the same day, with no help from Kenovis. Nothing noteworthy to record as a learning this round — no framework gap surfaced. This satisfies Phase 0's own Success Criteria above and Phase 1's Objective/Success Criteria below: the first real (non-smoke-test) end-to-end validation of the install → init-project → feature workflow chain.
---
MVP Core Modules

- CLI install command — scaffolds the Framework layer into a target repository.
- CLI sync/update command — pulls a newer Framework Release, Framework-layer files only.
- The Software Development Vertical's Agent Roster — already exists (AI/agents/: CTO, Product Manager, Designer, Frontend, Backend, Security, Database, Reviewer).

See PRODUCT/FEATURES.md for the detailed spec format once each module is speced as a FEATURE-NNN.
---
MVP Non Goals

Do NOT build:

- Hosted dashboard or web UI — no backend exists in v1 (DECISION-013).
- Billing or payment processing — open-core tier boundary is documentation-only until Phase 2.
- Multi-tenant accounts or authentication — there is no shared backend to authenticate against.
- Additional Verticals (Legal, Accounting) — Phase 3, not before Phase 1 validates the model on Software Development.
---
MVP Success Metrics

The MVP is successful when:

Usage

No installations target is set, and none will be — DECISION-045. Adoption count does not gate the MVP or `1.0.0`; the gate is the roadmap itself reaching empty, with item 33 (external validation) run last.

Engagement

Installations run at least one framework workflow (/feature, /bug, /review) after initialization.

Retention

Installations still sync new Framework Releases after 30/90 days.

Value

Customers report the framework caught something — a missed policy, an undocumented decision — that a plain AI coding session would have missed.
---
Phase 2 — Product Market Fit

Objective

Become indispensable for early-adopter software development teams.
---
New Capabilities

Paid open-core tier: additional specialized agents, priority support. Lightweight, explicitly opt-in feedback/telemetry loop (no default data collection — see ENGINEERING/SECURITY.md). Richer CLI update ergonomics (diff preview before sync, conflict detection against RULE-INST-01, an active version-check — e.g. `kenovis --version`/`kenovis init`/`sync` querying the npm registry to tell an installed customer a newer Framework Release exists, allowed under ENGINEERING/ARCHITECTURE.md's Hard Rules since it only touches the npm registry, not a Kenovis-operated server).

Flagged 2026-08-06, via /analyze on the v0.1.0/v0.2.0 → v0.3.0 upgrade path: deliberately not Phase 0 work. Priority formula score is low today (Pain/Frequency/Business Impact unvalidated against ~1 external team so far; Cost is real — first network dependency in the CLI's core logic, new failure modes to handle offline/registry-down/rate-limits). Phase 0 item 6's `cli/README.md` "Upgrading" section covers the same discovery gap at near-zero cost in the meantime. Revisit once Phase 2 has real Frequency/Pain data to size this against.

Prerequisite (flagged 2026-08-06, via /analyze on `.kenovis/` distribution packaging): before this tier ships, run /architect for an ADR on the actual gating mechanism for premium agent content — a real check (backend + license key) at the moment it's built, not cosmetic obfuscation of the free base tier. The base tier's plain, human/AI-readable markdown distribution is an intentional commitment (DECISION-010 tool-agnosticism, DECISION-013 open-core) and should not be reversed to simulate protection it doesn't provide. Blocks "additional specialized agents" until resolved.
---
Phase 2 Success Metrics

Target:

Not yet set — first real number should come from Phase 1 usage data, not invented here.

Metrics:

Retained installations, framework workflow invocations per active installation, paid-tier conversion rate, init-project completion rate.
---
Phase 3 — Expansion

Objective

Expand beyond the initial customer segment.
---
New Verticals

Legal/abogacía and Accounting/gestoría — see COMPANY_OS.md → Long-Term Market Vision. Each requires its own Agent Roster and glossary, authored from that vertical's real domain per DOMAIN/BUSINESS_RULES.md RULE-VERT-01, not copied from the Software Development roster.
---
Product Adaptation

The platform should support:

Different terminology.

Different workflows.

Different organization structures.

Without rewriting the core system.
---
Phase 4 — Platform

Objective

Become the operational infrastructure for the category.
---
Possible capabilities:

A marketplace of Vertical Agent Rosters (community and Kenovis-authored). An optional hosted layer for teams that do want a dashboard (only once justified by real demand, not built preemptively — see COMPANY_OS.md → What The Company Will NOT Become). Integrations with existing developer tooling (GitHub, Linear, Slack).
---
Features Backlog Philosophy

Features are prioritized using:

Customer Pain

How painful is the problem?

Frequency

How often does it happen?

Business Impact

Does it improve retention or revenue?

Implementation Cost

How complex is it?

Priority formula:

Priority =
(Customer Pain × Frequency × Business Impact)
/
Implementation Cost

---
Features That Require Strong Validation

Before building:

- Social features.
- Chat.
- Marketplaces.
- Complex accounting.
- Public communities.
- AI features.
---
Product Metrics

The company tracks:

Acquisition

How organizations discover the product.

Activation

How quickly organizations reach first value.

Retention

Whether organizations continue using the product.

Revenue

Subscription and transaction revenue.

Engagement

Operational usage.
---
North Star Metric

Active Installations — repositories with the Kenovis AI-OS installed that ran at least one framework workflow (/feature, /bug, /review, /next) in the last 30 days.

Supporting metrics:

init-project completion rate (installs that reach a real, non-placeholder Product layer), Framework Release sync rate, workflow invocations per active Installation, paid open-core tier conversion rate.
---
Final Product Principle

The roadmap is not a list of features.

It is a sequence of validated problems.

Build the smallest solution that creates undeniable value.
