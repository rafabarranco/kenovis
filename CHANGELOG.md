# Changelog

All notable changes to the Kenovis AI-OS **framework layer** are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). This log tracks the reusable framework (`.kenovis/AI/`, `CLAUDE.md`, `README.md`, `cli/README.md`) — not product-layer content, which is scaffolding meant to be rewritten per product via [.kenovis/AI/commands/init-project.md](.kenovis/AI/commands/init-project.md).

## [Unreleased]

### Fixed

- **A round that closed no roadmap item could find something and never be checked on declaring it — now the one artifact every round writes has the same required line closed items already do.** `framework/commands/next.md` (2.15 → 2.16) Step 13, `framework/policies/documentation.md` (3.13 → 3.14), `.github/scripts/check_item_findings.py`, `DECISIONS.md` DECISION-051, `PRODUCT/ROADMAP.md` OF-21/OF-61. `Findings this item did not fix:` only ever checked closed items, so a finding born as evidence inside a decision body, dropped in conversation, or raised inside an item still open was invisible to it — and the same check went permanently inert once a fully-archived roadmap left it with no closed items to check at all. Every round now writes `Findings this round did not fix:` alongside its own `Next:` pointer, checked the same way, and that population can never go structurally empty the way the item-scoped one can. Found and fixed in the same change: the guard's own id-matching regexes assumed two-digit ids and silently missed `OF-100`.
- **A rejected item or row could still be cited elsewhere as live, planned work, with nothing to catch it.** `framework/policies/documentation.md`, `.github/scripts/check_rejection_citations.py` (new), `DECISIONS.md` DECISION-052, `PRODUCT/ROADMAP.md` OF-22. A stale citation can't be detected mechanically without classifying prose — but the round doing the rejecting already knows the id, right now. Rejecting now requires a `Citations swept:` line naming the sweep and its result, checked on every rejection still inline.
- **This repository's own root `CLAUDE.md` and the stub every Installation actually gets were both restating the same finding-routing table by hand, with nothing keeping the two in sync.** `company-os/ENGINEERING/ARCHITECTURE.md` (1.7 → 1.8), `.github/scripts/check_claude_stub_sync.py` (new), `DECISIONS.md` DECISION-053, `PRODUCT/ROADMAP.md` OF-27. Root `CLAUDE.md` is exempt from the CLI's stub generation (DECISION-020) because it carries real repo-specific prose — but that also means a framework-level rule written there silently reaches no customer unless something else independently carries it, and the one rule that already needed to be carried (DECISION-027's routing table) was being kept in sync by hand. A recorded convention (root `CLAUDE.md` restates, never originates, a rule meant for every Installation) plus a structural guard on that one table close the gap without generating the file.

- **A `git push` or PR creation that fails with a 403 "Permission denied" against the wrong `gh` account now has a named diagnosis and fix.** `framework/policies/git.md` (2.4 → 2.5) → "Rebasing", `PRODUCT/ROADMAP.md` OF-98. Multiple `gh` accounts logged into the same machine mean the "active" one can silently be the wrong one for this repository's remote — reachable, but refused by GitHub itself. The section now names the symptom, `gh auth status` as the diagnostic, and `gh auth switch --hostname github.com --user <account>` as the fix, flagged explicitly as a global CLI state change rather than a per-command override, since a shared machine can have this flip underneath a different concurrent session.
- **`sync` now tells you when a release may have changed your Product-layer templates, instead of staying silent forever.** `cli/src/cli/bin.ts`, `DECISIONS.md` DECISION-048, `PRODUCT/ROADMAP.md` OF-78. `sync` never writes `company-os/` (RULE-INST-01), so an Installation set up before a template changed just kept its old, correct-at-the-time answer with nothing telling it a newer one exists — two real instances found in one round alone. `sync` now prints one line whenever the Framework Release actually changed, pointing at `framework/templates/product-layer/` for you to compare against your own `company-os/`. It never claims to know exactly what changed — that would need provenance tracking this release doesn't add — it just makes sure you know to look.
- **`PRODUCT/OPERATING_MODEL.md`'s Conformance table now carries a date on every row, so staleness is visible instead of silent.** `framework/templates/product-layer/PRODUCT/OPERATING_MODEL.md` (1.1 → 1.2), `framework/commands/next.md` (2.13 → 2.14) Step 13, `DECISIONS.md` DECISION-049, `PRODUCT/ROADMAP.md` OF-79. A row only got re-checked when a round happened to serve that section — measured: 1 of 17 rows the most recent round, 4 of 17 the one before. A full re-verification every round would cost seventeen commands paid forever by every Installation, so instead every row now carries an `As of` date, updated whenever a round actually checks it (state changed or not) and left alone otherwise — you can now tell a current row from an eleven-round-old one at a glance.
- **A finding tagged with an owning `Role` now has a mechanism that can actually route a round toward it, not just a label.** `framework/commands/next.md` (2.13 → 2.14) Step 3, `DECISIONS.md` DECISION-050, `PRODUCT/ROADMAP.md` OF-90. `PRODUCT/OPERATING_MODEL.md` §12 requires the role that owns a finding to process it; verified that no agent file reads its own tag at all. A structural per-role scheduler isn't architecturally available here (no backend, nothing runs between sessions), so `/next` Step 3's ranking now weighs an `Open` row's `Role` against whichever roles a round is already activating for its own objective — opportunistic, not a guarantee, but a real mechanism where there was none.
- **A pre-existing root `CLAUDE.md` used to abort the entire `init`/`add`/`sync` run — now it's preserved and Kenovis's block is appended below it.** `cli/src/domain/installation.ts` (`resolveClaudeMdWrite`), `cli/src/application/commands/init.ts`, `sync.ts`, `DECISIONS.md` DECISION-047, `PRODUCT/ROADMAP.md` OF-94. Before this, any target with a `CLAUDE.md` this CLI didn't already recognize as its own — the realistic case for the developers this product targets, who are "already fluent in agentic tooling" and likely to already have one — got a hard refusal before `.kenovis/` was even written, with `--force` as the only way through (which discarded the file's content entirely). Now your content is kept exactly as it was and the Kenovis block is appended below it, joined by a marker; the CLI still tells you what happened (`CLAUDE.md content preserved — the Kenovis block was appended below it`). `--force` still means what it always meant: discard and overwrite entirely. The one case that still refuses: you hand-edited *inside* the Kenovis-managed block since the last sync — that's the one edit only you can resolve.
- **Setup never asked the one question DECISION-034 depends on: how many roadmap items should a session complete before stopping.** `framework/commands/init-project.md` (1.12 → 1.13), `framework/commands/adopt-project.md` (1.12 → 1.13), `PRODUCT/ROADMAP.md` OF-83. `PRODUCT/OPERATING_MODEL.md`'s template already carried a "5. Working cadence" section asking exactly this, but Step 1/Step 2 of the two setup commands only ever asked the template's first four sections — so a compliant setup left section 5 as an unanswered `[ANSWER: ...]`, and an Installation that wanted a cadence other than the framework's one-item-per-round default had nowhere to say so. Both setup commands now ask a fifth question, worded from the template's own section 5 text.
- **A round that finds a predecessor's uncommitted Product-layer work had no stated recovery procedure, only detection.** `framework/commands/bootstrap.md` (2.9 → 2.10) Step 5, `PRODUCT/ROADMAP.md` OF-95. `next.md`'s Completion Criteria requires a round's own output to reach a branch before its thread ends, and `bootstrap.md` Step 5 already told a new round to inspect git status for unfinished tasks and potential conflicts — but neither named what to *do* once uncommitted Product-layer content from an interrupted predecessor round is actually found. Step 5 now names the recovery: fold the change into the current round's own branch and commit it as its own distinct, separately-attributed change, before starting the round's own objective.
- **Three promoted learnings had a real rule shipped in their name, and the rule never said whose reasoning was behind it.** `framework/policies/documentation.md` (3.12 → 3.13), `framework/commands/next.md` (2.12 → 2.13), `framework/SYSTEM.md` (1.13 → 1.14), `PRODUCT/ROADMAP.md` OF-84. `AI/memory/learnings.md`'s own promotion rule requires a clean close to cite the learning id at the point its rule appears, so the reasoning behind a clause is one hop away — three promotions shipped the rule and skipped the citation. Each of the three now names the learning that produced it, at the exact line the rule appears.
- **A harness permission classifier can refuse a post-merge git sync outright, and nothing named that as its own failure case.** `framework/policies/git.md` (2.3 → 2.4) → "Rebasing", `PRODUCT/ROADMAP.md` OF-97. The section's existing fixes (OF-65/OF-86/OF-88) all assume git itself reports the failure — an exit code, a credential prompt, a stale-lease rejection. A classifier block looks nothing like that: the command never runs, no git error surfaces, and the checkout is left exactly as far behind as it was, uncorrected. The section now names the case directly: state it plainly, do not retry silently, do not treat the checkout as leveled.
- **README.md never said what happens to a lesson learned while operating an Installation — a founder session spent real time worried the answer was "nothing".** `README.md` → "Where learnings go", `PRODUCT/ROADMAP.md` OF-91. The tree already answered no: a reusable learning is promoted into the Framework layer as a standing rule and reaches every Installation the next `kenovis sync`, the same way a framework-level `DECISIONS.md` entry does — but nothing owner-facing said so (`grep -icn "promot" README.md cli/README.md` → `0`, `0`). One new section states the loop in plain terms: product knowledge stays in `company-os/` and is never sent anywhere; team craft is promoted out of a learning entry into the Framework layer and ships with the next sync. No mechanism change — the loop already existed and now the document says so.
- **A blocked round's stop-and-reason record had a named file and no named place inside it — "somewhere in a 150 KB document" isn't findable.** `framework/commands/next.md` (2.14 → 2.15) → "When The Next Objective Is Not Yours To Execute", `PRODUCT/ROADMAP.md` OF-71. `PRODUCT/ROADMAP.md` has three places a round writes — an item's progress narrative, the `Open Findings` queue, and the `Next:` pointer — and a stop produces none of the first two, since it advances no item and finds no new finding. The step now names the `Next:` pointer (written under Step 13) as the destination directly, ruling out the other two.
- **A session's very first `git fetch` — bootstrap's own currency check — had no stated fallback for the exact failure this repository hits on every session, even though the fix already existed two sections away.** `framework/commands/bootstrap.md` (2.10 → 2.11) Step 5, `PRODUCT/ROADMAP.md` OF-65. `policies/git.md` → "Rebasing" already documented the HTTPS-credential-helper detour for a `128`/`Permission denied (publickey)` SSH failure, but only next to the post-merge relevel case. Step 5's own bare `git fetch origin` — the highest-frequency of the four bare instances, since every round hits it first — had no fallback beside it, so a session landing there read the repository as broken rather than as a missing credential. Step 5 now names the same fallback directly.

## [0.15.0] - 2026-08-18

Aligned with the `kenovis` npm package's own version, same as [0.2.0] through [0.14.0]. Minor rather than patch, decided at cut time per the standing instruction in `PRODUCT/ROADMAP.md` Phase 1 item 2. The first release since [0.2.0] to carry a CLI code change — `kenovis context` below — alongside the framework-only changes recent releases shipped alone.

`1.0.0` remains out of scope for this release. OF-11 (`PRODUCT/ROADMAP.md`) is still open — no installations target is set — and DECISION-041 already settles that `1.0.0` ships only after the roadmap's external-facing validation work (item 33) runs, ranked last, independent of that number.

**One breaking change for an existing Installation.** DECISION-043 re-roots the Product layer under `company-os/` — see "Changed" below for the exact migration commands. `sync` never touches a Product-layer file you already authored (RULE-INST-01), so this does not happen automatically; run the migration before your next sync.

### Fixed

- **A fully compliant `/init-project`/`/adopt-project` run could still fail its own Step 11/12 verify check.** `framework/templates/product-layer/PRODUCT/OPERATING_MODEL.md`, `PRODUCT/ROADMAP.md` OF-93. The Conformance table's row example used the `[ANSWER:]` marker reserved for founder questions on all three of its cells, but setup's own Step 2/3 explicitly tell the session to leave that one row unfilled — so the row's brackets always survived Step 11's "zero `[ANSWER:]` matches" check, on every Installation, unconditionally. Confirmed twice by independent fresh-subagent behavioral runs before the fix, and by simulation against the artifact after it: a Step-2-compliant setup now leaves zero `[ANSWER:]` matches, not three. The row was never a question for anyone — it is populated later, by the first closing `/next` round's own Step 13 — so the fix drops the marker rather than adding an exception to the check: the State cell reads the literal `unmeasured`, the other two use plain parenthetical example text.

### Changed

- **Breaking (repository layout) — the Product layer re-roots under `company-os/`.** `DECISIONS.md` DECISION-043, `PRODUCT/ROADMAP.md` item 44. The seven Product-layer elements — `COMPANY_OS.md`, `DECISIONS.md`, `PRODUCT/`, `DOMAIN/`, `ENGINEERING/`, `AUTOMATIONS/`, `AI/memory/` — move from generic repository-root names into one directory, `company-os/`, so a brownfield customer's own file of the same name is never an overwrite-or-move-aside candidate for the Collision Guard (DECISION-019). Re-root only, no internal renames. `/init-project` and `/adopt-project` now author into `company-os/`; the CLI's `NON_EVIDENCE_ENTRIES` gains `company-os` so a re-run never mistakes an authored Product layer for customer implementation evidence.

  **If you have an existing Installation** (the one known instance is `kenovis@0.3.0`): `sync` never touches your Product layer (RULE-INST-01), so this does not migrate automatically. Before your next sync, from your repository root:
  ```
  mkdir company-os
  git mv COMPANY_OS.md DECISIONS.md company-os/
  git mv PRODUCT DOMAIN ENGINEERING AUTOMATIONS company-os/
  mkdir -p company-os/AI
  git mv AI/memory company-os/AI/memory
  ```
  Then sync to the Framework Release that carries this change — it authors `CLAUDE.md` and `.kenovis/AI/` referencing the new `company-os/` paths.

### Added

- **`kenovis init`/`add` now generate native Claude Code slash commands (`.claude/commands/`), and `--tools` lets you choose which AI tools get scaffolding — not a hardcoded list this framework has to keep updating as new models ship.** `DECISIONS.md` DECISION-046, `PRODUCT/ROADMAP.md` OF-96/item 45, `cli/src/application/toolAdapters.ts`. Before this, your Installation got a root `CLAUDE.md` stub and nothing else — even Claude Code, this framework's own named primary tool, had no `/next`, `/feature`, `/bug` etc. as real slash commands; you had to know to point an agent at `.kenovis/AI/commands/<name>.md` by hand. `kenovis init`/`add --tools claude,cursor,...` (default `claude`, unchanged from today's behavior) now writes one `.claude/commands/<name>.md` per framework command, generated from `framework/tool-adapters/claude/`'s own template — the CLI never hardcodes a tool's identity or a command's name, so a new AI tool is future content under `framework/tool-adapters/`, shipped to your existing Installation via `sync`, not a CLI release you have to upgrade to. Your own pre-existing file at the same path is never overwritten: each wrapper carries a marker only Kenovis-generated files have, and anything else is left alone and reported. Your selection is remembered in `.kenovis/.tools`, so `sync` keeps it current without `--tools` being passed again.

- **`kenovis context "<query>"` — a native retrieval command, so your Installation can search its own accumulated Product layer instead of grepping or guessing which file to open.** `cli/src/application/commands/context.ts`, `PRODUCT/ROADMAP.md` item 23. This repository's own sessions have always had `graphify` for exactly this; a customer Installation has never had an equivalent, and bounding what a session reads once it already knows which citation to open (items 18-22) never solved "which citation is relevant" in the first place — a different capability. Filesystem-only, no network, no backend, no new dependency, inside `ENGINEERING/ARCHITECTURE.md`'s Hard Rules and DECISION-013: chunks `company-os/` (opt-in `--include-framework` also covers `.kenovis/AI/`) into paragraphs and findings-table rows, ranks by plain keyword overlap, and prints `path:startLine-endLine` citations with a short excerpt — it points at what to read, it never prints file contents itself. v1 is deliberately lexical, not semantic; a v2 with phrase-matching or configurable ranking is a candidate only if this proves insufficient in practice.

- **Citing a closed roadmap item, finding or learning now requires opening the archive it points to, the same discipline this framework already applied to a decision body.** `.kenovis/AI/SYSTEM.md` (1.11 → 1.12) → "Context Loading Rules", `.kenovis/AI/policies/documentation.md` (3.11 → 3.12). Your three archives held 450 KB combined against 285 KB active, and nothing anywhere instructed a round to open one — `grep -l "ARCHIVE"` across every command and workflow returned zero matches. A one-line pointer to a closed row was a citation nobody followed, in a framework whose stated purpose is not rediscovering what it already knows. No new command, no retrieval tooling — the fix reuses the exact "opened on demand, before it is cited" rule already governing `DECISIONS.md`, extended to cover the other three.

- **`/next` now observes the tree with a required, bounded step, instead of relying only on whatever a round's own objective happens to touch.** `.kenovis/AI/commands/next.md` (2.11 → 2.12) Step 3 → "Observe", `DECISIONS.md` DECISION-038. Your operating model states that Kenovis observes the product continuously; every command in this framework runs only when you decide to invoke it, including the one built specifically to audit its own surface, which had a perfect record of zero invocations across seven opportunities. "Continuous" needs a scheduler this architecture cannot carry without abandoning tool-agnosticism or the no-backend commitment — the same two reasons a hook was already rejected once. What is achievable is a fixed, short, unconditional checklist run inside the command that already executes on your product's own cadence: on its first instance, every governed document's size and every exemption still citing a live roadmap item, checked every round regardless of what that round was already going to do.

### Changed

- **Every round now refines the oldest unresolved finding, not only its own chosen objective.** `.kenovis/AI/policies/documentation.md` (3.10 → 3.11), `.kenovis/AI/commands/next.md` (2.10 → 2.11) Step 3, `DECISIONS.md` DECISION-036. Your operating model states the routing chain as discovery → analyse → classify → **refine** → plan → roadmap; what existed was discovery → a row with a disposition, and an `Open` row satisfied every rule on the day it was written and forever after, with nothing that ever revisited it. Every round now also refines the lowest-id `Open` row in the findings queue — the one that has waited longest, since ids are assigned in discovery order — re-checking its priority terms against the current tree, promoting it, or re-dispositioning it. No new field or counter: the queue's own id order already carries the age signal, and your framework forbids adding an eleventh CI guard while the tenth is still being placed. Demonstrated live in the round that shipped it, which also closed two further findings it surfaced along the way.

### Changed

- **An `Open` finding now names the role that owns analysing it.** `.kenovis/AI/policies/documentation.md` (3.9 → 3.10). Routing by destination file — `PRODUCT/ROADMAP.md`, `DECISIONS.md`, and the rest — said where a disposition gets recorded; it said nothing about who decides it, so a security finding and a typo got identical treatment from whichever role happened to be active. The requirement lives once, in this rule, rather than copied into every agent file — which role actually owns which kind of finding is a fact about your own Agent Roster, and stays out of the framework layer entirely so it never ships one product's answer to another.

- **The findings rule now fires when you notice something, not before a checkpoint that never arrives.** `CLAUDE.md` (2.6 → 2.7), `.kenovis/AI/SYSTEM.md` (1.10 → 1.11), `.kenovis/AI/policies/documentation.md` (3.8 → 3.9). All three said, in different words, "write it down before the session ends" — and nothing tells an agent a session is ending; a thread just stops. So the rule was loaded everywhere and applied only by whichever session happened to remember on its own. It now says what actually happens: write it when you find it. The one real exception — setup, before your Product layer exists — gets its own fix below.

- **A fresh Installation's `CLAUDE.md` stub stopped telling you to route findings to files that don't exist yet.** `cli/src/domain/installation.ts` → `claudeStubContent`. Between `kenovis init`/`kenovis add` and the end of setup, `PRODUCT/ROADMAP.md`, `DECISIONS.md` and three more destinations genuinely do not exist — your setup command creates them. The pending stub listed them anyway, unconditionally, in the one file Claude Code autoloads. It now says so and points at the setup command instead.

- **`init-project.md` and `adopt-project.md` each gained a closing step that disposes of what setup itself found.** `.kenovis/AI/commands/init-project.md` (1.11 → 1.12) Step 13, `.kenovis/AI/commands/adopt-project.md` (1.11 → 1.12) Step 14 — added after the existing final step rather than renumbering it, so nothing that already cites that step by number breaks. `/adopt-project` reads your entire existing codebase once, end to end; that is the session most likely to surface real technical debt and security gaps with nowhere to go until now.

- **The constitution stops denying the autonomy your own operating model gives Kenovis.** `.kenovis/AI/SYSTEM.md` (1.9 → 1.10), `CLAUDE.md` (2.5 → 2.6). Both files said, unconditionally, *"[The AI/You are] not an autonomous decision maker"* — loaded before every task, in every Installation. Your `PRODUCT/OPERATING_MODEL.md` splits ownership rather than denying autonomy: what stays with you (direction, strategy, business decisions) and what it assigns to the AI-OS (engineering awareness, technical planning, architectural consistency). An unscoped denial covered the half the operating model explicitly gives Kenovis, contradicting the document ranked above the constitution itself. Both files now name `PRODUCT/OPERATING_MODEL.md` as where the split lives, generically — not this repository's own section numbers, which are this founder's content and would be wrong for anyone else's.

### Removed

- **`.kenovis/AI/workflows/roadmap.md`, a second definition of `/next`.** It claimed the same trigger as `.kenovis/AI/commands/next.md`, had zero inbound references anywhere in the framework, and was four rounds behind — no findings queue, no `Next` pointer, no disposition rule, no operating-model declaration, no empty-roadmap handling. An Installation that somehow reached it instead of the command would run a process this framework no longer guarantees. Deleted rather than converged: bringing it to parity would mean re-deriving `commands/next.md` inside a second file, which this repository's own "Single Source of Truth" rule already forbids.

### Changed

- **A reach-measuring command now cites a rule's exact section title, not an improvised keyword.** `.kenovis/AI/policies/documentation.md` (3.7 → 3.8) → "A Claim Is Read Back Off The Artifact". `grep -ci "Open Findings\|disposition\|Findings this"` — one framework round's proxy for "does this file carry the findings rule" — scored three files zero when all three cited the rule correctly, by its exact title, in a form the keyword didn't anticipate. The wrong count had already set a ranking before the error was found. A citation of the section's exact title can't drift the same way a keyword can: it either names the rule or it doesn't.

- **`/next` Step 11 now validates a document-artifact round the way it actually gets validated.** `.kenovis/AI/commands/next.md` (2.9 → 2.10). The step said *"Run: Tests. Type checks. Linting. Build process."* — four checks that pass vacuously on every round whose artifact is markdown under `.kenovis/AI/`, which is most of them, since that markdown is the product (DECISION-026). It now splits code-artifact validation from document-artifact validation, and names the form that actually validates the second kind: read every claim, count and grep the round makes back off the artifact with the command that produces it, per `.kenovis/AI/policies/documentation.md` → "A Claim Is Read Back Off The Artifact" — already the standard every recent round used, and nowhere in the command that requires validation.

- **The commands that produce findings now hand them on instead of describing them, and `/explain` stops instructing the opposite.** `.kenovis/AI/commands/explain.md` (2.0 → 2.1), `.kenovis/AI/workflows/framework-review.md` (1.1 → 1.2), `.kenovis/AI/commands/release.md` (2.1 → 2.2), `.kenovis/AI/workflows/bugfix.md` (2.4 → 2.5). `/explain` — one of the three commands that actually get run, and the one whose entire output is knowledge — said *"Recommend creating documentation"* and named a `docs/` directory that exists in no Installation. Recommending is prose, and the framework's own policy names prose as not a disposition, so the command most likely to be standing in front of a knowledge gap was telling your session to mention it and move on. The audit behind that fix found a worse instance in the framework's own self-review workflow: every phase of it produces findings, one phase forbade applying them and the next said the run was not worth logging, which made "classify ten defects, propose ten fixes, schedule none" a fully compliant run that left the framework exactly as it was. Releases and bug fixes had the same hole in smaller form — "Problems discovered" with no destination, and a bug's lesson routed to a memory file nothing reads to decide what happens next. Twelve of nineteen commands and workflows now carry the disposition rule by name, up from eight.

- **`/next` no longer claims a cadence your own operating model has already set.** `.kenovis/AI/commands/next.md` (2.8 → 2.9) → "Autonomous Mode", `DECISIONS.md` DECISION-034. The section said Claude may continue through multiple roadmap items, full stop — in a framework whose top-ranked document is *your* statement of how your product is developed. It now defers to `PRODUCT/OPERATING_MODEL.md`, with one item per round as the default when you have not said otherwise. The default is not a preference: a second item in the same thread spends the context window that the first item's closing steps need most, and makes two items share one disposition pass, which is exactly where the second item's findings get folded into the first item's narrative and lose their ids.

### Added

- **`/next` has a defined behaviour when your roadmap runs out, and a ninth completion criterion that the work reached a branch.** `.kenovis/AI/commands/next.md` (2.8 → 2.9) Step 3 and Completion Criteria. An empty roadmap is the stated end of the loop, and the command had no behaviour for it — so the first session to reach that state would improvise, and the likeliest improvisation is inventing an item, which consumes a thread and enters your roadmap with the same authority as real work. It now names what empty means, requires the session to say which of the two things it signifies (nothing left to improve, or nothing observing), forbids manufacturing work, and stops. The completion criterion comes from a round that opened and found the entire previous round — a decision, a template, a document move — sitting uncommitted in a working tree with no open PR. Your Product layer is this AI-OS's memory, and its durability is git, not the filesystem.

- **A path written in these documents is a backticked repository-relative path, and the policy now says so.** `.kenovis/AI/policies/documentation.md` (3.6 → 3.7). A markdown link resolves relative to the file it sits in, so a link target written repository-relative inside `PRODUCT/` points at a path with `PRODUCT/` in it twice — and a dead link in your own product documentation is silent, in the directory you edit most. The convention was universal in practice and written nowhere, which is the shape a convention has right up until someone new follows it.

### Changed

- **The AI-OS now states that it replaces a conventional development team, and no longer states the opposite.** `.kenovis/AI/SYSTEM.md` (1.6 → 1.7) → "Final Principle", `DECISIONS.md` DECISION-028. That section had said *"The objective is not to replace engineering teams"* since initialization — read in full at the start of every session, in every Installation, and directly contradicting what this product is for. It now says what it does: a product can be built, maintained, evolved and supported without a conventional human development team, and the boundary is the human who owns the product, who keeps product direction, strategy, business decisions and final approval. Your agents read this before every task, so the change is in what they aim at, not only in what the document says. Found by auditing the framework against the founder's own statement of purpose — four analyses of "has this AI-OS lost its purpose" had run without anyone reading the sentence in which its constitution denied that purpose, because a constitution is loaded for instruction and not audited against a specification.

### Added

- **Nothing stays in the thread — the routing rule now ships inside the `CLAUDE.md` stub your repository autoloads.** `cli/src/domain/installation.ts`, `.kenovis/AI/SYSTEM.md` (1.5 → 1.6) → "Nothing Stays In The Thread", `.kenovis/AI/policies/documentation.md` (3.2 → 3.3), `CLAUDE.md` (2.2 → 2.3), `DECISIONS.md` DECISION-027. Everything a session finds — an improvement, a bug, technical debt, a decision, a lesson, an open question — goes into a Product-layer file in that same session, with a table naming exactly where each kind lands. The rule already existed and was already correct; it was written in five of nineteen commands and workflows, in a checkable form in one, and your Installation's stub carried none of it — so a session that ran no command was governed by nothing, and findings died with the thread that found them. That is the one failure an AI-OS exists to prevent: a team that spots a defect and says nothing is not a team. It now arrives in the first file your agent reads, with no command to invoke and no hop to follow, and a CLI test holds it there.

- **`.github/scripts/check_item_findings.py`, wired into CI as the ninth guard.** Every closed roadmap item must state, on one line, the finding ids it queued or that it queued none — and every id it names must exist in the queue. `check_future_actions.py` enforced the half of this rule with a fixed form and its docstring recorded why it stopped: findings parked in roadmap prose are "the larger half of the same leak" with "no mechanical form". That is true of *detecting* a finding inside prose, which is why guards built on classifying prose were rejected twice here. Requiring the declaration instead does have a form — an omission is invisible, a missing required line is exact. It found a real gap on its first run against the pre-fix tree, which is now `OF-18`.

### Changed

- **An improvement now lands where the work is loaded, and `kenovis check` is rejected.** `.kenovis/AI/policies/architecture.md` (1.1 → 1.2) → "An Improvement Lands Where The Work Is Loaded", `DECISIONS.md` DECISION-026. An AI-OS is injected into your repository and operates on it every task — it is not a package you invoke at a moment of your choosing. A planned `kenovis check` subcommand would have made your enforcement depend on someone remembering to run it, which is a linter standing beside the AI-OS rather than the AI-OS working. Rules now go into the policies, commands, workflows, agents and templates your agents already load, delivered by `sync`, in force on your next task with nothing to invoke. The test each change has to pass: *does this reach the next task without anyone doing anything?* Written after nine mechanical guards accumulated in this repository's own build and zero of them reached any Installation, with each round recording that gap while widening it.

- **A closed item now declares what it left behind, and a machine holds you to it.** `.kenovis/AI/policies/documentation.md` (2.9 → 3.0) → "A Finding Is Fixed, Scheduled, Or Rejected", `.kenovis/AI/commands/next.md` (2.3 → 2.4) Step 15, `.kenovis/AI/templates/product-layer/PRODUCT/ROADMAP.md` (1.4 → 1.5). The rule that a finding gets a disposition, and the ordering that puts dispositions before the closing summary, were both already written and both already loaded — and a round still found three things, queued none, and wrote two into a session summary nothing later reads. Ordering alone was the only defence and it had failed more than once. A round that found nothing now writes "none" and is done; a round that skipped the queue has to write "none" and be wrong on the record, rather than staying silent. Your Installation gets the rule in its roadmap template on sync; the guard behind it stays in this repository, which is the intended split rather than a gap — see the entry below.

- **A changelog now archives its own released sections, and the rule says what "closed" means for one.** `.kenovis/AI/policies/documentation.md` (2.8 → 2.9) → "Closed Work Is Archived, Not Kept Inline". The archive rule named `PRODUCT/ROADMAP-ARCHIVE.md` and `AI/memory/LEARNINGS-ARCHIVE.md` and stopped there, so `CHANGELOG.md` sat over the 60 KB lifecycle threshold on an exemption pointing at an unscheduled finding rather than at work. The gap was in the rule, not the file: every released section is closed the moment it ships, so archiving on "closed" alone would leave a changelog holding nothing but `[Unreleased]` — useless to the one reader it has, someone deciding whether to upgrade. The rule now keeps `[Unreleased]` plus the two most recent released versions inline, archives the rest verbatim, and runs the trim as a step of cutting a release instead of as cleanup somebody notices late.

- **A mechanical check now has to say where its rule lives in the layer you actually load.** `.kenovis/AI/policies/testing.md` (2.2 → 2.3) → "A Guard Belongs Where The Work Is Loaded". A CI script, a pre-commit hook or a linter protects the repository it runs in and nothing else: it is not in the bundle, so no Installation receives it, and it fires at merge time, after the work and the decision are already done. It is a local net over one repository's practice, never the answer to "how is this enforced". So a round that adds one states, inside the check itself, which policy section, command step or template carries the rule — or records that the rule has no Framework-layer form and why, which is a valid outcome rather than a failure. The failure this closes is cumulative rather than acute: each round adds a guard, records the gap as a caveat, and the distance between a repository and the product it ships grows by exactly one guard per round, with every individual round defensible. Written after that happened here four times. It does not restate `architecture.md` → "An Improvement Lands Where The Work Is Loaded", which carries the rule itself; it adds only what is specific to checks.

  The same section gained a second rule found while applying the first: **a permanent satisfier turns a recurring trigger into a one-time gate.** When a threshold rule accepts a structural answer — a registered split, a declared archive, a named exception — that answer gets registered once and never re-evaluated, so the guard passes forever while the condition it was built to catch returns. Found by reading a number off a file instead of off a guard's green run: this repository's roadmap went from 45.8 KB back to 70.4 KB against a 60 KB threshold in a single day, with its size check passing the whole time because a split was registered for it.

- **Two rules your agents were expected to follow are now written where they load them.** `.kenovis/AI/policies/documentation.md` (3.0 → 3.1) → "An Instruction That Produces An Artifact Names Where It Goes, And A Template Is Never Where", and `.kenovis/AI/policies/architecture.md` (1.2 → 1.3) → "Distribution Is Part Of The Architecture". Both were live conventions with no rule behind them. An instruction that says "generate the report" and stops does not read as incomplete — it reads as finished, and the agent picks a destination itself, which lands inside the directory `sync` mirror-replaces. The sentence marking a template as a form rather than a destination was present at every existing reference site and required by nothing, so the next reference added would not have carried it. And a new Product-layer document had a rule telling `/init-project` to write its `PROJECT-SPECIFIC` marker, and nothing telling anyone creating one later.

- **"Avoid duplicated documentation" now says the case it gets missed in: the policies themselves.** `.kenovis/AI/policies/documentation.md` (3.1 → 3.2) → "Single Source of Truth". Before adding a section to a policy, read the section headings of its siblings — the set is small, and the rule being written is usually general enough to already live under a heading that does not sound like it. Two copies of a rule do not stay identical: each is edited by whoever is looking at that file, neither reader opens the other, and the policy set ends up contradicting itself with both halves reading as authoritative. Written after exactly that happened here, one round after the rule it duplicated was added.

- **A shipped policy no longer claims a check protects you that you do not have.** `.kenovis/AI/policies/documentation.md` named two of this repository's own CI scripts as what enforces its size-threshold and findings-declaration rules. Those files are not in the bundle, so every Installation was reading that a mechanism protected it when none did — the inverse of the gap DECISION-026 was written about, and worse, because saying nothing would have been more accurate. Both sites now state what actually holds: the policy is loaded on every task.

- **A fix that works can make its own guard pass for the wrong reason, and the policy now says so.** `.kenovis/AI/policies/testing.md` (2.1 → 2.2) → "A Check Is Not Verified Until It Has Been Run". The section already required running a new check against a known-good corpus and confirming it fails pre-fix. It had nothing for the case that showed up immediately: when the remedy removes the condition that puts a file into the guard's scope, the branch the fix just registered stops executing, and the guard reports a clean pass that is true of the file and silent about the change. Found in this round — a changelog trimmed under the size threshold passed its guard by being small, leaving the split registration written for it untested until it was exercised in a fixture.

- **The archive rule now covers the parts of a document that were quietly outside it, and the pass has a trigger.** `.kenovis/AI/policies/documentation.md` (3.3 → 3.4) → "Closed Work Is Archived, Not Kept Inline", `.kenovis/AI/commands/next.md` (2.4 → 2.5) Step 13. The rule said closed *entries* move to the archive, and it was written before findings queues existed — so a queue's `Fixed` and `Rejected` rows, superseded planning prose, and rejected entries were never covered, and absence of an exclusion read as inclusion. Measured here: a findings queue reached 44% of an active roadmap while two archive passes ran straight past it with every check green. All three are now named as closed work, with one exception that matters — **archiving a queue row compacts it to one line rather than removing it**, because finding ids are cited by name elsewhere and deleting the row breaks those citations silently. The rule also gained the trigger it never had: declaring a split satisfies a size rule permanently, so an archived document is free to grow back with nothing complaining. The pass now runs as a step of the round that appends, not when someone notices — measured here as a roadmap returning from 45.8 KB to 161 KB in one day.

- **`/bootstrap` now checks that your branch is level with its remote before anything reads it.** `.kenovis/AI/commands/bootstrap.md` (2.6 → 2.7) Step 5, `.kenovis/AI/policies/git.md` (2.2 → 2.3) → "Rebasing". Step 5 inspected git status, recent commits and pending changes — all of which a stale branch passes, because local state is internally consistent whether the branch is current or three merges behind. `git status` compares against the local tracking ref, which is only as fresh as your last fetch, so it will report "up to date" or a confident and wrong "ahead by N" without contacting the remote. The ordinary way to arrive stale is merging a pull request with `--delete-branch`, which returns your checkout to a base branch that by definition lacks the merge that just landed. A session that starts there reads an outdated product layer and an outdated roadmap and draws conclusions that are entirely coherent, and nothing downstream detects it. This costs one fetch; skipping it costs a round of work built on superseded context. It matters more the more you run one task per conversation, where the branch state at thread start is a per-task risk rather than a per-day one.

- **There is now one Source Of Truth Hierarchy, and your statement of purpose sits at the top of it.** `.kenovis/AI/SYSTEM.md` (1.7 → 1.8), `CLAUDE.md` (2.3 → 2.4), `.kenovis/AI/commands/bootstrap.md` (2.7 → 2.8) Step 2, `DECISIONS.md` DECISION-031. Two hierarchies shipped in the two documents every session loads, and they disagreed on the pair that collides most often: when a recorded decision contradicted a business rule, one gave the decision the win and the other gave it to the rule. The rule that resolves every other conflict was itself in conflict, and neither reader ever opened the other file because both are loaded as authoritative. `SYSTEM.md` now holds the only hierarchy — `PRODUCT/OPERATING_MODEL.md`, `COMPANY_OS.md`, `DOMAIN/`, `DECISIONS.md`, `PRODUCT/`, `ENGINEERING/`, code, AI suggestions — and `CLAUDE.md` cites it instead of restating it. **A business rule outranks a recorded decision**: a decision is an engineering choice made at a point in time, a business rule is what the product must be true of, so a decision that contradicts one is a defect in the decision. And rank 1 — the owner's statement of what the product is for — now joins the session-initialization path in all three files, rather than being reached by accident as one file inside `PRODUCT/`. **Rank 1 is conditional and says so in the hierarchy itself:** neither setup command asks you for such a document and no template ships one, so where you have not authored one, rank 2 is the top. Whether every Installation should have one is an open product decision, recorded rather than quietly assumed.

- **`/next` now starts from the pointer the previous round left, and stops instead of quietly working around you.** `.kenovis/AI/commands/next.md` (2.5 → 2.6) Step 3, `DECISIONS.md` DECISION-030. Two defects that both fire on your very next `/next`, and both cost the most if you run one task per conversation. Step 15 has been writing a "Next" recommendation into your roadmap so the following run could read it — and Step 3 named two inputs and never that one, so every round re-derived the ordering from the priority formula over the whole roadmap while the previous round's reasoning sat unread. Step 3 now reads three inputs and starts from the pointer; a round that departs from it records why, in the same round. Second: `/next` had no behaviour at all for an objective only *you* can execute — a decision, an external party, a number nobody has. The command now presents the decision with the input the item already names, records that the round reached it and stopped and on what, and stops. **Descending the priority order to find something executable is forbidden in those words**, because skipping leaves no artifact: a board whose top item needs you would silently become a board of whatever the AI could do alone, every round defensible and the ranking quietly inverted. A stopped round with a written record is the correct output, and it will look like less output than the alternative.

- **A finding is now checked against your roadmap before it is written down, and a queued finding has to be rankable.** `.kenovis/AI/policies/documentation.md` (3.4 → 3.5) → "A Finding Is Fixed, Scheduled, Or Rejected". Two rules were missing between "record every finding" and "the roadmap represents all known future work". First, nothing told an agent to *look* before writing: a finding could take a fresh id while an item already covered it, or be waved off as "already known" when it was on no document at all. It now cites the id that covers it or takes a new one — two outcomes, never zero. Second, an `Open` row now carries **Pain, Frequency and Cost** in one line, writing `unknown` where a term is unknown. An unknown term still ranks and names the first thing to find out; an omitted term drops the finding silently out of every selection that follows, which leaves it indistinguishable from one you never recorded. Promotion to a scheduled item — problem, target, validation — stays a separate act performed by the round that picks the finding up, deliberately, because demanding a full item at the moment of discovery is what pushes a late finding back into prose. The sentence the whole change turns on is in the policy in those words: **recording is not planning.** See `DECISIONS.md` DECISION-029.

- **Setup now asks you what your product is for, and every round is measured against your answer.** `.kenovis/AI/commands/init-project.md` (1.10 → 1.11) Steps 1-2, `.kenovis/AI/commands/adopt-project.md` (1.10 → 1.11) Steps 2-3, a new `.kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md`, `.kenovis/AI/SYSTEM.md` (1.8 → 1.9), `.kenovis/AI/commands/next.md` (2.6 → 2.7) Step 13, `CLAUDE.md` (2.4 → 2.5), `.kenovis/AI/commands/bootstrap.md` (2.8 → 2.9), `DECISIONS.md` DECISION-032. The previous release put "the owner's statement of purpose" at rank 1 of the hierarchy and shipped that hierarchy to a repository that had no such document and was never asked for one — a conflict-resolution rule whose top entry described a file you do not have. Setup now asks four questions (what the product is for, what the AI-OS owns, what you own, which rules are non-negotiable) and writes `PRODUCT/OPERATING_MODEL.md` before `COMPANY_OS.md`. **The AI may never answer them on your behalf**, because that document is what every later round is graded against and an operating model the AI wrote is the AI grading its own work; where you already have a written statement, it is recorded verbatim rather than summarised. That document carries a **Conformance** table — one row per section of your own statement — and `/next` Step 13 now requires a closing round to name the section its work served and update that row, or to say it served none. Everything else a round can be measured against is instrumentation, and instrumentation is checkable in a way an objective is not, so without this a round is graded on the machinery and the product drifts from its purpose with every round defensible. Two caveats stated rather than discovered: an Installation set up before this release has no such document and nothing migrates it, and the table ships as a one-row form with no instruction yet for building the rest of its rows.

- **"An instruction is reachable" now names a third way a correct rule produces nothing: it has no population.** `.kenovis/AI/policies/documentation.md` (3.5 → 3.6) → "An Instruction Is Reachable, And Its Sink Is Read". A rule phrased as "update the row", "check each entry" or "declare what this item left behind" is correct, loaded, and produces nothing where the rows, entries or items do not exist yet — and whoever writes such a rule always has the population in front of them, which is exactly why the question does not occur to them. Three instances here, all found after shipping: a routing rule naming five destinations a repository does not have until setup completes; a conformance table whose rows one repository had built by hand and no other has any instruction to build; and a check that went permanently inert, passing, when its population emptied as the intended effect of a different rule. Two questions before such a rule ships: **what creates the population, and what does an empty one mean** — "nothing to do" and "nothing is set up" are different answers and only one is a pass.

- **Your Conformance table now gets built, and two rules that were easy to skip silently now have to be answered "none".** `.kenovis/AI/commands/next.md` (2.7 → 2.8) Step 13, `.kenovis/AI/templates/product-layer/PRODUCT/OPERATING_MODEL.md` (1.0 → 1.1), `DECISIONS.md` DECISION-033. The previous entry shipped a criterion — a closing round names the section of your operating model its work served and updates that row — and shipped it with two holes. The table reaches you as a **one-row form**, deliberately, because setup is the session least equipped to measure anything; so "update the row for the section you served" resolved to nothing, and the criterion was inert in every Installation from its first round. The instruction that would have fixed it did exist, in both setup commands and in the template, and neither is a file a round ever loads — a setup command is read once, by the session running setup. **The first closing round now builds the table**, one row per section, writing `unmeasured` where it did not verify a row itself; `unmeasured` is defined as the state a row is born in, not a fourth grade, and a row that stays `unmeasured` across rounds is itself the finding. Second hole: naming the section was permitted to be "none", and a round that served none and said nothing looked identical to a round that forgot. **`Operating model section served:` and `Next:` are now required lines** in your roadmap — the round writes the section or the word `none`, and the ranked next objectives or `none` with why. Same inversion the findings rule already uses: an absence is exact, a silence is not. Deliberately left open and recorded rather than glossed: nothing re-verifies the rows a round did not touch, so a table can go stale in sixteen of seventeen rows per round, and fixing that costs a re-measurement of the whole table on every round in every Installation — a design question, not a clause.

### Added

- **An eleventh check verifies the framework layer never names a repository-local script as how a rule is enforced.** `.github/scripts/check_github_citations.py`, `.kenovis/AI/policies/documentation.md` → "The Framework Layer Never Names A Repository-Local Script As Where A Rule Holds". Ten guards were each given a Framework-layer home or recorded as having none; nothing stopped a future edit from reintroducing the specific defect that work started from — the policy itself, twice, telling every Installation that one of this repository's own `.github/` scripts enforced a rule it does not have. The new check is one literal-substring grep with no classifier, run only against this repository's own tracked source, and the new rule states the citation from both directions: a repository-local check names its framework-layer home, and the framework layer never names a repository-local script back.

### Changed

- **A supplied artifact and a discovered finding are no longer the same rule.** `.kenovis/AI/policies/documentation.md` → "A Finding Is Fixed, Scheduled, Or Rejected". A document, decision or instruction you hand over directly, complete, was being queued like anything else the AI found on its own — which meant a round could hold your own material and still ask you, by way of a queue row, to produce it. A supplied artifact is now written down immediately, in the destination its kind already names; only what the work itself discovers gets a disposition.

- **`DECISIONS.md` stays one file, and "index-bounded" is now a real, checked answer to the size threshold — not a placeholder citing work that no longer exists.** `.kenovis/AI/policies/documentation.md` → "Document Lifecycle", `.kenovis/AI/SYSTEM.md` → "Context Loading Rules", `.github/scripts/check_document_size.py`. A prior release scheduled splitting `DECISIONS.md` into one file per decision to bound what a session loads. Run through `/architect` before any file was touched, per that item's own requirement: the retrieval property it targeted — loading one decision without the rest — is already delivered by the Decision Index plus a targeted read, which `SYSTEM.md` now states as an explicit mechanism instead of leaving it implicit. The split's own named risk was real and unsolved: every existing Installation's own `DECISIONS.md` is a monolith `sync` never migrates, so shipping framework instructions describing a directory would have told every existing customer's session about a shape their own repository doesn't have. Rejected as scoped; the size guard's own docstring already named "an index that bounds what is read" as a valid answer alongside a split or an archive, and its code now implements that third category instead of falling through to an exemption.

## [0.14.0] - 2026-08-12

Aligned with the `kenovis` npm package's own version, same as [0.2.0] through [0.13.0]. Minor rather than patch, decided at cut time per the standing instruction in `PRODUCT/ROADMAP.md` Phase 1 item 2. Seventh framework-only release running with no CLI code changed, and the argument is simple: a policy is loaded by your agents on every task, a learnings file is not. Twenty-two rules moved from the second into the first, so this upgrade changes what your agents do rather than only what they are told.

`1.0.0` was considered and rejected for the seventh time, on the reason [0.9.0] onward gave and OF-11 now records: the MVP Success Metrics still have no target, and one external team is on record — against `kenovis@0.3.0`, ten releases ago. Both are now scheduled work (`PRODUCT/ROADMAP.md` items 32 and 33) instead of an argument each release repeats.

**Nothing breaks for an existing Installation**, and the same two limits from [0.13.0] still apply: `sync` never touches a Product-layer file you already authored, so your `DECISIONS.md` gains no Decision Index and your `PRODUCT/ROADMAP.md` gains no Open Findings section. Both are a copy-paste from `.kenovis/AI/templates/product-layer/`.

### Added

- **`.github/scripts/check_learning_promotions.py`, wired into CI.** Every row of the learnings file's "Promoted And Archived" index must name a policy section that exists and that cites the learning id, and every archived learning must have exactly one row — checked in both directions, so a learning archived without a pointer fails too. It found a real defect on its first run: a rule claimed to be in `coding.md` that the policy never cited. As with every guard here, it runs in this repository's CI and not in an Installation — `kenovis check` (`PRODUCT/ROADMAP.md` item 25) is what closes that gap.

### Changed

- **A learning that becomes a rule now moves into a policy, and the move has a finished state.** `.kenovis/AI/policies/documentation.md` (2.7 → 2.8), `.kenovis/AI/templates/product-layer/AI/memory/learnings.md` (1.6 → 1.7) → "What 'Move It' Means". The learnings file has always documented a Review Process that promotes a recurring lesson into the policy that should enforce it. It said where the rule goes and never what happens to the entry afterwards, so "move it" had no completion condition — and in this repository it ran zero times across 24 learnings, 18 of which had a disposition line explicitly identifying themselves as candidates. A promotion is now done when the rule is in the policy, the policy cites the learning id, the entry is archived verbatim, and one line stays behind naming where the rule went.

- **Five policies gained the rules this framework had already learned and never written down.** `coding.md` (2.2 → 2.3) → "Guards, Recorded State, And Permissive Paths"; `architecture.md` (1.0 → 1.1) → "Distribution Is Part Of The Architecture"; `testing.md` (2.0 → 2.1) → "A Check Is Not Verified Until It Has Been Run"; `documentation.md` → "A Claim Is Read Back Off The Artifact" and "An Instruction Is Reachable, And Its Sink Is Read"; `git.md` (2.1 → 2.2) → "Promotion Chains And Content Sync". Twenty-two learnings, each cited by id from the section that now carries its rule. If you have been reading this project's learnings to understand how it works, the rules are now in the policies your agents already load per task.

- **An archive over the size threshold is classified, not exempted.** `check_document_size.py` treated `PRODUCT/ROADMAP-ARCHIVE.md`'s size as an exemption citing a roadmap item, which no item could ever close. Archives now declare which active document they absorb weight for, must point at a file that exists, and still print their size on every run. The exemption mechanism keeps its original meaning: a temporary state with real work behind it.

- **An `Open` finding the AI cannot execute now names who executes it and what input they need.** `.kenovis/AI/policies/documentation.md` (2.6 → 2.7). `Open` was a valid disposition under the rule shipped in [0.13.0] and also the easiest place to hide: a finding whose executor is not the AI — a decision only the human can make, work that needs an external party, a number nobody has — sat in the queue in perfect compliance and moved for no one. Found immediately after [0.13.0] shipped, by asking whether three findings tagged "founder call" were actually planned. They were recorded, which is not the same thing.

## [0.13.0] - 2026-08-12

Aligned with the `kenovis` npm package's own version, same as [0.2.0] through [0.12.0] — see `cli/package.json` and `cli/README.md` → "Cutting a release". Minor rather than patch, decided at cut time per the standing instruction in `PRODUCT/ROADMAP.md` Phase 1 item 2. No CLI code changed in this release either, but the argument is the strongest of any framework-only release so far: the session-initialization protocol itself changed, seven commands and three workflows changed behaviour, three CI guards are new, and two Product-layer templates gained structural sections every Installation will carry — a Decision Index and an Open Findings queue.

`1.0.0` was considered and rejected again, for the reason [0.9.0] through [0.12.0] gave: it would signal maturity Phase 1 has not validated, with one external team on record — against `kenovis@0.3.0`, eight releases ago — and the MVP Success Metrics still without a target. Both of those are now recorded as findings (OF-11, OF-12) instead of being re-argued each release.

**Nothing breaks for an existing Installation.** Two things the upgrade cannot do for you, both stated because `sync` never touches a Product-layer file you already authored: your `DECISIONS.md` will not gain a Decision Index and your `PRODUCT/ROADMAP.md` will not gain an Open Findings section — the new templates carry both, but only for documents authored after upgrading. Adding them by hand is a copy-paste from `.kenovis/AI/templates/product-layer/`, and until you do, the new instructions describe sections your files do not have.

### Added


- **Governed documents now have a lifecycle, enforced in CI.** `.kenovis/AI/policies/documentation.md` (2.5 → 2.6) → "Document Lifecycle: A Size Threshold, An Archive, And An Exit". Rigor in this framework was asymmetric — cheap to add, impossible to remove — and four documents grew from ~20 KB combined to ~360 KB in eleven days, all of it on every session's reading path.

  Four governed documents, a 60 KB threshold, and three acceptable answers to crossing it: an archive, an index, or a split. Exemptions are allowed and must name the roadmap item that closes them. `.github/scripts/check_document_size.py` enforces it and prints every governed document's size on every run, so growth is visible before it is a problem. Confirmed to fail when an exemption is removed.

- **Closed roadmap items move to a verbatim archive.** `.kenovis/AI/policies/documentation.md` (2.4 → 2.5) → "Closed Work Is Archived, Not Kept Inline": a document that accumulates closed entries splits, the active file keeps one line each plus a pointer, and the archive is never on the session-initialization path. Nothing is summarised away — the archive preserves the trail, it does not compress it. The Product-layer roadmap template (1.3 → 1.4) tells an Installation to create its archive when its first item closes, not in advance.

  In this repository: 31 closed items moved to `PRODUCT/ROADMAP-ARCHIVE.md` and `PRODUCT/ROADMAP.md` went 164.7 KB → 42.2 KB. Combined with the Decision Index above, the full bootstrap path goes 374.3 KB → 133.9 KB.

- **`PRODUCT/ROADMAP.md` gains an `Open Findings` queue, shipped in the Product-layer template** (1.2 → 1.3) so every Installation starts with it. A scheduled item is dimensioned work; a finding is a candidate that is not. Four dispositions — Scheduled, Open, Deferred, Rejected — and `commands/next.md` Step 3 (2.2 → 2.3) reads the queue alongside the scheduled items, so an open finding competes on the priority formula instead of waiting to be remembered. See DECISION-025.

- **`.github/scripts/check_future_actions.py`, wired into CI.** Most of the disposition rule cannot be checked by a machine — detecting a finding inside prose has no pattern (Learning-015). This is the part that can: `AI/memory/learnings.md` has fixed structure, so every `Future action:` must carry a `Disposition:` citing an id or stating that no work is implied. Confirmed to fail on the pre-sweep tree naming all 23 future actions.

  Population exact and printed; classifier declared shallow — it cannot tell a correct disposition from a plausible one. It does not cover findings parked in roadmap prose, and says so. This repository's CI only, until `kenovis check` ships.


- **`DECISIONS.md` gains a Decision Index**, and the Product-layer template ships the section empty so every Installation starts with it (`.kenovis/AI/templates/product-layer/DECISIONS.md` 1.1 → 1.2) instead of discovering the need for one at 100 KB. The item that scheduled this work assumed the index already existed; it did not — what sat at the head of the file listed fourteen of seventeen decisions by bare title. See `AI/memory/learnings.md` Learning-023.

- **`.github/scripts/check_decision_index.py`, wired into CI** beside the three existing docs-integrity guards. Reading an index instead of a log is only safe while the index is complete: a decision body with no index line is invisible to every session that follows it. The check fails on a body without an index line, an index line without a body, a duplicate, or a line that states a title and little else. Confirmed to fail on the pre-change tree before being kept.

  Per Learning-021 and Learning-022 the script states which of its parts is exact and which is not: the population — every `# DECISION-NNN` heading and every `- **DECISION-NNN**` index line — is exact and both counts print on every run; the substance test is a length heuristic and cannot tell a long empty line from a short useful one. Its scope is this repository only, because the bundle ships `.kenovis/AI/` and not `.github/` — a customer Installation carries the rule as an instruction with no guard behind it.

- **`.kenovis/AI/policies/documentation.md` → "A Decision Is Not Recorded Until Its Index Line Exists"** (2.2 → 2.3): writing a decision body and writing its index line are one change, never two; the index line says what was settled and never why; superseding updates the index line and leaves the body in place.

### Changed

- **A finding a round does not fix is now scheduled or rejected, never just described.** The framework had three sinks for knowledge — `DECISIONS.md` (why), `AI/memory/learnings.md` (lesson), `PRODUCT/ROADMAP.md` (what and when) — and every command routed findings to the first two. Only the third is read to decide what to do next. Nothing anywhere said that a finding implying work becomes a scheduled item, so findings landed in the narrative of whichever item was open, with no id, no priority and no life after that item closed.

  Measured in this repository before the fix: 13 findings parked in `PRODUCT/ROADMAP.md` prose, 24 `Future action:` entries in `learnings.md` of which 5 named the roadmap, and the oldest open finding — `kenovis sync` never naming the paths it deletes — untouched since 2026-08-09 despite being repeated in four rounds' closing paragraphs.

  `.kenovis/AI/policies/documentation.md` → "A Finding Is Fixed, Scheduled, Or Rejected" (2.3 → 2.4) states the rule: three dispositions, and **being described in prose is not one of them**. Rejection is first-class — most findings should not become work. Cited from every command that can produce a finding: `analyze.md` (2.1 → 2.2), `bug.md` (2.2 → 2.3), `review.md` (2.1 → 2.2), `feature.md` (2.2 → 2.3), `next.md` (2.1 → 2.2), `architect.md` (2.2 → 2.3), `workflows/review.md` (2.3 → 2.4), `workflows/hotfix.md` (2.2 → 2.3). The learnings template (1.2 → 1.3) now requires a `Future action:` to cite an id or state that no work is implied.

- **`/analyze` was forbidden from doing what its own Step 9 required.** Step 9 said an analysis with a durable residue belongs in "a scheduled item in `PRODUCT/ROADMAP.md`"; sixty lines below, "AI Responsibilities" said **"AI must not: Modify files."** The one command whose entire purpose is detection could not record what it detected. `analyze.md`'s prohibition now names what it actually means to forbid — implementing, changing product code or documents to apply a finding, inventing requirements — and states the recording obligation explicitly. The command detects and records; it never fixes.

- **The session-initialization protocol reads `DECISIONS.md` as an index, not as seventeen decision bodies.** A decision log is append-only by design and grows without bound; every session was paying the whole file to consult none of it. Measured over 2026-07-30 → 2026-08-10 in this repository, `DECISIONS.md` went 9.8 KB → 119.9 KB in eleven days, and the mandatory read of `COMPANY_OS.md` + `DECISIONS.md` + `AI/SYSTEM.md` stood at ~138 KB before any work started.

  `COMPANY_OS.md` and `.kenovis/AI/SYSTEM.md` stay full reads. `DECISIONS.md` is read as its Decision Index — one line per decision, stating what that decision settled. A body is opened on demand, and **citing a decision requires opening it**: the index says what was settled, never why, and a citation built on the index alone is a preference wearing a decision's ID.

  Stated in `.kenovis/AI/SYSTEM.md` → "Context Loading Rules" (1.4 → 1.5), `.kenovis/AI/commands/bootstrap.md` Step 2 (2.5 → 2.6), and this repository's own root `CLAUDE.md` (2.1 → 2.2). Two further sites told an agent to read the log rather than the index and were found by enumerating every read instruction rather than the three the item named (Learning-022's rule): `agents/database.md` (1.1 → 1.2), which sent a designer to the whole log for the engine and tenancy model, and `workflows/framework-review.md` (1.0 → 1.1), whose reading scope named DECISION-001/009/010 by number and now names the index's framework-level marks. Measured on the read path: this repository 138.2 KB → 22.2 KB (**-84%**); an Installation whose log has grown to the same size, 135.6 KB → 19.7 KB (-85%). A day-one Installation whose log is still the template saves 2.6 KB of 19.2 KB — the bound matters as the log accumulates, which is the point.

## Earlier releases

Kept verbatim in [CHANGELOG-ARCHIVE.md](CHANGELOG-ARCHIVE.md), one line each here. `[Unreleased]` and the two most recent released versions stay above; everything older is read on demand.

| Version | Date | What it carried |
|---|---|---|
| [0.12.0] | 2026-08-10 | Nine instructions named no destination for the artifact they required; all six workflows' unmatched code fences fixed; `check_artifact_destinations.py` wired into CI. DECISION-024, Learning-022. |
| [0.11.0] | 2026-08-10 | `/bug` and `/release` still named a template as a destination — the [0.10.0] fix had missed them; five working templates' fences fixed; `check_template_refs.py` wired into CI. DECISION-024. |
| [0.10.0] | 2026-08-09 | Every workflow that told an agent to write a document named a path inside `.kenovis/`, which `sync` mirror-replaces. DECISION-024. |
| [0.9.0] | 2026-08-09 | Line 1 of every Product-layer document told the next agent it was reading placeholder content. DECISION-023, DECISION-022, DECISION-019. |
| [0.8.0] | 2026-08-09 | Three Product-layer templates shipped this framework's own answers as framework-level prose, and every Installation inherited them. Learning-017. |
| [0.7.0] | 2026-08-09 | The Verify step could not tell an unanswered template question from content meant to survive. DECISION-022, DECISION-020. |
| [0.6.0] | 2026-08-08 | `/init-project` and `/adopt-project` made to work inside a real CLI Installation. DECISION-021, DECISION-020. |
| [0.5.0] | 2026-08-07 | **Breaking (repository layout, not the published package)** — this repository's own Framework layer moved under `.kenovis/AI/`. DECISION-017, DECISION-020, DECISION-018. |
| [0.4.0] | 2026-08-06 | `sync` refuses to overwrite a root `CLAUDE.md` it does not manage instead of discarding it silently; `cli/README.md` gained "Upgrading". Learning-006/007/008. |
| [0.3.0] | 2026-08-06 | `kenovis add`, bare `kenovis <dir>` autodetect dispatch, and the first-session auto-trigger for setup. DECISION-018, DECISION-019. |
| [0.2.0] | 2026-08-06 | First versioned release of the framework layer, aligned with the `kenovis` npm package's own version. Apache 2.0, `CONTRIBUTING.md`. DECISION-010 through DECISION-017. |

## How to add an entry

When a framework-layer change lands:

1. Add a bullet under `[Unreleased]`, in `Added` / `Changed` / `Deprecated` / `Removed` / `Fixed` / `Security` as appropriate.
2. On release, rename `[Unreleased]` to `[x.y.z] - YYYY-MM-DD` and start a fresh empty `[Unreleased]` above it.
3. Then trim: any released section below the two most recent moves **verbatim** to `CHANGELOG-ARCHIVE.md`, leaving one row in "Earlier releases" above. This keeps the file under the 60 KB lifecycle threshold (`.kenovis/AI/policies/documentation.md` → "Document Lifecycle") without ever summarising a released section away.

Product-layer changes (a specific team's `DECISIONS.md`, `DOMAIN/`, etc.) belong in that product's own history — not here.
